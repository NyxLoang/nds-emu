---
description: 项目目录结构与模块化约定：新功能按 src/<模块>/ 组织，边界清晰、代码简单整洁；结构变更必须同步更新 docs/01-project-layout.md
alwaysApply: true
---

# 项目目录结构与模块化约定

本项目的目录结构与模块化规范以 [`docs/01-project-layout.md`](docs/01-project-layout.md) 为准。开发任何新功能、新模块时必须遵循它。

## 核心要求

1. **按模块组织**：每个功能模块一个目录 `src/<模块名>/`，内含 `<模块名>.h`、`<模块名>.c`、`<模块名>log.md`。
2. **接口 + 功能文件**：模块内有多个子功能时，拆成 `<功能>.h/.c` 功能文件；`<模块名>.h/.c` 作对外接口文件。外部只依赖接口文件（模块间解耦），功能文件各司其职（模块内高内聚）。
3. **两层分层**：
   - `ndscore` 核心库：纯模拟逻辑，不依赖 SDL（供 `nds-emu` 与 `test_nds` 共用）。
   - 宿主侧（`main.c` + `window/` + `menu/` + `audio/` + `demo/` + `runner/` + `ppu.c`）：SDL 窗口/渲染/音频设备/演示/诊断。
4. **main.c 保持瘦身**：`main.c` 只做入口 + 参数解析 + 装载编排 + 主循环 + 键位映射。SDL 音频宿主、演示脚手架、headless 诊断等独立职责应拆到对应模块（`audio/`、`demo/`、`runner/`），不要让 `main.c` 堆砌无关代码。

## 目标

- 功能模块化
- 边界清晰化
- 代码简单整洁化

## 变更约束

**如果新增/移动/删除/重命名了目录或模块，必须同步更新 `docs/01-project-layout.md` 的目录树与说明**，保证文档与实际结构一致。

## 反面例子（避免）

- 把 SDL 音频设备、2D demo、headless runner 全部塞进 `main.c`（已拆分，勿回退）。
- 新模块不建目录、不写 `<模块名>log.md`、不在 `CMakeLists.txt` 登记。
