# 构建产物
build/
build-prof/
build-prof2/

# 真实卡带 ROM：体积大且受版权保护，不进入版本库（测试用假 ROM 见 homebrew/）
tools/*.nds
tools/*.sav

# 历史遗留：早期中断安装时下载的 CMake 便携包（工具链已由 WinLibs 提供）
tools/cmake.zip

# 无头运行默认写到当前目录的截图/图像产物（诊断用，不入库）
headless_shot*.bmp
/*.bmp
