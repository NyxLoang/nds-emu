# NDS 模拟器：分阶段微步骤拆解

> 把原 NDS 模拟器各阶段拆成可单独完成、可验收的微步骤；当前只定路线图，不一次做完。  
> **每完成一小微步都必须等你验证通过后，才进入下一步。**  
> 预习已全部完成：`docs/00-what-is-an-emulator.md`、`docs/01-project-layout.md`、`docs/00b-hex-endianness.md`。

## 怎么用这份拆解

- **一次只做一个微步骤**，绝不连做多步。
- **计划门禁（强制）**：每个大阶段和每个小微步开始之前，必须先产出并展示该步的计划（做什么、改哪些文件、怎么验证、验收标准），**等你确认计划后才开始编码**。阶段级计划在阶段开头给出；微步级计划在每一步开头给出。未确认计划前不得动手。
- **阶段级详细计划与验收（强制，新增）**：每个大阶段开始编码前，除阶段级计划外，还需给出三样东西，等你确认后才动手：①**详细开发计划**（每个微步改哪些文件、实现要点）；②**测试用例清单**（每个微步的用例名 + 关键断言点）；③**验收标准**（可量化的通过判据）。阶段完成后统一汇报时，必须附上**测试验收步骤**（跑哪条命令、逐条看什么输出）与**验收通过的最终呈现结果**（如 `ctest` 全绿、`test_nds.exe` 共 N 项检查 0 失败、某寄存器/内存/画面达到某具体值）。
- **验证门禁（强制）**：每完成一小微步后必须停住，向你说明：做了什么、怎么验证、验收标准是否达成；**等你明确说通过 / 继续** 后，才开始下一微步。你未验证前不得擅自推进。
  > 阶段 1（1.5–1.7）经你授权实行**统一确认模式**：每步计划+实施+自测通过后直接推进，不逐项停下；阶段 1 全部完成后统一汇报确认。后续阶段如需同样豁免，另行授权。
  > 阶段 2（2.1–2.8）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
  > 阶段 3a（3a.1–3a.7）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
  > 阶段 3b（3b.1–3b.8）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
  > 阶段 4（4.1–4.6）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
  > 阶段 12 起（12–21）经你授权实行**全程自动推进模式**：阶段级计划已按「最终能顺畅玩 .nds 游戏」这个总目标审校过，不再逐阶段等你确认计划/验收；每个阶段「计划+实施+自测（`ctest` 全绿 + `test_nds.exe` 0 失败）」通过后，直接进入下一阶段；仅在阶段全部完成时统一汇报（附测试验收步骤 + 最终呈现结果）。
- **注释约定（强制）**：涉及内存地址换算、位计算、小端拼拆等逻辑处理处，必须写中文注释说明「为什么/怎么算」，让读代码者能快速理解（如 `read_le32` 的 `b0 | b1<<8 | b2<<16 | b3<<24`、地址区间偏移换算）。功能入口给出「做什么」注释，关键分支说明意图。
- **版本管理门禁（强制）**：每个被你确认通过的微步改动，必须立即 `git commit` 固化，再进入下一步。提交遵循仓库分支工作流（当前在 `develop` 开发），提交信息用 `<微步号>: <一句话说明>` 格式（如 `0.6: 顶/底屏两种纯色`）；日志（`DEVLOG.md` 索引 + 模块 `*log.md`）与代码改动一起提交。未验证通过前不提交。
- **模块结构规则（强制）**：每个功能模块一个目录 `src/<模块名>/`；模块内若有多个子功能，按功能拆成 `<功能>.h/.c` 功能文件（如 cart 模块的 `arm9.h/.c`、`arm7.h/.c`）；`<模块名>.h/.c` 作**对外接口文件**，外部（其他模块、main.c）只依赖接口文件——**模块之间解耦**；功能文件各司其职、互不依赖——**模块内部功能内聚**。
- 每步结束时给出简短「请你验证」清单（例如：跑哪条命令、该看到什么、读哪篇文档）。
- 每步尽量满足：能编译 / 能打印 / 能看见窗口变化，三选一即可。
- 技术栈不变：C11、CMake、SDL2；测试用 C 数组机器码。
- 仓库现状：预习 P1–P4 完成；阶段 0 的 0.1–0.6 已完成（工具链 gcc/cmake、hello、SDL2、双屏主循环、空 `nds_t`、顶/底屏纯色）；开发日志见 `DEVLOG.md` 与各模块 `*log.md`。

---

## 预习 — 概念（写代码前）

| 微步 | 做什么 | 验收 | 状态 |
|------|--------|------|------|
| P1 | 读/补全 `docs/00`：取指→译码→执行 | 能口述这三步 | ✅ 已有 |
| P2 | 读 `docs/01`：目录职责 | 能说出 cpu/bus/cart/ppu 各干什么 | ✅ 已有 |
| P3 | 短文：十六进制与小端 → `docs/00b-hex-endianness.md` | 能解释 `0x12345678` 在小端内存里的字节序 | ✅ 完成 |
| P4 | 短文：寄存器 vs 内存 vs PC | 能说明「指令为什么是内存里的数字」 | ✅ 已并入 `docs/00` |

---

## 阶段 0 — 工程骨架

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 0.1 | 安装/配置本机 C 编译器 + CMake（便携工具进 `tools/` 或你本机已有工具） | `cmake --version`、`gcc --version` 可用 |
| 0.2 | 根目录 `CMakeLists.txt` + `src/main.c` 打印 `nds-emu` | 命令行跑出这句话 |
| 0.3 | 引入 SDL2（vcpkg / 预编译库 / FetchContent，选定一种写进 README） | 能 `#include <SDL.h>` 并链接 |
| 0.4 | 开 256×384 窗口，主循环清屏 + 处理退出 | 弹出双屏高度窗口，可关掉 |
| 0.5 | 定义空 `nds_t`（`nds.h` / `nds.c`），`main` 里创建一份 | 结构体能编译通过 |
| 0.6 | 顶/底屏用两种纯色区分（仍无模拟） | 窗口上下颜色不同 |

---

## 阶段 1 — `.nds` 装载（不执行）

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 1.1 | `docs/02-nds-rom-header.md`：头里 ARM9 四个关键字段 | 知道 offset/entry/ram/size 含义 |
| 1.2 | `cart`：读整个文件到缓冲区 | 打印文件大小 |
| 1.3 | 按偏移解析 ARM9 头字段并打印 | 假 ROM 或真 homebrew 打印合理十六进制 |
| 1.4 | 解析 ARM7 对应字段并打印 | 同上 |
| 1.5 | 造最小假 `.nds`（脚本或手工）放进 `homebrew/` | 不依赖商业 ROM 可测 |
| 1.6 | 把 ARM9 镜像拷进稍后的 RAM 缓冲区（可先是裸 `uint8_t[]`） | 拷贝后内存前几字节与文件一致 |
| 1.7 | CLI：`nds-emu path.nds` 打印装载摘要 | 一条命令完成 1.3–1.6 的展示 |

---

## 阶段 2 — 内存总线

| 微步 | 做什么 | 验收 | 状态 |
|------|--------|------|------|
| 2.1 | `docs/03-memory-map.md`：Main RAM / VRAM / IO 地址 | 能指认 `0x02000000`、`0x06000000` | ✅ |
| 2.2 | `bus` 里分配 Main RAM、VRAM 数组并挂到 `nds_t` | 初始化不崩 | ✅ |
| 2.3 | `bus_read8` / `bus_write8` + 地址落在 Main RAM 的换算 | 写再读回同一字节 | ✅ |
| 2.4 | `read16` / `write16`（小端） | 单测：写 `0xABCD` 两字节顺序正确 | ✅ |
| 2.5 | `read32` / `write32`（小端） | 同上 | ✅ |
| 2.6 | VRAM 区间映射 | 写 VRAM 地址能读回 | ✅ |
| 2.7 | IO 桩：未实现读 0、写忽略（可打日志） | 访问 `0x04000000` 不崩 | ✅ |
| 2.8 | 与阶段 1 衔接：装载后从 `ram_address` 读回镜像头 | 与文件内容一致 | ✅ |

---

## 阶段 3a — CPU 骨架（只会空转）

| 微步 | 做什么 | 验收 | 状态 |
|------|--------|------|------|
| 3a.1 | `docs/04-cpu-loop.md` | 理解 PC 与取指 | ✅ |
| 3a.2 | `arm_cpu_t`：`r[16]`、`cpsr` | 能打印寄存器 | ✅ |
| 3a.3 | `cpu_fetch`：按 PC 从 bus 读 32 位 | 给定内存能取到写入的字 | ✅ |
| 3a.4 | `cpu_step` 空壳：取指后 PC+=4 | 单步后 PC 正确前进 | ✅ |
| 3a.5 | 识别「无条件 B 到自己」（死循环）一种编码 | 多步后 PC 停在同一地址 | ✅ |
| 3a.6 | 未实现指令：打印机器码并停机/计数 | 遇到未知码有明确日志 | ✅ |
| 3a.7 | 主循环：每帧执行固定 N 步（画面可仍黑） | 窗口仍在，日志 PC 打转 | ✅ |

---

## 阶段 3b — 逐条指令（核心）

每一类指令都固定节奏：**编码说明 → 实现 → C 数组单测 → 再下一条**。

| 微步 | 指令/能力 | 验收 | 状态 |
|------|-----------|------|------|
| 3b.1 | 条件码执行（先只 AL 总是执行） | 框架就位 | ✅ |
| 3b.2 | `MOV` 立即数 | `r0` 变成期望值 | ✅ |
| 3b.3 | `ADD` / `SUB` 立即数或寄存器 | 算术单测过 | ✅ |
| 3b.4 | `CMP` + 更新 flags | 后接条件分支可测时再验 | ✅ |
| 3b.5 | `LDR` 立即偏移 | 从 RAM 读进寄存器 | ✅ |
| 3b.6 | `STR` 立即偏移 | 寄存器写入 RAM | ✅ |
| 3b.7 | `B` 相对跳转 | PC 跳到目标 | ✅ |
| 3b.8 | `BL` + `BX lr`（最小调用返回） | 调用后能返回 | ✅ |
| 3b.9 | 需要的 `AND`/`ORR`/`EOR` | 位运算单测过 | ✅ |
| 3b.10 | 综合：机器码写一个字到 VRAM 地址 | 内存转储看到颜色字 | ✅ |
| 3b.11 | （暂缓）Thumb | 本阶段不做 | — |

---

## 阶段 4 — 最小显示（假 PPU）

  > 阶段 4（4.1–4.6）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
  > 4.1–4.6 已全部完成 ✅（2026-08-09 统一确认）。

| 微步 | 做什么 | 验收 | 状态 |
|------|--------|------|------|
| 4.1 | `docs/05-framebuffer.md`：像素与 RGB555/888 | 知道一个像素怎么存 | ✅ |
| 4.2 | 约定：线性 framebuffer 在 VRAM 某基址 | 文档写清宽高与格式 | ✅ |
| 4.3 | `ppu`：把缓冲转成 SDL 纹理/像素 | CPU 手工 `memset` 色块能上屏 | ✅ |
| 4.4 | 顶屏画测试图、底屏纯色 | 上下可区分 | ✅ |
| 4.5 | 接 CPU：跑写 VRAM 的测试码再刷新 | 不靠主机 `memset`，靠模拟 CPU 出图 | ✅ |
| 4.6 | 简单缩放（如 2×）方便看 | 窗口更大但仍是双屏比例 | ✅ |

---

## 阶段 5 — 验收用例固化

> 阶段 5（5.1–5.6）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
> 5.1–5.6 已全部完成 ✅（2026-08-09 统一确认，`ctest` 40 项检查 0 失败）。

| 微步 | 做什么 | 验收 | 状态 |
|------|--------|------|------|
| 5.1 | `tests/test_nds.c` 统一测试入口 + `ndscore` 库 + `ctest` | `ctest --test-dir build` 全绿 | ✅ |
| 5.2 | 用例：清屏 | 屏为约定底色 | ✅ |
| 5.3 | 用例：画矩形 | 可见矩形 | ✅ |
| 5.4 | 用例：死循环保活 | 不崩 | ✅ |
| 5.5 | README：如何跑测试 | 按文档能复现 | ✅ |
| 5.6 | 可选文档：以后若装 devkitARM（不实现） | 仅文档 | ✅ |

---

## 阶段 6 — 中断 / 定时器 / 按键

> 阶段 6（6.1–6.7）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
> 6.1–6.7 已全部完成 ✅（2026-08-09 统一确认，`ctest` 70 项检查 0 失败）。
> 决策：IO 寄存器单独成 `src/io/` 模块（不等阶段 9），按模块结构规则拆 irq/timer/key 功能文件。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 6.1 | 短文：中断 = CPU 被打断去跑处理程序 | 能复述 ✅ |
| 6.2 | `IME` / `IE` / `IF` 读写 | 寄存器读写正确 ✅ |
| 6.3 | 用「指令计数」近似产生 VBlank 标志 | IF 里 VBlank 位置会置位 ✅ |
| 6.4 | 最小 IRQ 响应（可先只置位，稍后再进异常） | 日志或程序能看到 VBlank ✅ |
| 6.5 | Timers 0–3：启停、分频简化 | 计数会涨 ✅ |
| 6.6 | `KEYINPUT` + SDL 键位映射 | 按键改变读值 ✅ |
| 6.7 | homebrew/测试：等 VBlank 或读键 | 行为可演示 ✅ |

---

## 阶段 7 — DMA（精简）

> 阶段 7（7.1–7.4）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
> 7.1–7.4 已全部完成 ✅（2026-08-09 统一确认，`ctest` 90 项检查 0 失败）。
> 7.5（HBlank 触发）按计划「可后做」，本阶段未实现。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 7.1 | 短文：DMA = 硬件搬内存 | 能复述 ✅ |
| 7.2 | 一条通道：源/目的/长度/启动位 | 寄存器可写 ✅ |
| 7.3 | 立即模式：启动后拷贝字/半字 | 目的区数据正确 ✅ |
| 7.4 | 用 DMA 填 VRAM 色块 | 屏上可见 ✅ |
| 7.5 | （可后做）HBlank 触发简化 | 有测试再开 — 未做 |

---

## 阶段 8 — ARM7 + IPC

> 阶段 8（8.1–8.7）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
> 8.1–8.7 已全部完成 ✅（2026-08-09 统一确认，`ctest` 117 项检查 0 失败）。
> 按用户要求扩展：CPU 拆 `arm9`/`arm7` 功能文件；FIFO 做到「最终完整形态」
> （16 字双向队列 + CNT 全状态位 + 边沿中断 IF17/18，前置把中断寄存器按 CPU 分流）。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 8.1 | 短文：双核分工与共享内存 | 能复述 ✅ |
| 8.2 | 复用/裁剪 CPU 核为 ARM7 实例（`cpu` 接口 + `arm9`/`arm7` 功能文件 + `is_arm7`） | 第二颗 CPU 能 step ✅ |
| 8.3 | 装载 ARM7 镜像到其内存视角（ARM7 WRAM `0x03800000` 64KB） | 入口可取指 ✅ |
| 8.4 | 交错调度（ARM9:ARM7 = 2:1） | 两边 PC 都在动 ✅ |
| 8.5 | 中断按 CPU 分流（IME/IE/IF 两套） | 两核读各自 IF 不串扰 ✅ |
| 8.6 | IPC FIFO 完整（队列+CNT+SEND/RECV+IF17/18） | 一边写一边读到 ✅ |
| 8.7 | 双核测试：FIFO 传值 + 底屏体现 | 可演示同步 ✅ |

---

## 阶段 9 — 真 2D PPU

> 阶段 9（9.1–9.7）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
> 9.1–9.7 已全部完成 ✅（2026-08-14 统一确认，`ctest` 148 项检查 0 失败）。
> 成果：弃旧「VRAM 偏移=屏幕 framebuffer」，改为 DISPCNT/BGxCNT 寄存器驱动的真 2D 引擎——
> 直色位图 + Mode 0 tile（tilemap/调色板/透明/翻转/优先级）+ OBJ sprite + 副引擎（Engine B）对称；
> `src/ppu/render.h/.c` 纯渲染（SDL 无关）+ `src/io/disp.h/.c` 显示寄存器 + bus 调色板/OAM/VRAM 窗口。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 9.1 | 短文：BG/tile/palette 是什么 | 能复述 ✅ |
| 9.2 | 读显示控制寄存器（DISPCNT 等最小集） | 读写正确 ✅ |
| 9.3 | Engine A：Mode 3 或线性 FB 模式对齐真机 | 真模式出图 ✅ |
| 9.4 | Engine A：Mode 0 一层 BG + 调色板 | tilemap 出图 ✅ |
| 9.5 | Engine B 对称最小集 | 底屏用引擎 B ✅ |
| 9.6 | OBJ（精灵）最小：一个 sprite | 屏上有活动块 ✅ |
| 9.7 | 用非裸 FB 的 2D homebrew/自造数据验收 | 不靠假 FB 约定 ✅ |

---

## 阶段 10 — ARM 指令集补全 + SWI

> 主线：让真实 .nds 代码（含栈操作、模式切换、半字访存、BIOS 调用）能正确执行。
> 沿用阶段 3b 节奏：**编码说明 → 实现 → C 数组单测 → 再下一条**。
> 阶段 10（10.1–10.11）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
> 10.1–10.11 已全部完成 ✅（2026-08-15 统一确认，`ctest` 204 项检查 0 失败）。
> 成果：`exec.c` 补全移位操作数、9 种数据处理、MRS/MSR（含 SPSR/CP15 桩）、LDM/STM（PUSH/POP）、
> MUL/MLA + 长乘、字节/半字访存 + 变址、SWI/SWP/MRC/MCR；`cpu.h` 增 `spsr`/`swi_num`/`cp15[16]`。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 10.1 | 短文：为什么还需要这些指令（真码里 `MRS/MSR`、`LDM/STM`、`SWI` 的用途） | 能复述 ✅ |
| 10.2 | 移位操作数：`LSL/LSR/ASR/ROR` 作第二操作数 | `ADD r0,r1,r2,LSL#3` 单测过 ✅ |
| 10.3 | 更多数据处理：`MVN/BIC/ADC/SBC/RSB/RSC/TST/TEQ/CMN` | 位运算/比较单测过 ✅ |
| 10.4 | `MRS` / `MSR`（读/写 CPSR、SPSR） | 能读/写 CPSR 且不破坏模式位 ✅ |
| 10.5 | `LDM` / `STM`（含 `PUSH/POP` 别名，IA/DB 模式） | 块搬移、栈保存/恢复单测过 ✅ |
| 10.6 | 乘法：`MUL/MLA` + 长乘 `UMULL/UMLAL/SMULL/SMLAL` | 乘法结果单测过 ✅ |
| 10.7 | 字节/半字访存：`LDRB/STRB/LDRH/STRH/LDRSB/LDRSH` + 前/后变址、寄存器偏移 | 半字/字节/符号扩展单测过 ✅ |
| 10.8 | `SWI` 指令（先记录软件中断号，不跳异常） | 执行后能读到 SWI 号 ✅ |
| 10.9 | `SWP`（寄存器+内存交换） | 交换语义单测过 ✅ |
| 10.10 | `MRC` / `MCR` 协处理器访问（CP15 先做桩） | 游戏写的 CP15 控制字能被存取 ✅ |
| 10.11 | 综合：一段用满新指令的真码（栈保存 + SWI 调用 + 半字循环搬 VRAM） | 出图或内存结果正确 ✅ |

---

## 阶段 11 — BIOS HLE + SWI 分发

> 真游戏启动时大量调用 BIOS 函数（经 `SWI <编号>`），用 C 直接实现拦截，免于执行 BIOS ROM。
> 阶段 11（11.1–11.7）沿用**统一确认模式**：阶段末统一汇报，不逐项停下。
> 11.1–11.7 已全部完成 ✅（2026-08-15 统一确认，`ctest` 256 项检查 0 失败）。
> 成果：新建 `src/bios/` 模块（接口 `bios_dispatch` + 功能文件 arith/mem/decompress/wait）；
> `exec.c` 的 SWI 改为「`swi_num>>16` 取号 → HLE 分发，等待未满足则 PC 不动重跑」。
> 注：`DivArm` 是 GBA 专属（NDS 已移除），故 11.3 只实现 `Div`/`Sqrt`。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 11.1 | 短文：BIOS / SWI / HLE 概念 + NDS SWI 编号表 → `docs/12-bios-hle.md` | 能复述 ✅ |
| 11.2 | SWI 分发框架：`src/bios/` 模块 + `SWI <n>` → `bios_dispatch(n, regs)` | 未知号不崩 ✅ |
| 11.3 | 除法/开方：`Div`(0x09)/`Sqrt`(0x0D) | 数值单测过 ✅ |
| 11.4 | 内存搬移：`CpuSet`(0x0B)/`CpuFastSet`(0x0C) | 块搬移单测过 ✅ |
| 11.5 | 解压：`BitUnPack`(0x10)/`LZ77`(0x11)/`RL`(0x14)/`Huffman`(0x13) | 解压结果单测过 ✅ |
| 11.6 | 等待：`Halt`(0x06)/`IntrWait`(0x04)/`VBlankIntrWait`(0x05) | 能等 VBlank 返回 ✅ |
| 11.7 | 综合：一段调多个 SWI 的真码（LZ77 + Div + Sqrt） | 结果正确 ✅ |

---

## 阶段 12 — CP15 + 异常向量 + 中断真实化

> 阶段 12（12.1–12.5）沿用**全程自动推进模式**：阶段内自测通过即推进，阶段末统一汇报。
> 12.1–12.5 已全部完成 ✅（2026-08-15，`ctest` 282 项检查 0 失败）。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 12.1 | 短文：特权模式、CPSR 模式位、异常向量表 | 能复述 ✅ |
| 12.2 | 异常向量：SWI/IRQ/FIQ/未定义/中止 → 跳 `0x08/0x18/...` | 触发后 PC 到正确向量 ✅ |
| 12.3 | CPSR 模式切换 + SPSR 保存/恢复（`cpsr`/`spsr[5]`） | 进出 handler 状态正确 ✅ |
| 12.4 | CP15 控制寄存器桩（cache/MMU 相关读/写） | 游戏写 CP15 不崩 ✅ |
| 12.5 | IRQ 真实响应：VBlank 中断进 handler、返回 | 中断回调被执行 ✅ |

---

## 阶段 13 — Thumb 指令集（关键：真实游戏约 90% 是 Thumb 码）

> 3b.11 曾暂缓 Thumb。**要顺畅玩 .nds 游戏，Thumb 是硬前提**——游戏主体代码几乎全是 Thumb。
> 阶段 13 补齐 16 位 Thumb 指令集 + Thumb↔ARM 切换（BX），与阶段 12 的异常机制正交。
> ✅ 阶段 13 已完成（2026-08-15）：336 项检查 0 失败。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 13.1 | 短文：Thumb 16 位编码、T 位、`BX/BLX` 切换 | 能复述 |
| 13.2 | Thumb 译码框架：`thumb_step`（16 位取指、CPSR T 位、与 ARM 分发并行） | 单条 Thumb 指令能 step |
| 13.3 | Thumb 移位/数据处理（MOV/ADD/SUB/CMP/AND/ORR/EOR/MUL/移位类） | 数值单测过 |
| 13.4 | Thumb 访存（LDR/STR/字面量池/SP 相对/LDRB/STRB/LDRH/STRH） | 访存单测过 |
| 13.5 | Thumb 分支（B/BL/BX/BLX/条件分支）+ PUSH/POP/LDMIA/STMIA | 调用返回单测过 |
| 13.6 | Thumb 杂项（高寄存器操作、`SWI` 进 HLE） | SWI 可分发 |
| 13.7 | 综合：一段 Thumb 真码跑通（写 VRAM/调 SWI） | 结果正确 |

---

## 阶段 14 — 商业 ROM 装载 + 安全区解密（关键）

> 商业 .nds 的 ARM9/ARM7 二进制是 **Blowfish 加密**的（除头部外）。阶段 1 只能装 homebrew。
> 本阶段实现安全区解密，让真正的商业 ROM 能被装载启动（不依赖真 BIOS ROM）。
> ✅ 阶段 14 已完成（2026-08-15）：360 项检查 0 失败（KEY1 表自检 + 块加解密往返 + 安全区加密→解密 + 自制加密 ROM 完整装载）。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 14.1 | 短文：NDS ROM 安全区加密（Blowfish + secure area key 派生） | 能复述 |
| 14.2 | Blowfish 加解密实现（C，带已知向量单测） | 与标准向量一致 |
| 14.3 | 解析安全区 + 派生游戏子密钥 + 解密 ARM9/ARM7 镜像 | 解密后与明文一致 |
| 14.4 | 装载解密镜像到 RAM 并从 entry 启动 | PC 落在正确入口 |
| 14.5 | 综合：自制含加密区的 ROM 走完整装载流程 | 可启动 |

---

## 阶段 15 — 卡带协议 + DMA 补全

> 真游戏运行时**按需流式读卡带**（经 ROMCTRL/CARD_COMMAND/CARD_DATA），而非一次性整 ROM 装进 RAM。
> 本阶段实现卡带总线 cartbus（命令解码 + 数据端口）+ DMA 补全（4 通道/地址控制/repeat/触发源），
> 打通「游戏 → B7 命令 → 卡带 DMA → RAM」这条关键装载路径。
> ✅ 阶段 15 已完成（2026-08-15）：393 项检查 0 失败。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 15.1 | 短文：NDS 卡带命令协议（ROMCTRL/AUXSPICNT） | 能复述 |
| 15.2 | ROMCTRL 数据寄存器 + 命令写/数据读（游戏经此读 ROM 数据） | 读回卡带数据一致 |
| 15.3 | DMA 补全：4 通道 + 立即/重复/块模式 + 各触发源 | 各模式单测过 |
| 15.4 | DMA 读卡带（真游戏典型装载路径） | 装载数据正确 |
| 15.5 | 综合：DMA 从 ROM 搬数据到 RAM | 结果正确 |

---

## 阶段 16 — 存档（EEPROM / Flash）

> 游戏要能**保存进度**并在下次启动读回。NDS 存档芯片（backup memory）挂卡带槽辅助 SPI 总线
> （AUXSPICNT/AUXSPIDATA），与阶段 15 的 ROM 命令总线是两套独立接口。
> ✅ 阶段 16 已完成（2026-08-15）：423 项检查 0 失败（EEPROM/Flash 命令 + AUXSPI 接线 + .sav 持久化 + CPU 程序综合）。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 16.1 | 短文：NDS 存档类型（EEPROM 512B/8KB、Flash 256KB、FRAM） | 能复述 |
| 16.2 | 存档总线接口（SPI 兼容 EEPROM/Flash 命令） | 命令读写单测过 |
| 16.3 | 读/写存档 + 持久化到 `.sav` 文件 | 落盘读回一致 |
| 16.4 | 综合：游戏读/写存档数据正确 | 结果正确 |

---

## 阶段 17 — 触摸 SPI

> 游戏要能**读触摸输入**（点菜单、划地图）。触摸屏控制器（TSC）挂主 SPI 总线（SPICNT/SPIDATA），
> 与存档用的辅助 SPI（AUXSPICNT/AUXSPIDATA）是两套独立接口。
> ✅ 阶段 17 已完成（2026-08-15）：443 项检查 0 失败（SPICNT/SPIDATA + TSC 命令状态机 + CPU 程序读出 X/Y 坐标）。

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 17.1 | 短文：NDS 触摸屏 + SPI 总线 | 能复述 |
| 17.2 | SPI 控制寄存器 + 触摸坐标读取 | 点按能读出坐标 |

---

## 阶段 18 — 音频

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 18.1 | 短文：NDS 16 通道音频（PCM/ADPCM） | 能复述 |
| 18.2 | 音频寄存器 + 混音 + 回调发声 | 能出声音 |

---

## 阶段 19 — 3D

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 19.1 | 短文：NDS 3D 几何引擎 | 能复述 |
| 19.2 | 几何命令 → 顶点/三角形 → 光栅化 | 3D 出图 |

---

## 阶段 20 — 2D PPU 补全（旋转/缩放/捕获）

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 20.1 | 旋转/缩放背景（MODE1/2 + 仿射变换） | 仿射出图 |
| 20.2 | 显示捕获 / 混合 / 亮度 | 效果可演示 |
| 20.3 | 窗口 / 更多混合效果 | 效果可演示 |

---

## 阶段 21 — 真实 .nds 兼容性验收（最终目标）

| 微步 | 做什么 | 验收 |
|------|--------|------|
| 21.1 | 选取 homebrew/自制 ROM 跑通（出图 + 输入 + 声音） | 可玩 |
| 21.2 | 商业 ROM 能进标题/游戏（先验未加密部分） | 进标题 |
| 21.3 | 性能调优到可玩帧率 | 不卡顿 |
| 21.4 | 最终整体验收清单（出图/输入/声音/存档） | 全项通过 |

---

## 总览待办

- 预习 P1–P4：概念文档读完/补全 ✅（`docs/00`、`docs/00b`、`docs/01`）
- 阶段 0：工具链 → hello → SDL 双屏 → nds_t ✅
- 阶段 1：头解析 → 假 ROM → 装入 RAM → CLI ✅
- 阶段 2：map 文档 → 8/16/32 → VRAM/IO 桩 → 与装载衔接 ✅
- 阶段 3a：寄存器 → fetch → step → 死循环 B → 每帧 N 步 ✅
- 阶段 3b：逐条指令（条件码 → MOV/ADD/SUB/CMP → LDR/STR → B/BL/BX → AND/ORR/EOR → 写 VRAM 综合）全部完成 ✅
- 阶段 4：FB 约定 → SDL 出图 → CPU 驱动出图 → 默认 2× 缩放 ✅
- 阶段 5：统一测试入口 `tests/test_nds.c` + ctest；清屏/矩形/死循环用例；README 与 devkitARM 文档 ✅
- 阶段 6：IME/IE/IF → VBlank → IRQ 检测 → Timer → KEYINPUT → 等 VBlank/读键用例 ✅
- 阶段 7：单通道立即 DMA → 填 VRAM ✅
- 阶段 8：ARM7 实例 → 交错 → 中断分流 → FIFO 完整 ✅
- 阶段 9：真 2D PPU（DISPCNT → Mode3 直色位图 → Mode0 tile → Engine B → OBJ）全部完成 ✅
- 阶段 10：指令集补全 + SWI（移位操作数 → MRS/MSR → LDM/STM → 乘法 → 半字访存 → SWI → SWP → MRC/MCR → 综合）✅
- 阶段 11：BIOS HLE + SWI 分发（`src/bios/` 模块 → Div/Sqrt → CpuSet/CpuFastSet → 解压 → 等待 → 综合）✅
- 阶段 12：CP15 + 异常向量 + 中断真实化 ✅
- 阶段 13：Thumb 指令集（关键，真实游戏约 90% 是 Thumb 码）✅
- 阶段 14：商业 ROM 装载 + 安全区解密（Blowfish）✅
- 阶段 15：卡带协议 + DMA 补全（cartbus + 流式读卡带）✅
- 阶段 16：存档（EEPROM/Flash + .sav 持久化）✅
- 阶段 17：触摸 SPI ✅
- 阶段 18：音频待做
- 阶段 19：3D 待做
- 阶段 20：2D PPU 补全（旋转/缩放/捕获）待做
- 阶段 21：真实 .nds 兼容性验收（最终目标）待做

> 目标对齐说明：原「阶段 13–16」只覆盖卡带协议/触摸/音频/3D，**缺了三个「顺畅玩商业 .nds」的硬前提**——
> Thumb 指令集（游戏主体代码）、商业 ROM 安全区解密、存档。已据此扩充为阶段 13–21，并补 2D 补全与最终验收。

---

## 建议的立即下一步（确认后才编码）

只做 **一个** 微步，完成后停住请你验证；通过后再做下一步。  
当前进度：预习 P1–P4 完成；阶段 0–17 全部完成（见总览待办）；阶段 18–21 实行**全程自动推进模式**（阶段内自测通过即进入下一阶段，阶段末统一汇报）。  
默认顺序：**18.1 短文（NDS 16 通道音频 PCM/ADPCM）**（阶段 17 已全部完成）。
