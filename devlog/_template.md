# <模块名>log

> 本文件放在模块自己的目录下，命名为 `<模块名>log.md`（如 `src/cpu/cpulog.md`），只写本模块的详细记录。
> 根目录 `DEVLOG.md` 只做索引，新增条目时记得同步加一行。
> 约定：按时间从旧到新记录；每条固定三段：做了什么 / 怎么验证 / 结果。

## YYYY-MM-DD · 阶段 X.Y <一句话标题>

- **做了什么**：…
- **怎么验证**：…
- **结果**：✅ 通过（或 ❌ 未过，原因…）
