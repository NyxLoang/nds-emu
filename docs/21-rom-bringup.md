# 阶段 21：真实 .nds 兼容性 bring-up（Phase A 总结 + Phase B 路线）

> 阶段 21 的目标不是再实现一整块新硬件，而是让前面 0–20 阶段攒出来的模拟器
> **真正跑起来一个商业 ROM**（本项目验收对象：`tools/` 下的 FFXII）。这个阶段没有
> 一条“从头写到尾”的实现主线，而是一轮一轮地：跑真 ROM → 找到第一个卡点 →
> 修掉 → 再跑 → 再找下一个。这种调试方式叫 **bring-up**（逐段点亮）。
> 本文先总结已完成的 Phase A（bring-up 设施 + 首份 gap 清单），再说明 Phase B 的迭代路线。

---

## 1. 为什么真实 ROM 值得单独开一个阶段

之前的每个阶段都用「手写机器码 / 自制 mini.nds」验证：程序已知、数据已知，预期也已知。
真实商业 ROM 完全不同：

- 代码体积大（FFXII 的 ARM9 镜像 0x78038 字节 ≈ 480KB，ARM7 约 162KB）；
- 启动过程依赖很多**之前从未一起工作的子系统**（双核、CP15、中断、卡带、显示、内存镜像）；
- 某个环节差一点，后面的代码就会在错误数据上继续跑，现象往往不是“报错”，而是
  PC 慢慢滑进未映射内存、画面全黑、或者卡在一个看起来很像死循环的地方。

所以阶段 21 需要先有**诊断设施**：不开窗口也能跑 N 步、能逐条看指令、能把“未知寄存器/
未知 SWI/未实现指令”只打一次而不是刷屏。Phase A 就是建这套设施并跑出第一份卡点清单。

---

## 2. Phase A 做了什么

### 21-A1：修 ARM7 镜像装载

早期装载 ARM7 镜像时按“固定 64KB ARM7 WRAM（0x03800000）”判断，但 FFXII 的 ARM7
入口和装载目标其实是 **Main RAM 0x02380000**（165552 字节）。

修复：装载时按 ROM 头的 `ram` 字段判断目标区——落在 Main RAM（4MB）就写 Main RAM，
落在 ARM7 WRAM 就写 WRAM，不再假设固定 64KB。

### 21-A2：bring-up 诊断设施

新增：

- `bus.diag` 开关：未知 SWI / 未实现指令 / 未知 IO 访问**只打印一次**（位图去重），
  避免游戏循环轮询同一寄存器时刷屏；
- `--headless N`：不开窗口、不开音频，跑 N 步后打印两核 PC / cycles 并退出；
- `--trace`：逐条打印指令，用于追启动前几十步或定位跳转点；
- `src/runner/` 模块：headless 驱动（ARM9:ARM7 = 2:1 与主循环一致）。

### 21-A3：修 ARM r15 读值缺 +8

ARM 架构里读 `r15` 当普通寄存器用时应得到「当前指令地址 + 8」（流水线），
PC 相对的字面量池（`LDR rX, [PC, #imm]`）依赖这个值。原来直接返回 `r[15]`，
导致真 ROM 启动时 PC 相对读数差 8 字节。修复后统一走 `read_reg`：

```c
r15 读值 = (当前 PC & ~3) + 8
```

### Phase A 产物：首份 gap 清单

| # | gap | 状态 |
|---|-----|------|
| G1 | ARM r15 读缺 +8，PC 相对字面量池读错 | ✅ 已修 |
| G2 | Shared WRAM（0x03000000-0x03007FFF）及镜像（0x037F8000 一带）未映射 | ✅ 已修（21-B1，2026-09-05） |
| G3 | 双核启动握手时序（ARM7 挂起/释放 + IPC FIFO 在真实 ROM 上验证） | ✅ 已过旧卡点（21-B2..B8：IPCSYNC/镜像/BX/Thumb/WRAMCNT + ARM9 DTCM/ITCM + 信箱解码；2026-09-05 起 ARM9 进 boot 初始化，下个 gap 见 G4/B9） |
| G4 | 修完 G2/G3 后继续暴露的更多 gap（SWI/PPU/中断/内存等） | ⏳ Phase B（B9 起：ARM9 BIOS/IRQ 高向量 + ARM7 SWI 0x0E CRC16 + 一批未知 IO） |

---

## 3. 跑真 ROM 时看到的现象（读代码时可对照）

这是 Phase A 版本的现象记录，后面每个微步都在改写：

```sh
build/nds-emu.exe "tools/Z 最终幻想12…(1024Mb).nds" --headless 20000000
```

会看到类似：

```
headless: step=1048576 ARM9 PC=0022F004 cyc=699051 | ARM7 PC=037FFEAC cyc=349525
...
headless: done. ARM9 PC=132612F8 cyc=13333334 | ARM7 PC=037FFC44 cyc=6666666
```

两个特征值得记住：

1. **ARM7 的 PC 落在 0x037F8xxx**：这是 Shared WRAM 的镜像区，当时 bus 没映射，
   读返回 0。0x00000000 恰好是一条合法但无意义的指令（`ANDEQ r0,r0,r0`），
   所以 CPU 不报错，只是一直“取 0 → 前进 4”，PC 从低地址往高地址漂。
2. **ARM9 的 PC 也在按 4 字节递增漂移**：说明某条跳转把执行流送进了没有代码/没有
   映射的地址区间，模拟器既没有崩也没有报未实现指令。

诊断口诀：真 ROM “跑飞”时先看两核 PC 落在哪个区间，再往回找最后一次合理的分支/BL。

### 21-B2 之后（2026-09-05）：trace 定位两个具体跑飞点

修完 IPCSYNC 后 headless 不再报 `0x04000180/81` 未知 IO，但两核终点不变。逐条 trace
定位到两个明确原因：

1. **ARM9 在约 cycle 126,634 弹 PC=0**：调用 `0x0201A9B8` 时函数用
   `STMFD/LDMFD sp!, {r4,pc}` 保存/恢复返回地址，而 SP≈0x027E3Fxx。该地址落在
   Main RAM 的**无缓存镜像区 0x02400000-0x027FFFFF**（真机存在、当前 bus 未映射），
   栈写入丢失 → 弹回 0 → 在零区逐 4 漂移。
2. **ARM7 在约 cycle 231,180 起按 ARM 码跑飞**：`BX r12 -> 0x038043C9`（LSB=1，
   真机应切 Thumb 并把 PC 对齐到 0x038043C8）没有更新 T 位，CPU 把 0x038044xx 的
   Thumb 数据当 ARM 指令译码，最终 `B 0x049E07B5` 离开可执行区。

这两个点成为 B3/B4 的直接依据；每修一个就重跑一次，直到 hard_title。

### 21-B3 之后（2026-09-05）：ARM9 卡点解除

把 0x02400000-0x027FFFFF 映射为 Main RAM 别名后，ARM9 的栈读写不再丢失。trace 中
cycle 126,634 的返回点变为：

```c
cpu: PC=020008F8 insn=E8BD8010 LDM r13!, list=8010 n=2 cycles=126634
```

`headless 20000000` 全程 ARM9 PC 停在主存代码区 0x0200B9xx 循环（修复前是在 0 区
逐 4 漂移、终点 0x132612F8）。ARM7 仍按原轨迹跑飞（BX 奇地址未切 Thumb，B4），
ARM9 的循环疑似在等 ARM7 握手结果——B4 修完 ARM7 后重跑即可验证。

### 21-B4 之后（2026-09-05）：ARM7 进入 Thumb，但段内二次跑飞

ARM BX 切 Thumb 后，ARM7 能正确落到 0x038043C8（旧路径里它把这里当 ARM 数据译码、
跳到 0x049E07B5 的跑飞消失）。但 headless 随即出现约 65k 行
`io: read unknown addr=0400xxxx (arm7=1)` 顺序刷屏——ARM7 从 IO 区 0x04000182 附近
开始逐字节读取/取指漂移，20M 步终点为 0x0178CBD8（仍是跑飞，只是换了一条路径）。
普通 Thumb 指令当前不打印 trace，无法直接看到跳进 IO 区前的最后几条指令，
因此 B5 第一步补 Thumb 逐条 trace，再定位根因。

### 21-B5 之后（2026-09-05）：BX lr 解码错误修复，卡点前移

Thumb 逐条 trace 直接抓到二次跑飞源头：0x038043C8 只有两条 Thumb 指令
`SWI 3` + `BX lr`（0x4770）。旧解码把 BX/BLX 的 Rm 从 bit2:0+bit6 拼（0x4770→r8），
而规范编码是 bit6:3（0x4770→lr），于是“返回”跳进了 r8 里的 0x04000180，随后逐字
刷读 IO 区。修复 Rm 解码后 IO 刷屏归零，ARM7 正确反复进出该桩并轮询 0x027FFFF0。
卡点前移到 ARM9：约 cycle 463,554，ARM9 在 0x0200B9B0
`LDMFD sp!, {r4,r5,lr}` 后 `BX r14 -> 0xE1C010B0`——栈里保存的返回地址被污染，
B6 将定位污染来源。

### 21-B6 之后（2026-09-05）：栈污染来自 ARM7 误写 Main RAM 镜像

给 LDM trace 增加 SP/弹出 PC 后定位 LR 槽 0x027E3B34；总线写监视抓到写入者是
**ARM7 的块拷贝循环（0x02380124-130）**，它在约 cycle 141,868 把 0xE1C010B0 写进
该槽。根因：0x02400000-0x027FFFFF 是 **ARM9 专用的无缓存镜像**（ARM7 内存图没有
这段），B3 无差别映射让 ARM7 的拷贝真实落盘、覆盖了 ARM9 的栈。改为仅 ARM9 可见后，
ARM9 全程停在主存代码区不再崩栈；ARM7 停在 0x037FC0xx 轮询循环。
新出现的 3 条 ARM9 未知 IO（读 0x04000204/205、写 0x04000247=03）与 ARM7 轮询
循环一起成为 B7 素材。

### 21-B7 之后（2026-09-05）：EXMEMCNT/WRAMCNT 语义补齐，握手卡点前移 B8

给 bus 补齐真实 Shared WRAM 所有权之后，用 `--headless 3000000` 重跑 FFXII：

```text
headless: step=1048576 ARM9 PC=0200B844 cyc=699051 | ARM7 PC=037FC0B0 cyc=349525
headless: done. ARM9 PC=0200B840 cyc=2000000 | ARM7 PC=037FC0B0 cyc=1000000
```

1. `io: read unknown 04000204/205` 与 `io: write unknown 04000247` 三条全部消失；
   寄存器初值/读写位与 melonDS 直接启动口径一致（WRAMCNT=3、EXMEMCNT=0xE880）。
2. trace 显示 IPCSYNC 上的 8→0 计数握手已经能跑完（ARM9 走完 0x0200B94C 附近
   的轮询并正常返回），随后 ARM9 进入 0x0200B834 的事件位轮询：按 `r1` 选
   `0x027FFC00 + r1*4`、再读 `+0x38C` 处字并测试 bit12；ARM7 停在 0x037FC0A0/B0
   轮询 `0x027FFFF0`。ARM9 在等的那一位（0x027FFF8C bit12）始终未被 ARM7 置上。

结论：B7 已把未知 IO 清零，双核当前卡在「ARM7 命令口 0x027FFFF0 → ARM9 事件
状态区 0x027FFF8C」这条数据约定上，排入 B8 继续解码。

### 21-B8 之后（2026-09-05）：根因是 ARM9 DTCM/ITCM，不是“镜像仅 ARM9 可见”

解码 ARM7/ARM9 两侧代码后确认：B7 看到的两个轮询（ARM7 等 `0x027FFFBC==0x7F`、
ARM9 等 `0x027FFF88/8C` 事件位）都是**自研信箱协议的正常握手**，不是模拟器缺
寄存器。真正拦住握手的是内存归属：

1. **0x027E0000-0x027E3FFF 是 ARM9 的 DTCM**。FFXII 复位例程 `0x02000A6C` 用
   `MCR p15,0,rX,c9,c1,0`（值 0x027E000A）配置 16KB DTCM 到 0x027E0000，再把 SVC/
   IRQ/User 栈建在这里。ARM7 同时把自己的镜像搬到主存 0x027E0000 起 0x182A0 字节；
   ARM7 看不到 DTCM，写的是同一地址下的 Main RAM（真机按 0x02000000-0x02FFFFFF
   窗口解码，melonDS 的 ARM7 也命中两个 8MB 窗口）。B6 把镜像“仅 ARM9 可见”其实是
   掩盖了“DTCM 未实现”，ARM9 栈被写坏是因为它被错误地放在 Main RAM 里。
2. **0x01FF8000 起 32KB 是 ARM9 的 ITCM**。复位代码 `0x020009F0` 用尾部描述表
   `0x02078020`（目标 0x01FF8000、大小 0x6BC0；目标 0x027E0000、大小 0x1040）把
   ARM9 镜像 0x02070420 起的系统例程拷进 ITCM/DTCM，随后清掉源区。ITCM 未映射时
   ARM9 第一次 `BL 0x01FF81B4` 就取到 0，从 ITCM 零区一路漂进主存。
3. 0x027FFxxx 的主存高 4KB 就是共享信箱区：ARM7 拷贝后按
   `0x027FFFF0→0x027FFFB8/BC→0x027FFFBE` 的锁存/握手顺序运行，ARM9 事件位在
   `0x027FFC00+0x388` 起（事件 n 的 bitn），不再卡死。

**做了什么**：

- `bus` 新增 ARM9 DTCM（16KB，CP15 可配基址）与 ARM9 ITCM（32KB，固定
  0x01FF8000）；ARM9 优先命中 DTCM，ARM7 仍访问同地址的 Main RAM 镜像。
- `exec_coprocessor` 按子寄存器保存 CP15 c9,c1,0/1，c1 bit16 使能后把 DTCM 映射
  同步给 bus；ARM9 MRC c9 读回同一配置。
- Main RAM 镜像恢复双核可访问（melonDS 口径），撤掉 B6 的“仅 ARM9”限制并修正
  bus.h/buslog 里的错误结论；`direct-boot tables` 按 melonDS SetupDirectBoot 写
  0x027FFxxx 卡带信息表。
- 测试新增 `[case 21-B8]`：DTCM 与镜像互不覆盖、ARM7 在 DTCM 地址下写主存、ITCM
  仅 ARM9 可访问。全量 **594 项检查 0 失败**。

重跑 FFXII `--headless 2000000`：双核不再停在旧信箱轮询；ARM9 跑到 boot 初始化
0x02009EC0（打开中断后被 IRQ 打断），ARM7 首次调 `SWI 0x0E`（GetCRC16）没有
HLE 实现。下一卡点是 **ARM9 高向量 0xFFFF0000 的 BIOS/IRQ 分发 + ARM7 SWI 0x0E**，
排入 B9。

### 21-B9a 之后（2026-09-05）：GetCRC16 落地，ARM7 越过 SWI 0x0E

**做了什么**：

- 新增 `src/bios/bios_crc16.h/.c` 并登记 SWI 0x0E：`r0=CRC 初值`、`r1=数据地址`、
  `r2=字节数`，返回 `r0=CRC16`。算法为标准 CRC-16/IBM：初值由调用方给出（FFXII
  用 0xFFFF），反射多项式 0xA001 逐位右移，末尾不反转，与 GBATEK 伪代码/libnds
  `swiCRC16` 的用法一致。
- 测试新增 `[case 21-B9a]`：ASCII `"123456789"` + 初值 0xFFFF → 0x4B37；覆盖
  ARM、Thumb、ARM7 三条路径与长度 0 返回初值。全量 **600 项检查 0 失败**。

重跑 FFXII `--headless 2000000`：

```text
headless: step=1048576 ARM9 PC=02009EC0 cyc=699051 | ARM7 PC=037FC89C cyc=349525
headless: done. ARM9 PC=0025B6C0 cyc=1333334 | ARM7 PC=037FC898 cyc=666666
```

1. `bios: unknown SWI 0x0E` 消失，ARM7 首次 CRC 调用点通过并进入 0x037FC8xx
   的后续 boot 代码（旧终点 0x000366A4 不再出现）。
2. ARM9 仍停在 0x02009EC0 附近被 IRQ 打断后进 0xFFFF0000 高向量区读 0 跑飞
   （终点 0x0025B6C0），需要查高向量跳板/中断时机。
3. 日志露出大量 ARM9 未知 IO：0x04000304/305、0x04000240-249、
   0x04000004/05、0x0400004C-5F、0x04001004-6F，另有 ARM7 读 0x04000300/301；
   这些是下一 gap 的素材（PowerControl/PostFlag/中断控制/显示控制等）。

结论：B9a 后下一卡点排为 **ARM9 BIOS/IRQ 高向量 + 这批未知 IO 桩**，按 gap 继续。

### 21-B9b（2026-09-05）：首中断现场快照——确认 FFXII 用“DTCM 槽”装 IRQ handler

**做了什么**：

- `cpu.c`：IRQ 真正触发时（`--headless` 诊断模式）只打印一次首中断现场：核名、
  被打断 PC、CPSR、IME/IE/IF，以及 IRQ 槽末 8 字节（ARM9：DTCM+0x3FF8/FC，
  基址取 bus 的 CP15 配置；ARM7：0x03FFFFF8/FC）。用于判断游戏把 IRQ handler
  装到哪个约定槽，避免每步刷屏。
- `exec.c`：异常 trace 加 `arm9`/`arm7` 前缀，中断后执行流归属一眼可辨。

重跑 FFXII `--headless 1100000` 的关键输出：

```text
irq: first arm9 IRQ pc=02009EC0 cpsr=8000001F ime=04000001 ie=00042009 ifl=00000008
     slot+3FF8=00000000 slot+3FFC=01FF8000
headless: done. ARM9 PC=000117C0 ...
```

结论：

1. 游戏遵守 BIOS 槽协议：ARM9 的 DTCM 末 4 字节（0x027E3FFC）放着用户 IRQ
   handler 指针 **0x01FF8000**（ITCM 开头，复位时拷入的系统例程区）；
   0x3FF8 的等待标志为 0（本次不是等待被唤醒）。
2. ARM9 使能了 VBlank（bit3）与 FIFO 相关中断（bit17/18），首中断是 VBlank。
3. 模拟器没有 BIOS ROM，无法执行 0xFFFF0018 的跳板，于是从高向量读 0 一路漂到
   0x000117C0。B9c 将让 IRQ 入口模拟该跳板：保存异常现场后直接跳槽里的 handler。

### 21-B9c（2026-09-05）：ARM9 IRQ 槽跳板——FFXII 中断分发器被点亮

**做了什么**：

- `cpu.c`：IRQ 异常现场建好后，若 ARM9 DTCM 已使能且 `DTCM+0x3FFC` 槽非 0，
  直接改 PC 跳槽里的用户 handler（LSB=1 时按 Thumb 入口置 T）。槽为 0 或 DTCM
  未配置时保留旧的高向量路径便于诊断。ARM7 的 0x03FFFFFC 约定待出现真实 IRQ
  证据后再对称实现。
- 测试新增 `[case 21-B9c]`：槽指向 ITCM，handler 为 `SUBS pc,lr,#4`，验证
  触发后 PC/mode/I/SPSR/LR 与返回恢复，共 8 项检查（全量 **608 项 0 失败**）。

重跑 FFXII：ARM9 终点从 0x000117C0（高向量读 0 漂移）变为 0x01FF8028——
**成功进入 ITCM 里的 FFXII 中断分发器**。对 ITCM 0x01FF8000 起做反汇编：

```text
stmdb sp!, {lr}          ; 保存返回
mov ip, #0x04000000
add ip, ip, #0x210       ; ip = 中断寄存器区
ldr r1, [ip, #-8]        ; IME
cmp r1, #0 / ldmeq ...   ; IME=0 直接返回
ldm ip, {r1, r2}         ; r1=IE, r2=IF
ands r1, r1, r2          ; pending = IE & IF
ldmeq sp!, {pc}          ; 无 pending 返回
mov r3, #0x80000000
clz r0, r1               ; ← 模拟器未实现 CLZ，误译导致死循环
bics r1, r1, r3, lsr r0  ; 清掉当前最高位
bne 循环                 ; 逐个分发所有挂起中断
...
```

结论：跳板已通，下一卡点（B9d）是 **CLZ 指令缺失**——不是 IO/时序问题。

### 21-B9d（2026-09-05）：CLZ 前导零计数——中断分发循环跑完

**做了什么**：

- `exec.c` 实现 ARMv5 `CLZ Rd, Rm`：数 Rm 的前导零（0→32）。FFXII 的 IRQ
  分发器流程是 `CLZ r0,r1` 找最高挂起位 → `BICS r1,r1,r3,lsr r0` 逐位清除 →
  分发对应处理函数；此前模拟器把 CLZ 误译成普通数据运算，r1 永远清不空，
  循环永不退出。
- 测试新增 `[case 21-B9d]`：0/最高位/最低位/FFXII IE 值/VBlank 位共 5 组
  CLZ 结果 + PC 前进断言（全量 **618 项 0 失败**）。

重跑 FFXII：IRQ 分发器处理完 VBlank 并返回主程序，ARM9 终点从
0x01FF8028 前进到 **0x02006488**；反汇编该处是 `blx r1`（ARM 态寄存器间接
调用），模拟器未实现 ARM `BLX Rm`，排入 B9e。

### 21-B9e（2026-09-05）：ARM BLX Rm——ARM9 回到主等待循环

**做了什么**：

- `exec.c` 补 `BLX Rm`（0x012FFF3x）：`r14 = PC+4` 后按目标 LSB 切换
  Thumb/ARM 并跳转（与 BX 相同的 T/PC 规则，额外保存返回地址）。
- 测试新增 `[case 21-B9e]`：ARM→Thumb 与 ARM→ARM 两条路径，各验证
  LR/T/PC 与目标函数首条指令执行（全量 **628 项 0 失败**）。

重跑 FFXII `--headless 8000000`：ARM9 越过 0x02006488，回到 0x02009EC0
主等待循环（PC 在 0x02009EC4-ECC 间游走，等下一次 VBlank），8M 步内不再有
固定单点卡死。下一步观察多次 VBlank 后启动流程是否继续前进。

### 21-B9f（2026-09-05）：IRQ HLE 桩——被中断现场保存/恢复

**做了什么**：

- 跳用户 IRQ handler 前，把 r0-r3/r12/CPSR/返回点（被打断 PC）存入模拟器
  私有槽；FFXII 分发器弹栈返回后，在取指前恢复现场、重执行被打断指令。
- 修复异常 trace 参数顺序 bug；headless 打印前若干次 IRQ 触发/恢复点。
- B9c 测试改为 FFXII 分发器风格（push lr → 破坏 r1 → pop pc），断言 r1/CPSR
  恢复、被打断指令重执行（全量 **633 项 0 失败**）。

真机：第一次 VBlank 完整处理链（含处理函数内嵌套的大块 memset）已执行完；
但第二次 IRQ 前 ARM9 落入未映射区 0x0027xxxx。反汇编启动序列确认 FFXII 分别
初始化了 SVC/IRQ/System 栈（0x027E3FC0 / 0x027E3F7C / 0x027E3AF8），
而模拟器所有模式共用 SP——IRQ handler 在 System 栈上压栈嵌套会覆盖用户现场。
**排 B9g：实现按模式独立的 r13/r14（banked registers）。**

### 21-B9g（2026-09-05）：模式私有 r13/r14——ARM9 停止跑飞

**做了什么**：

- CPU 增加 User/System 主 r13/r14 与 FIQ/IRQ/SVC/ABT/UND 私有 r13/r14 槽；
  新增 `exec_apply_cpsr` 统一处理“切模式时保存旧栈/加载新栈”。MSR、异常
  入口、异常返回、IRQ HLE 恢复全部走该入口。
- 启动代码中 FFXII 分别配置的 SVC/IRQ/System 三个栈真正隔离；B9c 用例先配置
  IRQ 专用 SP 再中断。新增 B9g 用例验证三模式往返栈值互不串扰
  （全量 **643 项 0 失败**）。

重跑 FFXII：ARM9 不再落未映射区，第一次 IRQ 后继续执行至 **0x02007CE0**
链表遍历（ARM7 在 0x037FC8A0 轮询配合）。日志新露出 ARM9 读
0x04000280-2BF 未知 IO——排 B9h 查这批寄存器（IPC/中断扩展区）。

### 21-B9h（2026-09-05）：LDM ^ 的 User 槽语义——任务队列不再成环

**做了什么**：
- 反汇编 + 运行时写监视确认 0x02007CCC 是 FFXII 协作调度器的“就绪队列删任务”函数；
  队列曾出现 A=0x02076FE4 ↔ B=0x02076F24 两节点互指成环，删除函数死转。
- 根因不是链表代码，而是 **LDM ^ 的 User 槽语义缺失**：上下文恢复
  `ldm r0,{r0-r14}^`（SVC 模式、列表不含 PC）应把 r13/r14 装入 User/System 槽，
  旧实现装进 SVC 私有槽，导致“切到 A 却还在跑旧现场”。
- 修复后队列恢复 A→B→NULL；第二次及以后的 VBlank IRQ 能正常进入/恢复。
- 顺带按硬件修正中断位：VBlank=bit0、Timer0-Timer3 溢出=bit3-bit6。

**验证**：新增 21-B9h 单测 6 项；全量 **649 项检查 0 失败**；headless 4M/8M：
ARM9 到达 0x0200EA84 忙等（0x02078F0A bit1 等清 0），Timer0 溢出已能触发
handler，下一步 B9i 追“忙位谁清”。

### 21-B9i（2026-09-05）：FIFO 命令送达 ARM7 + ARM7 IRQ 槽跳板

**做了什么**：
- 0x0200B780 反汇编确认是 **IPC FIFO 发送**（读写 0x04000184/188）；ARM9 发命令后
  置忙位并等清 0。此前 `cnt9/cnt7` 恒 0，命令被静默丢弃——根因是 CNT 高字节
  0xC4（错误应答+使能+收 IRQ）合并写被旧代码当成“纯错误应答”。
- ARM7 IRQ 首次真实触发后确认槽在 **0x0380FFFC**（0x03FFFFFC 是空的），指向
  0x037FB8F4 的中断分发代码；按 ARM9 相同 HLE 方案实现 ARM7 槽跳板。

**验证**：新增 B9i 单测 10 项；全量 **659 项检查 0 失败**；headless：ARM9 FIFO
命令入队并触发 ARM7 IRQ，ARM7 handler 能完整返回。ARM9 忙位仍未清，B9j 查
ARM7 回执路径。

### 21-B9j（前半，2026-09-05）：定时器重载语义 + ARM7 VBlank 信号

**做了什么**：
- service6 的真实运行时入口定位为 0x03804D7C；第二条命令会成功置 state=1，但
  完成回执需后续事件推进状态机。
- 修两处硬件语义：① TM3/任意定时器 CNT_L 应作为 reload，CNT_H 使能时从 reload
  起跳、溢出回 reload；② VBlank 同时置 ARM9/ARM7 两套 IF（此前 ARM7 IE bit0
  永远等不到帧事件）。

**验证**：B9j 单测 4 项 + 6.3 补 1 项；全量 **664 项检查 0 失败**。ARM7 现在能
收到 VBlank IRQ，但 service6 仍不发完成回执，B9j 后半继续。

### 21-B9j（后半，2026-09-06）：SPI device1 固件最小实现——ARM9 首次离开忙等

**做了什么**：
- service6 的真实工作内容锁定为 **SPI 访问**：事件处理器 0x03804A54 操作
  0x040001C0/1C2，SPICNT=0x8900 的 device 位是 1（固件 Flash）。
- 旧实现只认触摸 device 2，固件读命令全被忽略；补“空固件回 0xFF”最小 HLE。

**验证**：新增 SPI device1 单测；全量 **665 项检查 0 失败**。headless 真 ROM：
ARM9 PC 从 0x0200EA90 推进到 0x0200B838/0xF1BC 服务循环，ARM7 进入 0x027F6xxx
继续处理；后续需补固件真实内容或完整 SPI 状态机。

**后续修复（IRQ 入口帧）**：ARM7 长跑后会把调度器上下文 PC 存成 0，根因是
0x37FBA10 从 IRQ 栈上方读旧任务寄存器帧，而槽跳板没有预填该帧。已在 ARM7 IRQ
入口把 r0-r3/r12/lr(=PC+4) 写到 IRQ SP 上方，3200 万步内 ARM7 不再跑飞。

### 21-B9j+（2026-09-06）：SPI device1 完整状态机 + 合法固件用户区

**背景**：B9j 后半的最小 HLE 让 ARM9 第一次离开忙等，但 2M 步诊断显示 ARM9 仍
卡在 0x0200EA88，ARM7 在 0x038043E2 被反复打断。抓 SPI 字节流后发现问题不是
数据而是**解析错位**：

```text
8900:03 8900:03 8900:FE 8900:00 ...   ; READ 0x3FE00（地址首字节就是 0x03）
```

旧状态机把地址里的 0x03 再次当成 READ 命令，地址变成 0xFE0000 起，镜像全错。

**做了什么**：
- device1 状态机改为按片选（HOLD）分事务：命令只在事务开头解析，READ 收满
  3 字节大端地址后逐字节回读；`touch.c` 合成最小固件内容：主机型号/MAC/偏移
  0x7FC0/两个 CRC 合法的用户设置镜像。
- 验证 675 项 0 失败；FFXII ARM9 推进到 0x02011918 卡带读块循环，新卡点不是
  固件而是 ROMCTRL 忙位。

### 21-B9k（2026-09-06）：ARM9 硬件除法/开方 + ROMCTRL 块忙位

**背景**：0x04000280-2BF 是 NDS9 硬件 DIV/SQRT（此前被当成“未知 IO”），
IRQ/调度器要保存这些寄存器；而 0x02011918 循环读 ROMCTRL 的 bit23/bit31，
旧实现只置 DRQ 不落 bit31，块读完循环不退出。

**做了什么**：
- 新建 `src/io/math.h/.c`：DIVCNT/SQRTCNT 三模式除法（含除零/溢出钳制）与
  32/64 位开方，瞬时模型。
- `cartbus.c`：命令激活置 bit31=1，块内最后一字读完后 DRQ 与 bit31 一起清 0。
- 验证 700 项 0 失败；FFXII 越过卡带读块循环，继续走 ARM7 SoundBias/启动服务。

### 21-B9l（2026-09-06）：ARM7 SWI 0x08 SoundBias

ARM7 在 0x038043FA 调 SWI 0x08（NDS7 BIOS SoundBias）。此前 unknown SWI 把
ARM7 弹到 0x00000008 向量，之后在未映射低地址按 Thumb 漂移。新增
`src/bios/bios_snd.c` 瞬时写 SOUNDBIAS（0x000/0x200），仅 ARM7 登记。
验证 704 项 0 失败；ARM7 回到 Halt 服务入口。

### 21-B9m（2026-09-06）：ARM9 CP15 WFI

NDS9 无 HALTCNT，FFXII OS 空闲任务用 `MCR p15,0,r0,c7,c0,4` 代替 Halt。
旧实现当普通 CP15 写忽略，空闲任务满速空转；按 GBATEK 口径实现 WFI：
IME 门控下等 (IE&IF)，挂起后唤醒并进 IRQ。ARM9 的 CPU 周期不再无界增长。

### 21-B9n（2026-09-06）：POWCNT/POSTFLG 默认值——越过“提前空闲”死锁

**背景**：即使 WFI 正确，ARM9/ARM7 也会双双空闲，屏幕全白、DISPCNT=0。对比
melonDS 直接启动初值发现缺整组电源寄存器：POWCNT1 读 0 让游戏以为 LCD/3D
未上电，提前结束启动任务。

**做了什么**：
- 新建 `src/io/power.h/.c`：POSTFLG 双核=1、POWCNT1=0x820F、POWCNT2=1、
  WIFIWAITCNT=0x30；写掩码/粘住位按真机。
- headless 增加 `--screenshot`：跑完用纯渲染器输出双屏 24 位 BMP，便于不开
  窗口核对画面。
- 验证 716 项 0 失败；FFXII 离开 0x0200957C 空闲死锁，继续 0x027FFF8C 信箱
  握手与启动服务（截图仍是白屏，说明画面初始化还在其后）。

### 21-B9o（2026-09-06）：双核定时器实例分离

排查 ARM7 0x03807584 的“请求完成单元=1”等待时发现：真机 NDS9/NDS7 各有
4 个定时器（共 8 个），同址但实例独立；旧模拟器只有一套，两核配置会互相覆盖。
`io->timer` 改为 `timer[2][4]`、推进函数带核参数，716 项测试保持 0 失败。
该卡点仍在 ARM7 请求队列，说明不是定时器实例冲突。

### 21-B9p（2026-09-06）：DMA 完成中断接线

排查 ARM7 请求等待时补上真机 DMA 完成中断（IF bit8-11）：`dma_transfer`
结束后若 CNT bit14 置位，按当前核置 IF。15.3 增加 DMA0 IRQ 断言，718 项
检查 0 失败；headless 复跑 ARM7 的 0x03807584 请求等待仍在，下一步需追
ARM7 请求链表（0x03808248）为何为空。

### 21-B9q（2026-09-06）：DMA 控制器双核分离

与定时器一样把 DMA 拆成 ARM9/ARM7 两套 4 通道（真机 8 条），卡带 DRQ/VBlank
按核触发。718 项测试保持 0 失败；ARM7 请求等待仍未解除，已排除 DMA 实例
冲突这一层。

### 21-B9r（2026-09-06 分析，无代码改动）：IPC 握手时序定位

用 trace + 运行时反汇编把最终卡点收敛到 FFXII 自己的双核 IPC 协议：

- ARM9 引导初始化（0x0200B8C8，经 0x0200B650 跳板从 0x0200836C 调用）先配
  FIFO CNT=0xC408、清 0x027FFC00 事件表，随后在 0x0200B94C 反复读写 IPCSYNC，
  等 ARM7 把自己的输出 nibble 从 0 变成 8；
- ARM7 引导拷贝完成后从 0x037F8468 进入主循环，0x037FCECC → 0x0380760C →
  0x03807524，把 IE 设成 0x40000（FIFO 接收）后等请求完成单元（0x0380BA94）
  变成 1；
- 0x037FEB24 才是 ARM7 真正处理请求、经 0x037FEA B8 置 0x027FFF8C 事件位、
  并经 0x037FEC00 回 IPCSYNC 的代码，但它由 FIFO 消息驱动；
- 因此现在两边互相等待：ARM9 在等“ARM7 就绪/IPC 回应”，ARM7 在等 ARM9 的
  FIFO 数据；下一步应确认 0x0200B8C8 之前是否应发出第一条 FIFO（或在直接启动
  语义下 ARM7 应先置 IPCSYNC nibble=8），并对照 melonDS 的 IPC 初始化顺序。

**临时注入实验（已还原）**：在 ARM7 首次进入 0x03807578 FIFO 等待时，从 ARM9
侧补发历史消息 0x80004106/0x40402806，ARM7 会回 0x80004126/0x40402826，
ARM9 随后开始写 2D 仿射背景寄存器并做显示初始化（0x02002A04 等）——说明
“ARM9 该发而未发 FIFO”确实是当前死锁根因，而不是渲染/显示寄存器缺失；
但仅靠注入会让该握手无限循环，必须找到 ARM9 真正的发送时序（0x02006A94/
0x0200EE4C 路径）才能收尾。

### 21-B9s（2026-09-06）：ARM BLX 立即数缺失 + ARM7 Halt 唤醒语义

**背景**：B9r 的判断方向（ARM9 该发 FIFO）是对的，但“为什么 ARM9 没发”的根因
不在 IPC 寄存器，而在更早的一条指令：ARM9 0x02012500 是
`BLX 0x020007A8`（ARMv5 立即数形式，进安全区里的 Thumb SWI 桩），旧译码把它
当成普通 `B`。于是 CPU 没切 Thumb、没写 LR，直接落入安全区占位字节，
ARM9 后续执行流偏离，永远到不了 service13 的发送函数 0x2012614。

对照 melonDS（本地参考核心 + 探针）拿到三条关键证据：

1. ARM9 首次真实 FIFO 发送值是 **0x03CC504D**：低 5 位=13（服务号），
   参数 = 0x03CC504D>>6 = **0xF3141**；ARM7 收到后在 0x03807510
   写 BA84=0xF3141、BA94=1，这正是 ARM7 主服务等待的“请求完成单元”。
2. 上一条指令 0x02012500 在参考里译成 `BLX 0x020007A8`，目标安全区内容是
   Thumb `svc #0x0B; bx lr`（CPU 拷贝桩），不是 ARM 代码。
3. ARM7 处理 FIFO 请求时若在 Halt（SWI 0x06，Thumb `DF06`）里被中断唤醒，
   真机应继续 SWI 之后的 `bx lr`；模拟器统一“重执行被打断指令”会让它再次
   睡死，处理完的回执/队列逻辑永不落地。

**做了什么**：

- `exec.c` 实现 ARM BLX 立即数（cond=1111 分支编码）：LR=PC+4、切 Thumb、
  H 位 +2、PC 清最低位；
- `cpu.c` ARM7 IRQ HLE 恢复：被打断指令为 Thumb `DF06`（Halt）时 PC 前进 2，
  只对 Halt 特殊处理，IntrWait 等条件等待仍重执行；
- `cpu.c` ARM9 CP15 WFI 唤醒同族修复：中断唤醒时 PC 前进 4（WFI 先完成再进
  IRQ），避免返回后仍停在 WFI 上重执行；
- 新增 `[case 21-B9s]`（BLX 立即数 5 项），全量 **723 项检查 0 失败**。

**真 ROM 效果**：BA84/BA94 首次与参考完全一致（0xF3141 / 1）；ARM7 会回发
service6 回执（C0204006 类），ARM9 离开 0x0200EA88 忙等并越过 service6 完成
握手，最终双核停在系统空闲（ARM9 0x0200957C WFI / ARM7 SWI6 Halt），屏幕仍黑、
DISPCNT=0。进一步 trace 确认 ARM9 其实短暂进入过 0x020119xx 卡带读块循环
（约 300 周期）就退出并空闲，而参考里该循环跨 12+ 帧持续运行；本模拟器
疑似卡带读块状态机过早收尾（0x0201193C 的 r8/r0 比对提前失败），且 ARM7
周期性回执 C0240046 未出现。下一卡点排入 B9t：对照参考的 ROMCTRL/CARD_DATA
逐字时序，让卡带读块循环跑满。

### 21-B9t（2026-09-06 分析）：C0240046 缺失的真实根因是 Halt 唤醒后的任务路径

B9s 后对参考核心加了两级日志（状态机入口 + 关键函数模式），并与本地逐段对照，
结论比“卡带时序”更靠前：

1. 参考在 C0204006 之后不是“再入队一个新请求”，而是 ARM7 从 Halt 醒来后进入
   一个周期任务上下文（r10=0x0380A9E8 / A9C0 / AA10 …），该任务调用 0x4588
   为 16 个 service-0x10 对象入队，随后状态机逐个发 C0240046/C0240086/
   C02400C6/C0240006。
2. 本地在 C0204006 后同样回到 Halt/SWI6，但我们的 BIOS HLE 把 SWI6 当成纯
   “等中断”，唤醒后直接重跑/跳过 SWI，永远不进入参考里 BIOS 返回后继续执行的
   周期任务路径；因此后续 service-0x10 入队与回执全部缺失。
3. q82D8/q8248/q82C4 在两次回执发送瞬间均为 0，说明差异不在这些队列标志，
   而在“Halt 唤醒后由谁选择下一个执行上下文”。

**下一步 B9t 实现方向**：给 ARM7 BIOS HLE 的 Halt（SWI 0x06）补“唤醒后继续
周期任务/调度”语义（参考用 ARM7 BIOS 低地址 0x000011xx/0x00001Fxx 的异常返回
与 SWI 分发完成这一步），而不是继续在 FIFO/回执内容上做补丁。

### 21-B9u（2026-09-06）：VCOUNT / DISPSTAT 只读实现

对照任务注册路径时发现 FFXII ARM7 频繁读 0x04000006（VCOUNT）作为任务截止点
输入；模拟器此前把它当未知 IO 读 0，导致任务调度排序退化为“全部截止点相同”。
补最小实现：io 增加 vcount，帧起始回到 0 并由 runner 逐行推进（0..262 循环，
见 21-B9v），0x04000006/07 按只读字节返回；DISPSTAT（0x04000004/0x04001004）bit0 在
VBlank 后置位，作为帧边界状态。验证 729 项 0 失败。

### 21-B9v（2026-09-06）：逐行 VCOUNT + VCount 匹配中断调起任务调度器

把 VCOUNT 从“每帧一次”改为“逐行推进”（runner 每 ~4000 步一条扫描线），并实现
DISPSTAT 写语义：bit3-5 IRQ 使能可写、bit8-15 为比较值；当 vcount 到达比较值时
置 DISPSTAT bit2 并按 bit5 使能产生 IF bit2 中断。FFXII ARM7 把 IF bit2 的
handler 表项设为 0x37FDDF0——正是周期任务调度器；补上该中断后任务队列被真正
调起，C0240046 系列回执按帧重复出现，ARM9 也重新进入 0x020119xx 卡带读循环。
验证 731 项 0 失败。下一卡点转为 ARM9 收下回执后如何继续显示初始化。

### 21-B9w（2026-09-06）：direct-boot 卡带 ID 口径 + 卡带 B8 芯片 ID 命令

**参考对照**：同一阶段导出的参考 Main RAM 快照里，0x027FF800/0x027FFC00 两组
系统表值都是 `00007FC2`，而本地是 `4A465841`（“AXFJ”）。本地把 ROM 头 0x0C
的游戏代码当成了卡带 ID；melonDS `NDSCart::ParseROM` 实际用“补成 2 的幂后的
ROM 大小”推导芯片 ID（0xC2 | ((size>>20)-1)<<8，本 ROM 补到 128MB → 0x7FC2）。

随后追踪 FIFO 差异发现本地 ARM9 在 service11 之前多发了一条 service14
（0x0000004E），参考没有。调用栈落在 0x02011FE8 → 0x020120E0 → 0x02012058，
而 0x02011FE8 开头会把 `0x02011840()` 从 CARD_DATA 读回的值与 0x027FFC00
比较。反汇编 0x02011840 后再对照 melonDS `CartCommon::ROMCommandReceive`：
它发的是 **B8 命令，B8 返回芯片 ID**，不是 B7 的 ROM 读；旧 cartbus 把 B7/B8
都当 ROM 读，于是拿“AXFJ”比较，走了 service14 错误分支。

**做了什么**：
- `main.c` direct-boot 表按补幂 ROM 容量推导芯片 ID，与 melonDS 一致；
- `cartbus` 新增 `chip_id`：B7 仍读 ROM，B8 激活后固定返回一个字的芯片 ID，
  读完清除 DRQ/busy；
- 15.2 用例补 B8 断言（0x400 字节测试 ROM → 0x100C2）。

**效果**：修正后本地 ARM9 不再发 service14，改走与参考一致的 service11
（0x0000002B/0x81E3E82B/0x000000AB…），开始把 Worldmap/Menu 等 bmd 资源读入
Main RAM；全量测试 735 项 0 失败。随后暴露的“空闲任务 PC 被写 0”由 21-B9x
的 IRQ 六字帧修复解决。

### 21-B9x（2026-09-06）：ARM9 IRQ 入口补 BIOS 六字帧

21-B9w 之后 ARM9 在切回空闲任务时仍会跳 0：写监视显示空闲任务上下文
（0x02076F24 一带）被 IRQ 分发器保存现场时写坏。对照 ITCM 机器码与 FreeBIOS
源码确认：真机 IRQ 高向量入口在跳用户 handler 前会先执行
`stmdb sp!,{r0-r3,r12,lr}`，FFXII 的 ITCM 分发器随后从 IRQ 栈弹这 6 个字保存
“被打断任务”的现场。本地 HLE 缺这一步，分发器弹到 IRQ 栈底的全 0，把空闲任务
的恢复 PC/SVC SP 写坏，最终 `subs pc,lr,#4` 弹到 0。

修复：ARM9 跳槽指针前先压 6 字帧（r0-r3/r12 + 被打断 PC+4）并把 IRQ SP 下移
0x18，与真 BIOS/FreeBIOS 行为对齐。效果：ARM9 不再跳低地址空扫，稳定停在
0x0200957C 空闲任务；`[case 21-B9c]` 补帧断言 3 项，全量 **738 项检查 0 失败**。

### 21-B9y（2026-09-06）：IRQ 返回时恢复 ARM9 IRQ SP

B9x 压帧后，每次 IRQ 入口都会把 IRQ SP 下移 0x18；本地 HLE 不走 BIOS 的
`ldmia sp!,{r0-r3,r12,lr}` 返回路径，SP 一直没有弹回。约 180 帧后 IRQ 栈从
DTCM 顶部一路溢到 0x027E0000 的 handler 表，把 FIFO 表项 0x027E0048 覆盖成
0x04000188；下一次 FIFO 中断就 `bx` 到 IO 地址开始空扫。长跑 2 亿步复现：
handler 表被覆盖、ARM9 PC 从 0x04000188 逐字扫到 0x0983CFA4。

修复：`irq_hle_ctx` 记录压帧前的 IRQ SP，`irq_hle_restore` 返回前恢复。
2 亿步与 5 亿步长跑均保持 0x0200957C 空闲，不再覆盖 handler 表；
`[case 21-B9c]` 补 SP 恢复断言，全量 **739 项检查 0 失败**。

### 21-B9z（2026-09-06）：B7 地址掩码/重定向 + ROM 尾部零填充

对照 melonDS `CartCommon::ROMCommandStart` 的 B7 分支，补上两处口径：
地址先 `& ROMMask`（补成 2 的幂后的容量-1），请求落在 0x8000 以下时重定向到
`0x8000 + (addr & 0x1FF)`；同时 `PadToPowerOf2` 尾部是 0 而非 0xFF。
测试假 ROM 放大到 0x10000 并改用 0x8100 地址，保持 739 项 0 失败。

### 21-B9za（2026-09-06）：GXSTAT FIFO IRQ 模式 → IF bit21

参考日志里 ARM9 IF9 一直带 0x200000（GXFIFO），本地始终没有。对照 melonDS
`GPU3D::Write8`/`CheckFIFOIRQ`：GXSTAT 0x04000603 的 bits30-31 是 FIFO IRQ
模式，FFXII 设为 mode=2（FIFO 空触发）后 IF21 应挂起。本地把 GXSTAT 当只读，
游戏配置被吞掉。补上可写模式并在 io 层同步 IF21，本地 IF9 与参考一致。

---

## 4. 装载时“secure: not encrypted”不是错误

FFXII 这份 ROM 的安全区已经是**解密后的形态**：开头不是 `"encryObj"`，而是
`E7 FF DE FF …`（真机用于“防执行”的占位头）。`cart_decrypt_secure_area`
校验 `"encryObj"` 失败就保持原样，所以打印 “not encrypted (homebrew) or decrypt failed”，
对已解密的商业 ROM 这是正确行为，不需要改代码。

---

## 5. Phase B 路线（每轮一个 gap）

Phase B 的验收口径：**hard_title**——FFXII 能进入标题画面。

每轮固定节奏：

1. 给本轮微步写计划（改哪些文件、用例、验收标准）；
2. 实现 + 自测（`ctest` 全绿 + `test_nds.exe` 0 失败）；
3. 用 `--headless N` / `--trace` 跑 FFXII，观察下一卡点；
4. 更新 `DEVLOG.md` + 相关模块日志（必要时补学习文档）；
5. 提交并汇报，进入下一轮。

按当前 gap 清单，顺序预排为：

| 微步 | 做什么 | 验证 |
|------|--------|------|
| B1 | 映射 Shared WRAM：0x03000000 起 32KB + 0x037F8000 镜像（✅ 已提交 bf8899b） | 单测 + headless 重跑，确认 ARM7 不再在镜像区逐 0 漂移 |
| B2 | 实现 IPCSYNC（0x04000180/81）：双核 out 交叉读写、bit13 → 对端 IF16、bit14 门控 | 单测 544 项 0 失败；headless 不再报 IPCSYNC 未知 IO |
| B3 | 映射 Main RAM 无缓存镜像 0x02400000-0x027FFFFF（FFXII 栈放 0x027E0000 附近） | 单测 552 项 0 失败；headless：ARM9 稳定在 0x0200B9xx 主存代码区，不再弹 PC=0 |
| B4 | 修 ARM BX 奇地址未切 Thumb（ARM7 0x038043C9 入口） | 单测 555 项 0 失败；ARM7 不再按 ARM 误译 Thumb 区，旧 0x0626D1xx 终点消失 |
| B5 | 补 Thumb 逐条 trace，定位并修复 ARM7 跳进 IO 区的根因（Thumb BX/BLX Rm 解码错误） | Thumb trace + headless：IO 刷屏消失，ARM7 正常进出 SWI3/BX-lr 桩（557 项 0 失败） |
| B6 | ARM9 栈 LR 污染定位（当时以“镜像仅 ARM9 可见”临时修复；B8 修正为 ARM9 DTCM 未实现） | LDM trace + 写监视：ARM9 过 0x0200B9B4 返回点、不再弹 0xE1C010B0（559 项 0 失败） |
| B7 | ✅ 排查 ARM9 新未知 IO（0x04000204/205、0x04000247）并实现 EXMEMCNT/WRAMCNT + Shared WRAM 双核切分 | 587 项单测 0 失败；headless 3 条未知 IO 消除 |
| B8 | ✅ 解码 ARM7 0x027FFFF0 命令口与 ARM9 0x027FFF8C 事件位约定；实现 ARM9 DTCM/ITCM、镜像双核别名、direct-boot 表 | 594 项单测 0 失败；headless 越过旧轮询，ARM9 到 0x02009EC0、ARM7 到 SWI 0x0E |
| B9a | ✅ SWI 0x0E GetCRC16（CRC-16/IBM）：新增 `src/bios/bios_crc16.*`，ARM/Thumb/ARM7 单测 | 600 项单测 0 失败；headless 不再报 unknown SWI 0x0E，ARM7 进 0x037FC89C |
| B9… | ARM9 BIOS/IRQ 高向量 + 批次未知 IO 桩（GetCRC16 已完成） | 每个 gap 一次提交 |

> B9 后续实际按 gap 拆成小步：B9a（本表，GetCRC16）已完成，剩余 ARM9 BIOS/IRQ
> 高向量与未知 IO 桩继续逐 gap 提交。

> 后续步骤只有在真机现象出现后才能精确拆解，这也是本项目“一次一个微步”的原因——
> bring-up 阶段不预先猜十步，而是一步一个证据地往前走。

---

## 6. 自测

1. ARM9/ARM7 的 PC 若按 +4 匀速漂移，通常说明什么？
2. Shared WRAM 主区（0x03000000）和镜像区（0x037F8000）为什么必须映射到同一块内存？
3. `--headless` 与 `--trace` 各自适合什么时候用？
4. “secure: not encrypted”为什么不一定代表解密代码有 bug？

<details>
<summary>答案</summary>

1. 多半是在执行未映射内存读回的 0（`ANDEQ r0,r0,r0`），执行流已经跑飞。
2. 真机同一块 32KB RAM 有两个地址别名；不映射会让写入丢失、读回 0，启动代码拷贝过去的内容执行不了。
3. headless 适合大批量跑（看卡在哪个地址区间）；trace 适合小步追（看具体跳转/寄存器/未知访问）。
4. 商业 ROM dump 可能已由工具解密并把安全区开头替换为 `E7FFDEFF`，此时无需再解密。

</details>
### 21-B9zb（2026-09-06 分析）：长帧参考对齐后，卡点收敛到 ARM7 周期对象与卡带零延迟

**背景**：B9za 后本地停在 ARM9 0x0200957C 空闲任务，参考在 80/128 帧内也停在
0x02009584，无法直接看出差异。本轮用补帧法把参考 exe 循环初值改到 -2400，跑满
2480 帧，并做了 500/530/560/770 帧偏移快照。

**关键证据**：

1. 参考 ARM7 WRAM 0x03808480 每帧 +1；约第 534-539 帧之间任务队列指针
   0x0380A740 从 A88C 走到 A8EC、对象计数 7→11，同时 ARM9 当前任务从空闲切到
   0x02076FE4，并开始往 Main RAM 0x02079640+ 写入文件记录（可看到 `U.vx`、
   `SWD_U.vx` 等资源名）。即参考不是“一直空闲”，而是约 17 秒后才开始第二阶段。
2. 本地用 200M/800M 步快照对齐 ARM7 计数（0x8480=0xBD/0x2F9，与参考约 190/770
   帧接近），但本地当前任务始终是空闲 0x02076F24，worker 0x02076FE4 状态恒为 1
   却不被调度；0x0380A740 停在 A7FC、对象计数 1，参考则为 A8xx/A94C 且 ≥7。
3. 本地 ARM7 的 0x038070F0 状态机与 0x03804588 入队在启动后仍被反复调用：
   参考 80 帧里 0x03804588 只出现 23 次（帧 7-13），本地在 ARM7 周期 35M 内已打满
   400 次上限；本地 0x03808244 也被持续改写（0xBF/0x303），参考稳定在 0x18。
4. 按 ARM7 tick 对齐取样：本地在第 0-15 tick 期间就已经进入空闲，而参考同样 tick
   区间还在 0x020119xx 卡带读循环；说明本地第一阶段“完成太快”，不是帧数不足。
5. cartbus 仍是“瞬时传输”模型（命令激活立即 DRQ/数据就绪）。实测 FFXII 发 B7
   时 ROMCTRL=0xA1416657：按 melonDS 公式 xfercycle=8、cmddelay=8+0x657+
   ((ROMCTRL>>16)&0x3F)=1632，首字就绪延迟为 8×(1632+4)=13088 周期，后续每字
   约 32 周期、每 0x200 块边界再加间隙；当前本地完全跳过这些等待。

**结论**：ARM9/ARM7 的握手本身已能推进，但零延迟卡带让第一阶段读卡在 ARM7 周期
任务尚未建立完整对象队列前就提前完成，后续 ARM7 反复入队、ARM9 不再消费，最终
双核僵在“worker ready 但无人调度”。下一步实现 cartbus 按 ROMCTRL 的周期延迟
（首字/cmd/gap/逐字），再与第 539 帧参考快照对齐。
### 21-B9zc（2026-09-06）：卡带 B7/B8 周期时序

按 21-B9zb 的分析实现 melonDS 口径的卡带就绪延迟：

- `cartbus_t` 增加 `wait_cycles/xfer_pos/wait_phase`；命令激活后先清 DRQ，
  首字延迟 = xfercycle×(8 + ROMCTRL 低 13bit + 数据间隙 + 4)，逐字延迟 =
  xfercycle×4，每 0x200 块边界再叠加 ROMCTRL bit16-21 间隙（NDSCart.cpp
  `ROMAdvanceReceive` 同款算法）。
- `io_advance_cart()` 每个 ARM9 指令周期推进卡带时钟；就绪边沿置卡带中断并触发
  卡带 DMA；DMA 从 CARD_DATA 取数前也按就绪时钟等待。
- 单测改为激活后先验证“未就绪”，再推进足够周期后读数据；新增 1 项检查，
  全量 **743 项 0 失败**。

**FFXII 效果**：ARM9 不再 2M 步就提前空闲，而是在 0x020119xx 卡带读循环停留到约
39.8M 步（ARM7 tick 约 46 帧）才进入空闲，第一阶段节奏明显接近参考。但长跑到
900M 步仍未出现参考第 539 帧的第二阶段。

**新证据/下一卡点**：本地 ARM7 状态机仍把 0x027FFC30 写成 0（PC 0x0201270C 从
0x020798A0+0xBE 拷贝），参考保持 0xFFFF；0x020798A0 区域参考在 frame3→4 之间从 0
变为 FF，本地一直被多处清零（0x020008AC/0x02009E68 等）。下一步对照参考 frame3→4
之间初始化该区域的代码路径，找出本地少执行/多清零的分支条件。
### 21-B9zd（2026-09-06 实验）：强制保留 0x027FFC30=FFFF 后的结果

做了一次不提交的验证补丁：在 bus 层阻止 ARM9 把 0x027FFC30 清 0，长跑到 900M 步。
结果：

- 0x027FFC30 保持 FFFF；ARM7 的 0x03808240=1、0x0380BA7C=1、0x03808244=0x15，
  状态机不再像原来那样反复入队（0x03804588 风暴停止）。
- 但 ARM7 对象区 0x0380A740 仍停在 0x0380A7FC、0x0380A7B4=1，而参考第二阶段前是
  A88C/计数 7；长跑仍无第二阶段。

结论：0x027FFC30 被写成 0 只是 ARM7 状态机风暴的“后果/旁路”，不是第二阶段缺失的
根因。真正的差异仍是本地 ARM7 基座对象太少（计数 1 vs 参考 7），下一轮应对比
0x0380A740 队列在本地/参考中的分配与释放路径。
### 21-B9ze（2026-09-06 分析）：ARM7 服务任务首个上下文 r1 差 4 字节

用临时写监视定位 ARM7 服务管理器指针偏移的写入点：

- 0x0380A4F0+0x10 与 0x0380A4F8+0x08 在本地为 0x0380A4FC、参考为 0x0380A500，
  差正好 4 字节。
- 写监视显示该值不是由“指针计算”直接写入，而是来自 ARM7 第一次保存任务 A4F8
  上下文：Shared WRAM 0x037FCB14 的 `stmia r1!,{r0-r14}` 把当时用户 r1 的值存进
  A4F8+0x08。参考那一刻 r1=0x0380A500，本地 r1=0x0380A4FC。
- 说明问题发生在“进入第一次保存之前，谁给用户 r1 赋了 A500/A4FC”，而不是链表
  插入或 STM 语义本身。下一步反查任务 A4F8 创建链 0x0238481C/0x02384C38 及其
  调用方，确认本地为何把入口指针少算了 4。
### 21-B9zf（2026-09-06 实验）：强制 A500 自指不能触发第二阶段

用不提交的 bus 补丁把 ARM7 写 0x0380A500 的任意值都改写为 0x0380A500（模拟参考
快照里的自指字段），长跑到 900M 步。结果：

- ARM9 仍停在 0x0200957C 空闲，第二阶段未出现；
- 说明 A4F8+8=A500 不是第二阶段缺失的充分条件，只是参考上下文里的一个状态值；
- 本地更深层差异仍是 ARM7 服务对象队列/任务调度本身，不是单独一个字段。

已还原实验补丁，743 项测试 0 失败。
### 21-B9zg（2026-09-06）：ARM7 STM 基址寄存器在列表内保存当前写地址

用临时重建的 melonDS 参考采集器（refhead，带 ARM7Write32 日志）抓到决定性证据：

- 参考保存任务 A4F8 时，STMIA 的 r1 槽写入的是 **0x0380A500（当前写地址）**，
  本地写的是 0x0380A4FC（原 r1 值），导致上下文自指字段差 4。
- melonDS `A_STM` 的口径：ARM7 的 STM 当基址寄存器 rn 也在寄存器列表、且列表里
  存在更低编号寄存器时，该槽保存当前写地址而不是原 rn；ARM9 与“无更低寄存器”
  的情况仍保存原值（源码里标了 “checkme”，但 FFXII ARM7 OS 依赖此行为）。
- 本模拟器 `exec_block_transfer` 已补同一口径，新增 `[case 21-B9zg]` 四项断言，
  全量 **747 项检查 0 失败**。

**FFXII 效果**：任务 A4F8 上下文自指字段与参考一致，但长跑到 900M 步仍未出现
第二阶段；说明该语义修复是必要条件，ARM7 服务对象队列仍缺后续入队/任务对象。
### 21-B9zh（2026-09-06 分析）：本地 ARM9 完全没有发出 service11

B9zg 后 ARM9 仍进不了第二阶段。用参考/本地两侧 FIFO 与 A79C 队列日志对比：

- 参考 80 帧内 ARM9 共 25 次 FIFO 发送，其中 service11=20 次（0x2B/0xAB/0x1AB/
  0x22B/0x26B），service13/6/7 各 1-2 次；本地 5M 步里只有 20 次发送，service11
  **0 次**，service13/6/7 后直接停在卡带读循环。
- 参考 A79C 队列对 A4F8 的插入 284 次；本地 2M 步只插入 11 次（随后 5 次移除），
  最终 A79C 头/尾为 0，参考保持 A4F8。
- 说明卡点已收敛到“service7 完成之后 ARM9 为什么不发 service11”，而不是显示或
  调度器本身。下一步在 ARM9 PC 0x0200B81C 附近抓 service7 完成后的返回分支。
### 21-B9zi（2026-09-06 分析）：service7 后卡带循环时长差一个量级

本地 a9send 日志显示 ARM9 在 0x0200B81C 只执行 4 次 FIFO 发送（service13/6/6/7），
之后进入 0x020119xx 卡带读循环约 1M 步就空闲；参考 fifo9 日志中 service7 在第 9
帧、service11 从第 21 帧开始，中间 0x020119xx 循环持续约 12 帧。

- 本地 5M 步终点仍在 0x020119xx，6.3M 步左右已空闲；参考至少到第 20 帧仍在
  同一循环并持续发 service11。
- 说明本地不是“少发一条消息”，而是 service7 打开的读请求在 ARM7 侧没有持续喂给
  卡带循环，循环被提前认为完成。下一轮对照参考第 9-21 帧内 ARM7 服务对象队列
  （0x0380A7FC 起的 16 个 0x18 槽）逐槽变化。
### 21-B9zj（2026-09-06 分析）：事件号停在 0x41，参考为 0x42

沿状态机调用链 0x02010D6C ← 0x02010AE4 ← 0x0200DA30 追到事件号来源：

- 参考 0x02010DD0 写入的状态号是 0x42，本地是 0x41；
- 事件号来自半字 0x02078E5C（读路径 0x0200DA20-30），本地 0x41/参考 0x42，
  正好少一次“服务完成事件”；
- 0x02078E5C 附近没有直接字面量写入，说明它由 ARM7 完成/入队路径按事件累加；
  下一步抓 0x02078E5C 的写 PC，找到本地漏掉的第 0x42 次事件。
### 21-B9zk（2026-09-06 分析）：事件分配位图同样是“少推进”的结果

继续向上追事件号来源 0x020067D8：分配器使用位图 0x027FFFB0。快照对比：

- 参考 0x027FFFB0=0x1FFFFFFF，本地=0xBFFFFFFF；
- 本地还有更多高位空闲，说明不是位图被错误置位，而是本地确实少分配/少释放了
  后续事件，位图只是“进度落后”的镜像；
- 因此根因仍回到 ARM9 在 service7 后没继续走“申请下一事件/发 service11”的分支。

下一步不再追位图，改为在 0x0200D9FC（service 分发）处对比本地/参考的 r1 和返回
分支，找出 service7 之后它停在哪个返回值。
### 21-B9zl（2026-09-06 分析）：service 分发只完成 4 轮事件就等待

在 0x0200D9FC 打点：本地只交替执行 r1=9 / r1=0xA 共 4 轮（r1=9 4 次、r1=0xA
3-4 次）就停在等待；参考会持续数十轮，直到 service11 全部发出。

- r1=9 路径每次从 0x02078E5C 取事件号并推进状态机；
- 本地事件来源在 4 轮后枯竭，说明 ARM7 服务任务 A4F8 只被激活 11 次（参考 284
  次）是更上游的现象；
- 下一步不再追 ARM9，改为追 ARM7 0x037FC510（A4F8 激活/入队调用点）在本地为什么
  只执行 11 次：对比它到 0x03804588/0x038049DC 服务循环之间的调用条件。
### 21-B9zm（2026-09-06 实验）：帧/VCOUNT 节奏 4 倍加速仍无 service11

在 STM/卡带修复后再次做帧节奏实验（VBlank 从 1M 步缩到 262K、扫描线约 1K），
跑到 20M 步：

- ARM9 仍在约 5M 步后进入 0x0200957C 空闲，service11 依旧未出现；
- 说明缺口不是“参考在 12 帧内完成、本地帧太少”，而是本地 ARM9 在 service7
  后的某个分支/状态没有推进。

已还原节奏，747 项测试 0 失败。
### 21-B9zn（2026-09-06 代码 + 复测）：A_STM 基址口径补到 ARM9；本地其实已发 service11

对照 melonDS `ARMInterpreter_LoadStore.cpp::A_STM` 确认：STM 的 rn 在寄存器列表
内且存在更低编号寄存器时保存“当前写地址”，是 ARM9/ARM7 共用的解释器口径。
B9zg 只对 ARM7 生效；本步去掉 `is_arm7` 条件让 ARM9 也按同一规则保存上下文，
并补 4 项 ARM9 单测（`[case 21-B9zn]`）。全量 **751 项检查 0 失败**。

用临时发送点探针（PC=0x0200B814 前读 r2、DMA/FIFO/调度器计数）复测真 ROM：

- 本地其实**已经能发出 service11**：ARM9 在约 4.01M 周期从 0x0201115C 连续发
  `0000002B`、`81E3E82B`、`000000AB`，ARM7 回 `0000006B` 两次；
- 0x0200B65C FIFO 分发循环正常按 idx=0xB 调 0x02011F34 清 `[ctx+0x114] bit5`
  并唤醒 0x02076FE4；本地写 `[ctx+0x104]`/`[0x02076F18]` 的值与参考同刻一致，
  不是等待指针被写错；
- 卡点从“没有 service11”修正为“3 次后不继续”：唤醒后调度器反复切到
  0x02079960/0x02076FE4/0x02076F24，最终回 0x0200957C 空闲；参考 frame21-23
  会继续 20 次左右的 service11 轮次；
- 主存缓冲对照：本地 0x02180000 起内容与参考 f20 不是单纯少块，而出现约
  +0xC3F 的偏移（本地 0x0218FE80 的模式在参考 0x0218F241），指向更早的
  ARM7 C024 回执节奏/数据落点差异，而非 ARM9 任务上下文保存。

下一步回到“service7 后 C024 回执为什么本地只 11 次、参考 46 次”，对照帧级
回执与 0x02180000 数据落点。
### 21-B9zo（2026-09-06 代码）：ROMCTRL 块长 off-by-one 是缓冲缺块根因

melonDS `NDSCart::WriteROMCnt` 的块长公式是 `datasize = 0x100 << datasize`
（字段 1=0x200 … 6=0x4000，7=4，0=None）；本地旧实现写成
`0x100 << (blk-1)`，把 FFXII 常用的字段 1 解成 0x100。

效果链：B7 每块只就绪 64 字，读循环提前退出；0x0201193C 仍把目标指针前进
0x200，于是主存缓冲每隔 0x100 缺一段。写入点探针定位到 0x0201192C 的
64 字/块模式，对照 melonDS 源码确认映射后修正。

验证：

- 15.2/15.4/15.5 B7 激活改用块字段 1；全量 **751 项检查 0 失败**；
- 真 ROM 60M 步结束时 0x0217F000-0x02190000 与参考 frame30 **0 字差异**，
  旧实现约每 0x200 少 0x100；
- ARM9 不再在 4.01M 周期回空闲，继续读卡带到约 7.58M 周期后停在
  service11 三次后的空闲。

下一步：仍停在 1115C 发完 2B/81E3E82B/AB 后未返回 0x0203A4BC，112E0/
113D4 分块循环未进入；追第二次 6B 唤醒后的 ARM9 任务调度/上下文恢复。
### 21-B9zp（2026-09-06 分析）：第二次 6B 唤醒后调度器多切回空闲任务

在 0x0201115C/11228/1128C、0x0203A4BC、0x0203A510/0x0202CA0C、
0x020078D4/7820、0x020077D4、0x01FF8120 打点，并把参考采集器扩到
0x02077048/4C、0x02076F88/8C、0x020799C4/C8 后对比：

- 第一次 6B 唤醒：0x02011F34 置 `[0x76FE4+0x64]=1`，ITCM 尾部
  0x01FF8120 把 F18 切到 0x76FE4，随后本地成功续跑并发出 AB；
- 第二次 6B 唤醒：ITCM 尾部同样把 F18 切到 0x76FE4，但本地随后在
  0x020077D4 又切回 0x76F24 并恢复出空闲 WFI（0x0200957C）；
- 参考同一时刻在 ITCM 切到 0x76FE4 后直接发下一条 1AB/22B，没有
  再次切回 0x76F24；唤醒瞬间 `[0x76FE4+0x64]` 两侧都是 1。

对 FIFO 发送/接收加队列计数复测后排除“积压”：本地每次 ARM7 发送后 ARM9
立即读走，`from7` 深度始终 0/1，与参考 fifo7 行的 f=0/0 一致；第二次 6B
前后都没有 20 余条 C024 积压。差异只发生在 ITCM 0x01FF8120 把 F18 切到
0x76FE4 之后的调度器执行流（本地 0x020077D4 又选 0x76F24，参考直接续跑）。

下一步方向：在 0x01FF8120 之后的 CPU 指令流上逐条对比本地/参考，定位本地
多出的“再选 0x76F24”来自哪次 07708 调用（可能是任务状态/就绪链顺序差异）。
### 21-B9zr（2026-09-06 分析）：第二次唤醒后的 0x76F24 不是新扫描结果

在 0x0200775C（扫描返回）与 0x020077D4（写 F18）同时打点，并打印就绪链
（head=0x79044 → 0x76FE4 → 0x79960 → 0x76F24）与各任务 state：

- 第二次 6B 之前最后一次扫描（0x00733613）已返回 0x76F24，随后
  0x00733655 写 F18=0x76F24（睡眠切出）；
- 第二次 6B 唤醒（0x02011F34 → 0x02007820）把 `[0x76FE4+0x64]` 置 1，
  ITCM 0x01FF8120 写 F18=0x76FE4；
- 之后 0x007337A6 再次写 F18=0x76F24，但**两次写之间没有新的 0775C 扫描**，
  说明这不是“扫描又选了 0x76F24”，而是 ITCM 切换后的执行流把旧的
  0x76F24 选择又落盘一次，0x76FE4 没有真正成为当前任务；
- 参考同一时刻 F18=0x76FE4 后直接发下一条 service11，没有这次回写。

下一步重点从“调度器选错任务”改为“ITCM 0x01FF8120 切换本身为何在本地
只写 F18、没有把 0x76FE4 的上下文真正恢复为当前”：
检查 0x01FF8120 前后 CPSR/SP/保存帧与 LDM ^ 的交互。
### 21-B9zu（2026-09-06 大改造路线）：双核周期级调度 + 内核切换语义

用户确认采用大改造路线。当前 headless/窗口主循环都用固定
“ARM9:ARM7=2:1 指令交错”近似时间，IRQ/上下文切换只能落在固定边界上；
第二次 6B 唤醒反复被旧的 0x76F24 sleep 切换抢回，可能是该近似的边界问题，
也可能需要周期成本才能复现参考的唤醒边界。

改造分四步，全部做完前不把“service11 继续”当成完成条件：

1. **周期成本模型**：给 `arm_cpu_t` 增加每条指令的近似成本
   （参考 melonDS `CodeCycles/DataCycles` 与区域内存时序），先把
   `cpu->cycles` 语义与“指令数”解耦，补成本单测；
2. **事件目标调度器**：新增统一时间戳，支持最小事件表（VBlank、
   扫描线、定时器、FIFO IRQ、卡带就绪），按 `NextTarget` 决定一次执行
   到哪一时刻；
3. **双核驱动迁移**：headless 与窗口主循环都改为按时间戳跑
   （ARM9 2 倍时钟、ARM7 1 倍），WFI/Halt 不再空转占调度；
4. **回到第二次 6B**：用参考时间戳（如 t9≈24.35M、t7≈12.17M）校准后，
   重新比较 ITCM 0x01FF8120 切换与 0x76F24 回写，直到 service11 能
   继续发 1AB/22B。

若第 1-3 步完成后第 4 步仍复现，则把内核切换本身（CPSR 模式、
LDM/STM ^、banked r13/r14）抽成最小可复现单测再逐条对照 melonDS。
### 21-B9vv（2026-09-06 代码）：周期成本模型骨架

路线第 1 步落地最小骨架：`arm_cpu_t.step_cycles` 表示刚执行指令的近似
成本，WFI 无挂起中断时置 0（等待不消耗调度时间），普通指令置 1；
`cpu->cycles` 仍保留“已执行指令数”语义，避免破坏现有单测。

验证：新增 4 项断言，全量 **755 项检查 0 失败**。下一小步把成本从固定 1
升级为按指令类别/内存区域计算的 melonDS 近似值。
### 21-B9vw（2026-09-06 代码）：事件目标调度表

新建 `src/timing/` 最小事件表：`timing_t.now` 为系统时间戳，8 个事件槽
可 arm/disarm；`timing_next()` 给出下一截止，`timing_advance(target)` 推进
并触发到期事件（回调可重新武装）。单测覆盖最早截止、到期前不触发、
自动解除与重新武装，全量 **762 项检查 0 失败**。

下一步把扫描线/VBlank 事件用该系统时间驱动，先保留固定 2:1 作为回退，
再逐步把 FIFO/卡带事件接入。
### 21-B9vx（2026-09-06 代码）：事件目标 headless 驱动（实验入口）

新增 `runner_headless_cycles()`：用 `timing_t` 挂扫描线/VBlank 事件，
按系统时间戳选择 ARM9/ARM7（ARM9 时钟 2 倍），WFI 等待不占调度；
命令行 `--headless-cycles N` 启用，`--headless N` 固定 2:1 保留回退。

当前事件目标驱动仍停在 service11 三次后，与固定驱动同点；本步只固化
架构入口，后续调成本/事件边界时用它复现。
### 21-B9vy（2026-09-06 分析）：参考时间戳校准基准

给参考采集器打印 `ARM9Timestamp/ARM7Timestamp` 后，第二次 service11
窗口的时间戳（frame21）：

- 第 1 个 6B：t9=24,353,584，t7=12,176,730；随后 AB 在
  t9=24,354,663（Δt9=1079，Δt7=545）；
- 第 2 个 6B：t9=24,357,120，t7=12,178,504；随后 1AB 在
  t9=24,358,251（Δt9=1131，Δt7=588）；
- 之后每组“6B→下一条 service11”的 Δt9 都在 1079-1131 之间。

本地固定 2:1 在第一次 6B→AB 之间只走约 548 条 ARM9 指令，说明周期成本
需让本地该窗口的累计成本与参考 Δt9 对齐，而不是简单翻倍所有指令。
### 21-B9vz（2026-09-06 分析）：第二次 6B 后旧 sleep 收尾顺序相反

给 melonDS ARM9 解释器直接加 0x0200778C/077D4/07840/01FF8128 寄存器钩子，
对照第二次 6B 窗口：

- 参考：6B → 07840（wake）→ 01FF8128（F18=76FE4）→ **先发 1AB**
  → 之后才执行 0778C（r5=76F24、r6=76FE4）完成旧 sleep 收尾；
- 本地：6B → 07840（wake）→ 01FF8128（F18=76FE4）→ **先执行
  0778C/077D4 把 F18 写回 76F24** → 进空闲 WFI，没有发 1AB。

因此问题不是“参考取消旧切换”，而是“参考让 service 续跑先于旧 sleep
收尾，本地顺序反了”。下一步把 ITCM 切换后实际恢复的上下文/寄存器集与
参考逐指令对比，定位本地为什么恢复的是旧 sleep 上下文而不是 service
续跑上下文。

### 21-B9wa（2026-09-06 代码/测试）：ARM9 IRQ 返回桩改为 FreeBIOS 尾部

**逐指令对照（方式 1）**：给 melonDS ARM9 解释器加“0x01FF8120 后连续
1500 条”的 [tr] 钩子，本地在 cpu_step 临时加同窗口 [ltr]，对齐第二次
6B（参考 t9=24,357,580 起）：

- ITCM 0x01FF8120（写 F18=76FE4）→ 01FF8198（LDMIB ^）两侧寄存器几乎
  完全一致：LDM ^ 后 r0=1、r1=02077020、r4=02076F14、r5=02076F24、
  r6=02076FE4、r8=1、r9=0xB、sp=027E3F78、lr=02007F78；
- 分歧出现在 0x01FF81A0 的 `ldmia sp!,{pc}`：
  - 参考弹到 **0xFFFF06F0**——FreeBIOS IRQ 入口（0xFFFF06D8）先
    `stmdb sp!,{r0-r3,r12,lr}` 压六字帧，再用 `mov r14,r15` 把 lr 设成
    返回桩地址；ITCM 尾部 STMDA 后从栈底弹回的就是该桩。随后 BIOS 尾部
    `ldmia sp!,{r0-r3,r12,r14}; subs pc,r14,#4` 把新任务接到
    0x02007F74/0778C，service11 续发 1AB/22B/26B/00040005；
  - 本地弹到 **0x02009580**（被打断旧空闲任务 PC）——本地 HLE 把
    handler 的 lr 直接设成被打断 PC，irq_hle_restore 恢复旧 sleep 上下文，
    之后 077D4 把 F18 写回 76F24 并回空闲。

**做了什么**：

- `cpu.c` ARM9 IRQ 槽跳转改为等价执行 FreeBIOS 0xFFFF06D8 入口：压
  r0-r3/r12 + 被打断 PC+4 六字帧，lr=0xFFFF06F0（不再用 irq_hle 私有槽
  保存 ARM9 现场）；
- 新增 `bios_irq_tail9`：当 ARM9 在 IRQ 模式执行到 0xFFFF06F0 时模拟
  `ldmia sp!,{r0-r3,r12,r14}` + `subs pc,r14,#4`（用 SPSR_irq 恢复
  CPSR），等价真机 BIOS 尾部；IRQ HLE 私有恢复只保留给 ARM7；
- `[case 21-B9c]` 改写为新语义：lr=桩地址、handler 弹栈先回桩、tail 恢复
  现场后重执行被打断指令、IRQ SP 回弹。

验证：全量 **763 项检查 0 失败**。

真 ROM 效果：第二次 6B 不再被旧 76F24 sleep 切换抢回——service11 从 3 次
续到 AB/1AB/22B/26B/00040005 等多轮；headless 60M 步终点从约 7.55M 周期
空闲推进到约 16.4M 周期空闲（ARM9 0x0200957C），显示初始化之前的卡点需
继续追下一处。

### 21-B9wd（2026-09-06 代码/测试）：VRAMCNT 动态映射

**证据**：参考核心导出 frame120 的 VRAMCNT=83/8B/84/81/82/83/85/82/82，
对应 bank D→Engine A BG、bank C→Engine B BG、bank E→A OBJ、bank H→B OBJ，
而 bank A/B 只作 3D 纹理。本地旧总线把 bank A 当顶屏 BG，顶屏取错 bank；
本地 bank C/D/E/H 原始字节与参考完全一致，BG/OBJ 控制寄存器和调色板也一致。

**做了什么**：

- bus 增加 vramcnt[9] 与 ABG/AOBJ/BBG/BOBJ/tex 槽位掩码，移植 melonDS
  `MapVRAM_AB/CD/E/H` 的当前必需分支；0x06000000-0x067FFFFF 的读写按映射表
  落到物理 bank；
- io 层把 VRAMCNT 寄存器（ARM9 视角）接给 bus，ARM7 的 0x04000241 保持
  WRAMCNT 语义；
- 新增 `[case 21-B9wd]`，全量 **768 项检查 0 失败**。

**效果与遗留**：bank C/D 与参考逐字节一致后，Engine A 的 THINK&FEEL 画面已能
渲染；但换屏后顶屏（参考 SQUARE ENIX 的 Engine B）仍是黑屏，说明下一个 gap
在 OBJ/扩展调色板或仿射精灵渲染路径，而不是 BG bank 映射。

### 21-B9we（2026-09-06 代码/测试）：LCDC 分 bank 映射 + BG 扩展调色板

**证据**：参考核心的 H via LCDC 日志显示扩展调色板在 0x0689C000 写入
（mask 后 addr=0x4000，即 bank H 槽 2 起点），值正是 H[0x4000] 的
0x7FE0/0x842 调色板。旧本地把 LCDC 地址按线性 VRAM 偏移映射，写到数组错误
位置，bank H 保持 0；修复后本地 bank H 与参考逐字节一致。

**做了什么**：

- bus_resolve 的 LCDC 区按 bank 窗口映射（A-D 每 128KB，E/F/G/H/I 各自小窗），
  写 0x0689C000 落到 vram bank H 的 0x4000 偏移；
- bus_set_vramcnt 的 H mode2 同时接到 Engine B BG 扩展调色板；新增
  bus_vram_extpal16，renderer 在 DISPCNT bit30 时对 256 色 BG 使用扩展调色板；
- `[case 21-B9wd]` 补 LCDC/ext 2 项，全量 **770 项检查 0 失败**。

**效果**：本地 Engine B 已能渲染出与参考一致的 SQUARE ENIX 画面（同为
81 色、46288 个近白像素的统计形状）；两屏现在分别是 SQUARE ENIX 与
THINK&FEEL，开发商画面完整。继续追标题画面（下一帧段/输入）阶段。

### 21-B9wf（2026-09-06 代码/测试）：ARM7 FreeBIOS 低地址等待路径 HLE

**证据**：给参考 ARM7 解释器加 0x115C/0x112C/0x1FB0/0x1FC0 采样，看到
FFXII 自 frame 8 起大量时间停在 **0x0000115C**，反汇编确认这是
`subs r0,#1; bgt`（SWI 3 WaitByLoop），不是 Halt。本地旧 HLE 把 SWI 3 当
空操作、SWI 6 当“SWI 原地重跑”，ARM7 只执行约 30 万条指令后就停在 WRAM
包装器上；参考同阶段 ARM7 的时间戳持续增长并反复走低地址。

**做了什么**：

- `arm_cpu_t` 增加 bios7 状态机字段；SWI 3 进入 HLE 的 0x115C 循环
  （每步减 r0、step_cycles≈3，减到 0 走 0x112C 尾部恢复调用方）；
- SWI 6 进入 0x1158 Halt 暂停态：只被 (IF&IE) 唤醒（ARM7 不要求 IME），
  唤醒先走 BIOS 尾部再让 IRQ 接管；
- `bios_dispatch` 新增 REDIR 返回码，避免 exec/thumb 在 HLE 已改 PC 后
  再前进；
- 事件 headless 修复两个等待陷阱：单核等待时系统时间由运行核推进；
  等待核唤醒时成本跳到当前系统时间，不再把暂停期当积压指令补跑。

**验证**：新增 `[case 21-B9wf]` 20 项断言（delay 三轮/成本/尾部恢复、Halt
暂停/唤醒/尾部恢复），全量 **790 项检查 0 失败**。真 ROM 事件驱动长跑里
ARM7 不再沿主存跑飞；下一卡点收敛到周期时间模型/事件表让系统时间真正按
560190 周期/帧推进，并让 ARM9 定时器在等待期继续走。

### 21-B9wg（2026-09-06 代码/测试）：ARM7 IRQ 入口/尾部改 FreeBIOS 桩

- 与 B9wa 对称：ARM7 IRQ 触发时等价执行 FreeBIOS 0x1FB0（六字帧压栈 +
  lr=0x1FC0 返回桩），`bios_irq_tail7` 在 0x1FC0 弹帧并用 SPSR_irq 恢复，
  不再依赖模拟器私有的 irq_hle 恢复槽。
- 依据：跑飞现场 0x038071BC 的函数在恢复 r14 时拿到数据区 0x0380A740，
  说明任务恢复把“调用方返回点”和“上下文首指针”混用；这与 ARM9 在 B9wa
  之前的症状同族，先按参考 FreeBIOS 栈帧语义收敛。
- 验证：`[case 21-B9i]` 增加 0x1FC0 桩/弹帧两步断言，全量 **792 项
  0 失败**。真 ROM 事件 20M 步内不再出现早期跑飞；Timer0 回绕后的标题段
  仍在追，作为 B9wf 大改造的下一卡点。

### 21-B9wh（2026-09-06 代码/测试）：CLZ Rd 掩码修正，标题解码越过崩溃点

**证据**：标题段逐指令对照停在 0x01FF847C。该地址的 E16FAF13 被本地
反汇编标成 `cmn r15,r3,lsl r15`，实际是 **CLZ r10,r3**——CLZ 在 S=0 时
占用 CMN 的 opcode 表行，靠 Rn=1111、bit11-4=11110001 区分。B9d 的识别
掩码 `0x0FFFFFF0` 没有把 Rd（bit15-12）留成可变位，只有 Rd=0 的 CLZ 能
命中；Rd=10 时掉进数据运算分支，r10 不更新、标志被 CMN 误写，r3 随后被
错误移位清 0，第二次进入该函数时从表尾外取到 EA000988 并线性空扫。

**修复**：掩码改为 `0x0FFF0FF0`，固定位只保留 bit27-20、bit19-16、
bit11-4；`[case 21-B9d]` 补非零 Rd 回归（E16FAF13），全量 **795 项
检查 0 失败**。

**效果**：修复后同一调用点 r10=1，标题位流解码能继续执行到
0x01FF8524/8528、0x01FF86A4 等位流复制循环（往 0x021Bxxxx 写填充字节），
不再跳 EA000988。长跑终点 ARM9 在 0x01FF84xx/0x020119xx 间活动；ARM7
后期仍在未映射主存区跑飞，下一对照点是 ARM7 在标题段时间轴上的任务恢复。

### 21-B9wi（2026-09-06 代码/测试）：ARM7 SWI 0x1A-0x1D，标题时间轴不再跑飞

**证据**：ARM7 逐指令日志抓到第一个稳定跑飞点——ARM7 在 4,478,972 周期
执行 0x037FF484 的 `BX r12` 切到 0x0380443C Thumb，紧接着 `swi 0x1C`
（DF1C）。本地 bios_dispatch 未登记 0x1C，把它当 unknown SWI 送进
0x00000008 异常向量；低地址 BIOS 字节未实现，CPU 就以 SVC+Thumb 状态在
全零区从 0x00000008 逐 2 字节漂移，直到越过 0x4000 进入 0x00xxxxxx/0x1Bxxxxxx。
FreeBIOS（melonDS 默认参考 BIOS）的 SWI 表第 0x1C 项是 ARM7 专用
GetVolumeTable——它在 `adr r1,volume_table` 后用 `ldrb r0,[r1,r0]` 返回
音量折线字节，调用参数 r0=0x2A0 时返回 0x47。

**做了什么**：

- 从 FreeBIOS `bios_common.s` 抽取正弦/音高/音量表（64 半字/768 半字/
  724 字节）为 `bios_audio_tables.h`；
- bios_snd 实现 SWI 0x1A GetSineTable、0x1B GetPitchTable、
  0x1C GetVolumeTable 与 0x1D GetBootProcs（r0=0xA2E/r1=0x2C3C/
  r2=0x5FF），bios 分发只在 ARM7 核放行；
- runner 事件驱动 headless 结束处增加帧号/VRAM 非零统计，并支持
  `--screenshot`（原先 cycles 模式忽略截图参数）。

**验证**：新增 `[case 21-B9wi]` 10 项，全量 **805 项检查 0 失败**。
事件驱动 1.5 亿步终点 ARM7=0x1158、无跑飞；400M/900M 步终点仍是
0x1158，ARM9 持续在 0x01FF84xx/0x020119xx 标题与 service 代码间活动。

**当前画面状态**：900M 步摘要 `frame=1126 vram-nz=393208`，与参考
frame1310 的 `vram-nz=393099` 只差 109 字节，DISPCNT=00121F10 与参考
标题态一致；本地截图底屏已有约 2159 色画面，顶屏仍全黑——下一对照点是
顶屏 Engine A 在该标题视频模式下的渲染/映射。

### 21-B9wj（2026-09-06 代码/测试）：DISPCNT VRAM 直显模式，顶屏出现画面

**证据**：参考核 `GPU_Soft::DrawScanlineA` 对 DISPCNT bit16-17 分四种显示
模式；FFXII 标题顶屏 `DISPCNT=00121F10` 的 bit16-17=2，是 **VRAM 显示模式**
——像素直接来自 bit18-19 选定的物理 VRAM bank，而不是 2D 图层合成。本地旧
`render_engine` 只处理“0=关、其余=常规 2D”，所以顶屏没有图层可画，一直黑。

**修复**：新增 `bus_vram_phys16`（物理 bank 直读，带边界检查）；
`render_engine` 在主引擎 `dmode==2` 时按选定 bank 展开 RGB555 帧，副引擎按
硬件实现只接受常规显示。新增 `[case 21-B9wj]` 3 项，全量 **808 项检查
0 失败**。

**效果**：本地 frame1306 顶屏从 49,152 像素全黑变为 2,912 色画面（VRAM 直显
bank A），底屏保持 1,958 色；帧号/VRAM 非零量仍与参考标题段同量级
（393,211 vs 393,099）。**遗留**：本地 bank A 图像与参考 frame1310 快照的
bank A 仍逐字节不同（主存 0x19F100-0x333E67 大段解码缓冲也有差异），说明
标题位流解码输出仍不是参考图像，下一步继续做参考/本地逐指令对照。

### 21-B9wk（2026-09-06 代码/测试）：ARMv5 DSP 乘法补齐，解码函数逐条对齐

**证据**：参考/本地同窗口逐指令对照（全寄存器）在 0x01FFD904 发现首个
分歧：`E1690584` 是 **SMULBB r9,r4,r5**（ARMv5 DSP 乘法），参考 r9 变
0xFFFFE740；本地把它当 row 0x16 的普通数据运算，r9 不变，后续循环边界
与输出缓冲随之跑偏。

**修复**：`exec.c` 新增 `exec_dsp_mul`，覆盖 SMLAxy/SMLAWy/SMLALxy/
SMULxy/SMULWy（半字选择、Q 标志、64 位累加），识别掩码
`(insn & 0x0F900090) == 0x01000080`；ARM7 保持未定义语义。新增
`[case 21-B9wk]` 8+2 项，全量 **818 项检查 0 失败**；修复后同一解码段
**42,899 条指令**与参考寄存器逐条一致。

**里程碑候选状态（待人工验收）**：

- `build\test_nds.exe`：**818 项检查 0 失败**；
- `build\nds-emu.exe "<ROM>" --headless-cycles 1103000000 --screenshot`：
  终点 frame=1306、ARM9 PC=01FFDA28、ARM7 PC=00001158、
  VRAM 非零 393,208 字节、主 DISPCNT=00121F10、副=00010435；
  本地 ARM9 时间戳 2×tm.now=1,463,914,014，与参考 frame1305 的
  1,463,216,281 仅差约 0.05%；
- 截图 `headless_shot.bmp`（本步修正了上下半屏顺序：上半=主引擎 A，
  下半=副引擎 B）。本地两屏各有数千种颜色、无跑飞、无黑屏；
- 与参考 frame1310 快照的逐字节 bank 对比仍不同（参考 A/B bank 是
  LCDC 双缓冲直显数据，本地 bank 结构相关度低），因此“画面是否就是
  标题”需要人工看图确认。验收步骤见下。

**人工验收步骤**：

1. 打开 `build\test_nds.exe` 输出的末尾，确认 `818 项检查，0 项失败`；
   或直接运行 `build\test_nds.exe`。
2. 运行
   `build\nds-emu.exe "tools\rom_ascii.nds" --headless-cycles 1103000000 --screenshot`，
   确认末尾 `summary frame=1306 ... vram-nz=393208 disp=00121F10`。
3. 打开生成的 `headless_shot.bmp`（上半=主引擎、下半=副引擎），
   对照参考快照 `%TEMP%\ref_fb_1310.bin` 导出的画面，判断是否已能
   辨认出《最终幻想12 亡灵之翼》标题/标题动画画面。
4. 若画面不是标题（例如仍是随机纹理、渐变或加载画面），请说明看到的
   主要颜色/图形，后续继续按逐指令对照追画面合成/VRAM 写入路径。

### 21-B9wl（2026-09-06 里程碑）：hard_title 达成候选——本地渲染出标题

**怎么找到的**：对本地逐帧 VRAM bank 图与参考 framebuffer 做 OCR/结构对照：

- frame1000-1475：本地画面是**开场制作人员表**，OCR 稳定读出
  `MOVIE DIRECTOR EIJI FUJII`、`GRAPHIC DESIGNER SHO OKAMURA`、
  `(Basiscape Co., Ltd.) KENICHIRO`、`HITOSHI WATANABE / EISUKE YOKOYAMA`
  等字幕——说明 2D 渲染、VRAM 写入、字体/纹理链路都在正常工作；
- frame1700-1875：本地 bank A/B 图上 OCR 读出 `FINAL FANTASY …` 标题字样；
- 参考核 frame100-900 同样经历 SQUARE ENIX / THINK&FEEL / Ivalice
  Alliance / IMAGINE codec / 制作人员表，frame1000 之后进入无 OCR 文字的
  画面（标题/场景）。

**结论**：本地已经能持续运行并在 frame≈1700-1875 渲染出标题画面，
**hard_title 达成候选**。启动后固定跑
`--headless-cycles 1650000000 --screenshot`（约 frame1790），模拟器自身
渲染的 Engine A 画面 OCR 可读出 `FINIL III`（“FINAL FANTASY”花体字被
OCR 误读），Engine B 为标题配套画面。

**已知偏差**：本地开场/字幕时间线比参考晚约 500 帧（参考 frame1000 后
已离开字幕，本地 frame1500 仍在滚字幕、frame1700 才出标题）。这属于
周期成本/事件节奏尚未逐项校准的问题，不影响“进入标题”这一里程碑；
后续对齐 gameplay 时序时继续处理。

**验收步骤**：

1. `build\test_nds.exe` → 末尾 `818 项检查，0 项失败`；
2. `build\nds-emu.exe "tools\rom_ascii.nds" --headless-cycles 1650000000 --screenshot`
   → 末尾 `frame=1790`、ARM7 PC=00001158、`disp=00121F10`；
3. 打开 `headless_shot.bmp`（上半=主引擎 A）或
   `build\title2_engineA_main_x2.png`，确认是《最终幻想12 亡灵之翼》的
   标题 logo/标题画面；
4. 对照参考 `%TEMP%\ref_fb_1310.bin`（或其导出 PNG）确认同为标题段画面。

### 21-B9wm（2026-09-06 验证）：标题后可交互，START 能推进流程

在无头事件驱动里临时加脚本按键（`NDS_KEY_FRAME/NDS_KEY_MASK`，验证后已从
代码移除）：frame1950 按住 START 10 帧后释放，运行继续到 frame5580：

- ARM9/ARM7 均保持有效代码区（ARM7 仍停在 0x1158），没有跑飞；
- DISPCNT 从标题段的 `00121F10` 切到 `00161F10`，VRAM 内容随场景更新；
- 画面从标题 logo 切到深红/棕场景（Engine A 602 色），随后进入等待/过场
  状态，说明 **KEYINPUT 输入路径在真实 ROM 上可用**，标题不是死画面。

后续若继续推进“正常游玩”，需要在输入/菜单/时序（本地开场比参考慢约
500 帧）上继续校准；本轮里程碑只验收 hard_title 与基本可交互性。

### 21-B9wn（2026-09-06 工具）：无头脚本按键 + IRQ 计数

`--headless-cycles` 新增三个可选参数，便于自动验收后续交互：

- `--key-frame N`：从第 N 帧起注入按键；
- `--key-mask M`：键位掩码（`0x8` = START，见 `src/io/key.h`）；
- `--key-period P`：每 P 帧再按一次（0=只按一次）。

结束摘要新增两核中断计数。实测 4 亿步、frame684：ARM9≈3680 次、ARM7≈3514
次中断，说明 VBlank/服务中断没有整帧缺失；开场字幕比参考慢的原因是更深层的
周期成本/CPU 吞吐差异，后续单独校准。

### 21-B9wo（2026-09-06 代码/测试）：定时器按经过周期推进

**证据**：参考核在 frame600-1400 的字幕段 TM0/TM1 均为 `CNT_H=00C1`
（使能 + 溢出 IRQ + 1:64 分频），计数器随系统周期持续增长；本地旧模型把
`timer_advance()` 绑在 `cpu_step` 上、每次只 +1，导致等待/跳帧期间定时器
冻结，字幕与过场节奏偏慢。

**修复**：`timer_advance(t, cycles)` 与 `io_advance_timers(io, core, cycles)`
改为按周期推进（while 补足分频计数）；`cpu_step` 传 `step_cycles`；事件
headless 在单核/双核等待造成时间跳跃时，给等待核补上同样的周期增量。

**验证**：全量 **818 项检查 0 失败**（原有定时器用例期望值不变）。真 ROM
同一步数（13 亿步）下字幕页从 frame≈1475 提前到 frame≈1380（约 5-6%），
ARM7 仍稳定 0x1158。剩余的字幕/过场偏慢来自尚未完成的逐类指令多周期成本
模型，后续继续。

### 21-B9wp（2026-09-06 验证）：定时器修正后标题复测

重新运行 `--headless-cycles 1650000000 --screenshot`：终点 frame=1683、
ARM7 PC=00001158、VRAM 非零 389114、DISPCNT=00161F10；Engine A 画面 OCR
仍读出 `FINAL FANTASY …`（`fl £ ! E …`，花体字误读），Engine B 为配套画面。
导出图：`build/title3_A_x2.png`、`build/title3_B_x2.png`。里程碑结论不变：
hard_title 可达，输入 START 可离开标题；下一步继续做 gameplay/时序校准。

### 21-B9wq（2026-09-06 代码/测试）：帧驱动 runner 接入窗口模式

**问题**：窗口模式此前每帧只跑固定 8 条 CPU 指令再手动置 VBlank，游戏逻辑
每帧只有个位数指令，实际无法正常游玩；headless 的事件驱动循环才是正确的
周期/等待模型，但只存在于诊断入口。

**修复**：

- 新增持久化 `runner_t`：封装 timing 事件表（扫描线/VBlank）、ARM9/ARM7
  周期成本、Halt/WFI 唤醒、等待期定时器补偿与脚本按键；窗口模式与
  `--headless-frames N` 共用同一调度器；
- `main.c` 主循环改为每帧 `runner_run_frame()`，不再手动 `io_set_vblank`；
- 本工程目标加 `-O2`：300 帧从约 5.5s 降到约 2.9s；1700 帧（标题段）
  约 93s，等价吞吐约 18fps（标题解码段最重），达到可交互量级。

**验收命令（推荐）**：

```
build\nds-emu.exe "tools\rom_ascii.nds" --headless-frames 1700 --screenshot
```

期望输出：`frame=1700 ... vram-nz=389114 disp=00161F10`，截图上半为主引擎
标题画面（OCR 可读出 FINAL FANTASY 花体字）。直接运行不带
`--headless-frames` 即进入窗口模式，用模拟器主循环逐帧运行同一模型。

### 21-B9wr（2026-09-12 代码/测试）：KEYCNT 按键中断 + NDS7 卡带 DMA 触发

**证据（卡在 ROMCTRL bit31 的那次长跑）**：ARM9 在 0x02011C54 轮询
`ROMCTRL(0x040001A4)` bit31 等卡带传输结束，而卡带日志显示每帧只有一次
「搬 1 个字」的 DMA：`arm7=1 ch=3 n=1 cnt_h=AF00 sad=04100010`。`0xAF00`
按 NDS7 的定义是 **bit13-12=10b**，即 melonDS `CheckDMAs(1,0x12)` 的
DS 卡带触发；本地旧 `dma_fire` 只按 ARM9 的模式号 5 匹配，ARM7 的卡带 DMA
从不触发，于是 512 字节的块只被 CPU 轮询搬走 4 字节就停住。

**修复**：

- `io/dma`：新增 `dma_fire_card(dma,bus,is_arm7)`，ARM9 用模式 5、ARM7 用
  `((cnt>>12)&3)|0x10 == 0x12`；`io_card_dma_check` 改调它（两核各查各的）；
- `io/key`：补 KEYCNT（0x04000132，两核各一份）+ 按键中断 IF bit12
  （OR/AND 两种匹配，边沿触发），标题/菜单的按键唤醒依赖它；
- `runner`：脚本按键的保持单位从“调度迭代”改为“帧”（8 帧），
  `--headless-frames` 每 100 帧打印一行进度（帧号 + 两核 PC + DISPCNT）。

**验证**：新增 `[case 21-B9wr]` 8 项 + KEYCNT 4 项，全量 **830 项检查
0 失败**。真 ROM 长跑
`--headless-frames 2600 --key-frame 1700 --key-mask 0x8`：

- frame1700 仍是标题（`disp=00161F10`、ARM9=02085604）；
- frame1900 起 START 生效后 ARM9 落到空闲任务 `0200957C`（`cpsr=0000001F`
  System 模式），ARM7 稳定停在 BIOS Halt `00001158`（`cpsr=8000001F`），
  两台核都没有跑飞，`irq9=14215 / irq7=32612`；
- **不再**出现“ARM9 死等 `ROMCTRL(0x040001A4)` bit31、512 字节块只搬 4 字节”
  的卡带 DMA 停摆；下一个 gap 是 ARM9 空闲任务等不到 ARM7 调度器派发的任务。

**验收步骤**：

1. `build\test_nds.exe` → 末尾 `830 项检查，0 项失败`；
2. `build\nds-emu.exe "tools\rom_ascii.nds" --headless-frames 2600 --screenshot`
   → 进度行里 ARM9 不再长时间停在 `02011C54`；`frame=2600` 结束时摘要为
   `vram-nz≈103264 disp=80111418`、ARM9=0200957C、ARM7=00001158。

### 21-B9ws（2026-09-12 代码/测试）：ARM7 FreeBIOS 低地址 Halt/调度路径参考级 HLE

**这一步做了什么**：把 ARM7 的 SWI/IRQ 进出 BIOS 低地址从模拟器私有桩换成
与参考核（melonDS + FreeBIOS）逐地址等价的 HLE：

- 新增 `src/bios/bios7_low.{h,c}`：按 FreeBIOS ARM7 镜像反汇编出的地址逐条
  等价执行 SWI 分发器（0x1080：SVC 栈 4 字 + 切 System + System lr 入栈 +
  0x10B0 表跳转）、`swi_complete`（0x112C）、Halt（0x114C）、WaitByLoop
  （0x115C）、interrupt_check（0x1168）、IntrWait/VBlankIntrWait（0x1190/0x1188）、
  Stop（0x11B8）、CustomHaltPost（0x1FA4）、SoftReset（0x1FD8）、
  IRQ 入口/返回（0x1FB0/0x1FC0/0x1FC4）；
- 新增 `src/bios/bios7_image.{h,c}`：向量表 0x00-0x1F 与 SWI 表 0x10B0-0x112B
  的可读字节影子（游戏恢复「PC 停在 SWI 向量」的现场时，分发器要从 BIOS 取编号）；
- `cpu_step`：ARM7 HALTCNT 暂停（`(IF&IE)` 唤醒）→ 定时器/卡带推进 → IRQ 检查 →
  `bios7_low_step()` → 常规取指；异常入口低 8 位改成参考口径（SWI 0x93、
  未定义 0x9B、IRQ 0xD2，**一律清 T**）；
- `bus_resolve`：ARM7 视角 0x03800000-0x03FFFFFF 按 64KB 镜像（0x03FFFFFC 与
  0x0380FFFC 同字节）；`power` 增加 HALTCNT；`cpu_direct_boot()` 按
  melonDS `SetupDirectBoot` 设置两核 sp/sp_irq/sp_svc。

**参考核对照（本步的关键证据）**：给参考核 ARM7 加低地址事件钩子跑 6000 帧：

- SWI 入口 `pc=00000008 cpsr=60000093 lr=038043CA sp=0380FFC0`（I=1、T 已清）；
- Halt 函数体 `0x114C cpsr=8000001F` 出现 **542 次**；
- IRQ 596 次进 `0x18→0x1FB0→0x1FBC`，其中 **543 次 lr=0x115C**——即打断点就是
  BIOS Halt 的暂停地址 0x1158，不是调用方代码；
- WaitByLoop 每轮 3 周期（subs 1 + bgt 2）。

本地实现后同一批观测完全对应（首个 ARM7 IRQ 就是 `pc=0000115C cpsr=2000001F`）。

**验证**：`build\test_nds.exe` **860 项检查 0 失败**（新增/改写 30 项 ARM7
用例）；真 ROM 900 帧：`f=900 ARM7=00001158 cpsr=8000001F`（与参考 Halt 体一致）、
标题段 `f=300 disp=00161F10` 不回归；2600 帧（1900 帧起按 START）终点
`ARM9=0200957C`、`ARM7=00001158`、`vram-nz=103264`，与 B9wr 基线一致。

**验收内容**：

1. `build\test_nds.exe` → 末尾 `=== 共 860 项检查，0 项失败 ===`；
2. `build\nds-emu.exe "tools\rom_ascii.nds" --headless-frames 900` →
   进度行出现 `ARM7=00001158`，结束摘要 `ARM7 PC=00001158 cpsr=8000001F`；
   日志开头有 `irq: first arm7 IRQ pc=0000115C cpsr=2000001F ... slot+3FFC=037FB8F4`
   （被打断点是 BIOS Halt 内部，与参考核 543 次 lr=0x115C 同形）；
3. `build\nds-emu.exe "tools\rom_ascii.nds" --headless-frames 300 --screenshot`
   → `disp=00161F10`，截图仍能看出标题画面（本步不改显示链路，作回归用）。

**已知偏差（有意保留）**：SWI 0x1F 按真机 BIOS 口径接 CustomHaltPost；
低地址只影射向量表/SWI 表，其余读 0；ARM9 的 SWI 仍是阶段 11/12 的直接 HLE。

### 21-B9wu（2026-09-12 代码/测试）：DMA 归属修复 + 无头 SPU 推进 + 现场导出

**背景**：B9ws 之后游戏能到标题、START 能离开标题，但随后 ARM9 停在空闲任务
`0x0200957C`（WFI）不再出图。这一步先补齐诊断设施，再用它们定位。

**新增诊断**：

- `--dump <前缀>`：导出 `mainram/arm7wram/sharedwram/vram/itcm/dtcm`。
  与参考 harness 的 `ref_fNNN_*.bin` 对照得到：**ITCM 逐字节一致**（0 差异）、
  DTCM 只有 38 字节差（都是栈帧/计数器）、ARM7 WRAM 922 字节差（队列指针/计数器）。
- 进度行补 `if9/if7`、IPC `cnt`、FIFO 深度与 GX 计数；参考核一侧同时加了
  ARM7/ARM9 指令计数与低地址事件钩子（每帧 ins7≈29.4K、ins9≈400K）。
- 结论性观察：两核 IPC 报文序列完全一致（`C0240006/46/86/C6` 各 1 条/帧，
  同样的发送 PC `0x037FEA78`），FIFO 也都排空；分歧在「ARM9 收到消息后做什么」。

**两处真实修复**：

1. **DMA 搬运期间按属主核分流 IO**：`dma_transfer()` 没有设置
   `bus->active_is_arm7`，沿用了上一步 CPU 的值。于是 ARM9 的显示列表 DMA 写
   `0x04000400` 时被当成「ARM7 视角 = 音频通道寄存器」，GX 收到的是空命令流
   （实测 GX FIFO 通路 256 次写入全是 0、端口通路 2235 条命令里**没有一条顶点**）。
   修复后 DMA 前后切换并恢复访问者身份，完成中断也按属主核写 IF。
2. **无头模式补推 SPU**：ARM7 热点在 `0x037FEFD0` 轮询 `0x04000403+idx*16`
   （声音通道状态位）。窗口模式靠 SDL 回调推进混音器，无头模式没人推进，
   通道永远停不完。新增 `snd_advance()` 与 `snd_host_render_active()`，
   runner 每帧按 `tm.now/1024`（32768Hz = 33.51MHz÷1024）补推。

**验证**：`build\test_nds.exe` **865 项检查 0 失败**（新增 5 项：DMA 归属 3 项、
无头 SPU 2 项）。真 ROM 复测：标题（frame300 `disp=00161F10`）不回归；
3D 段 `disp=80111418`、`disp3dcnt=0x3011` 时 GX 统计可见
`cmd/fifo-wr/port-wr` 增长，便于继续追「为什么只发设置命令不发顶点」。

**当前卡点（下一步）**：ARM9 在 `0x0200957C` 的 WFI 空闲循环里循环（每帧仍有
约 700 条指令的显示列表构建 + 缓存清洗），3D 帧缓冲 `fb-nz=0`、无顶点命令，
说明游戏在等一份还没就绪的数据（场景/任务）。下一步按「ARM9 收到的 IPC 报文 →
它设置的任务位图/队列」继续对照参考核。

### 21-B9wv（2026-09-12 代码/测试）：KEYINPUT 复位值修正（全松开）

**怎么找到的**：给 `--dump` 加上两核视角的 IO 寄存器快照（0x04000000-0x04000FFF），
与参考 harness 的 `ref_fNNN_io9.bin` 逐字对照：

| 寄存器 | 本地（frame1900） | 参考（frame1800） | 说明 |
|--------|------------------|-------------------|------|
| `0x04000130` KEYINPUT | `00000000` | `000003FF` | **本地被读成“所有键按住”** |
| `0x04000060` DISP3DCNT | `00003011` | `00000011` | 本地 3D 层使能更早 |
| `0x04000600` GXSTAT | `84000000` | `86000000` | FIFO 状态位不同（bit25/26） |
| `0x040000D4-DC` DMA3 | `0` | 有活动 DMA | 参考在做 VRAM/LCDC 搬运 |

根因：`keypad_t` 由 calloc 得到 0，而 KEYINPUT 的“按下=0”约定下 0 表示**所有键
都按着**——游戏从开机起就看到 START/A 等键一直按住（这就是此前“不按键也会自己
离开标题”的原因）。

**修复**：`key_reset()`（= `key_set_pressed(k, 0)`）在 `io_create()` 里调用，
KEYINPUT 默认 `0xFFFF`（低 12 位全 1 = 全松开）。

**效果**：真 ROM 时间线整体后移约 200 帧（标题/制作人员表按正常时长走完），
ARM9 在 frame1800-2000 落到**与参考一致的卡带读循环** `0x020119xx`
（参考同窗口热点正是 `0x0201191C/0x0201193C`）。之后仍在 frame≈2100 进入
`0x0200957C` 空闲循环——下一个 gap 见上。

**验收内容 / 步骤**：

1. `build\test_nds.exe` → `=== 共 866 项检查，0 项失败 ===`；
2. `build\nds-emu.exe "tools\rom_ascii.nds" --headless-frames 2600` →
   `if9=...` 一行的 `ARM9=` 在 frame1700-2000 之间应出现 `0201 19xx`/`020856xx`
   （卡带读/服务代码），frame2100 之后才落到 `0200957C`（空闲）；
3. `build\nds-emu.exe "tools\rom_ascii.nds" --headless-frames 300 --screenshot`
   → 标题/制作人员表不回归（`disp=00161F10`）。

### 21-B9ww/B9wx（2026-09-12 代码/测试）：GX FIFO DMA 与 NDS RTC

继续追「ARM9 进空闲循环、3D 段只有设置命令没有顶点」：

1. **ARM9 DMA 模式 7 = GX 命令 FIFO**（`DMA_START_GXFIFO`）：对照参考核
   `DMA::WriteCnt`，`StartMode==0x07` 时 melonDS 走 `GPU3D.CheckFIFODMA()`；
   IO 快照里参考的 ARM9 DMA3 正是
   `SAD=0238BB84 → DAD=04000400`（GX FIFO）`CNT=7C400185`。本地旧实现只认
   立即模式，模式 7 永不触发 → GX 收不到显示列表。现在同步整体搬运，
   单测 `[case 21-B9ww]`。
2. **NDS RTC**（0x04000134 RCnt / 0x04000138 串行数据）：游戏对该区读取
   2300 帧内高达 **112,610 次**（≈49 次/帧），本地此前把这两个寄存器当未映射
   返回 0，RTC 事务永远拿不到数据。新增 `src/io/rtc.{h,c}`（3 线协议 +
   状态寄存器/日期时间/闹钟寄存器），runner 按经过时间走时；单测
   `[case 21-B9wx]` 6 项。

**验证**：`build\test_nds.exe` **875 项检查 0 失败**。真 ROM 复测：
RTC 读取事务能正常结束（读数从 112,610 降到 85,488，说明游戏不再空转在同一轮询里），
标题/制作人员表时间线（frame300 `disp=00161F10`）不回归。

**当前卡点（下一步）**：本地 frame≈2100 之后 ARM9 仍进 `0x0200957C` 空闲，
3D 帧缓冲 `fb-nz=0`、无顶点命令、无 mode-7 DMA——说明游戏仍在等场景数据任务；
参考核在同一阶段已经在跑 `0x020119xx` 卡带读 + GX 显示列表 DMA。下一步继续沿
「ARM9 收到的 IPC 报文 → 任务位图 0x027FFFB0 → 卡带任务」对照参考核。

### 21-B9wy（2026-09-12 代码/测试）：GXSTAT FIFO 状态位 + 吞吐对照

- **GXSTAT**：参考核 `GPU3D::Read32(0x04000600)` 在 FIFO 空时同时置 bit25
  （不足半满）与 bit26（空），本地只有 bit26（`0x84000000` vs 参考
  `0x86000000`）。已补齐，单测 `[case 21-B9wy]` 4 项。
- **吞吐对照（新发现，记入待办）**：给参考核加指令计数后测得
  参考 ARM9 约 **40 万条指令/帧**、ARM7 约 2.9 万条/帧；本地同阶段 ARM9 约
  **95 万条/帧**（ARM7 3.3 万/帧）。原因是本地成本模型给 ARM9 每指令 1 周期、
  等价 IPC=1（参考实测约 0.36）。做过一次「把 ARM9 预算砍半」的试验，游戏
  反而在开局就停住（时间推进相对游戏工作过快），因此**未**采用；正确做法是
  给 ARM9 加访存等待周期模型（待办，见下）。
- **热点集合一致（重要）**：本地整段运行（0-2100 帧，ARM9 共 17.6 亿条指令）
  的前几名热点 `0208561C 14.3%`、`0208560C 9.0%`、`0201193C/1C/20`（卡带读）、
  `01FFD7BC`/`01FF95xx`/`01FFDDxx`（ITCM 调度器）与参考核同窗口的热点**完全同族**
  ——说明两边代码路径一致，分歧在时序/外设状态与“每帧完成的游戏工作量”。
- 同一批对照还确认：本地与参考的**解压后运行时代码逐字节一致**
  （0x02085600 起 1KB 内 0 差异），即分歧不在代码而在时序/外设状态。
- **hard_title 复测（像素级）**：本轮修好 KEYINPUT/RTC/DMA/GXSTAT 之后，标题
  从 frame≈1700 后移到 **frame≈1900**（按键不再“一直按住”，制作人员表按正常
  时长走完）。把 frame1900 的截图与历史验收图 `build\title3_A_x2.png` 逐像素
  比对：**12288 个采样点 0 差异**，即仍是同一张已人工验收过的标题画面。

**当前卡点（更新）**：frame≈2100 后 ARM9 进 `0x0200957C` 空闲、3D 段无顶点命令；
参考核同阶段已在跑 `0x020119xx` 卡带读 + GX 显示列表 DMA + 每帧 4 条
`0000C187` 报文。下一步按「ARM9 服务队列指针（IPC 报文里的 0x81xxxxxx 令牌）→
ARM7 队列」继续对照，或先补 ARM9 访存等待周期模型让两核吞吐对齐。

### 21-B9xa（2026-09-12 代码/测试）：IME 掩码 + 卡点期的外设状态对照

把两核视角的 IO 快照（`--dump` 的 `_io9/_io7`）与参考核逐字节对照后确认：

| 项 | 本地 | 参考 | 结论 |
|----|------|------|------|
| IPCFIFOCNT（两核） | `8501` | `8501` | 一致 |
| IE7 | `0104009D` | `0104009D` | 一致 |
| IE9 | `00042009` | `0004200B/19` | 游戏按阶段改写，非固定差异 |
| IME9/IME7 | `04000001` | `00000001` | 本地高位垃圾 → 已修成只保留 bit0 |
| SOUNDCNT | `807F` | `807F` | 主输出开 |
| 声音通道 CNT | 停在 `8800025B` 等旧值 | 每帧改写 | 参考的 ARM7 声音驱动每帧重触发通道 |

新增诊断 `io: snd-cnt-writes=N`：统计 ARM7 写 SOUNDxCNT 高字节（含 start 位）
的次数，用来判断声音驱动是否在跑（2300 帧内 1005 次 → 驱动有活动）。

**验证**：全量 **879 项检查 0 失败**（IME 掩码不改变功能，仅让 dump 与参考一致）。

### 21-B9xb（2026-09-12 分析）：服务队列条目对照——分歧定到 ARM9 的“任务内容”

进度行补 `sp9/lr9` 后发现：卡住时 ARM9 停在 `0x0200957C`（WFI），
`lr9=0x0200761C` —— 它处在 `0x0200760C` 的“关中断 + 无限 WFI”例程里
（`bl 0x0200926C` → `loop: bl 0x02009578(WFI); b loop`），即 ARM9 主动进入
“等外部给活”的深睡，不是跑飞 ✓。

把两边 IPC 令牌解包（`token >> 6` = 队列条目指针）并读队列内容：

| | 参考（frame2200） | 本地（frame2150） |
|---|---|---|
| 令牌 | `81DFB007`→`02077EC0`、`81DFCE07`→`02077F38`、`81DFEC07`→`02077FB0` | `81DD9A07`→`02077668`、`81DDA007`→`02077680` … |
| 条目[+0x00] | 下一条指针（+0x18 递增） | 同结构 ✓ |
| 条目[+0x04] | `0000001A`（任务号 26） | `00000017`（任务号 23） |
| 条目[+0x08] | **`00003000`（0x3000 = 12KB）** | `0000007F/007B/0077/0063`（每帧 −4 的小值） |

即：**队列结构与步长（24 字节）两边一致，但本地 ARM9 提交的任务内容不同**
——参考在提交“读 0x3000 字节”的加载任务，本地在提交任务号 23、参数为
0x7F/0x7B… 的小步任务。下一步沿这条线索查“为什么本地 ARM9 走的是另一条
文件/流任务分支”（优先怀疑它读到的某个文件表项/长度与参考不同）。
它设置的任务位图/队列」继续对照参考核。

### 21-B9xc（2026-09-12 代码/测试）：VBlank 时序对齐真机（第 192 行起）

**证据**：本地帧边界的 IF 一直是 `if9=00280005 / if7=00080001`（VBlank 位还挂着），
参考核同一阶段是 `if9=00280000 / if7=00000000` —— 参考的 VBlank 在帧内就被处理了。
查 runner 发现 `runner_ev_frame`（帧边界）才调 `io_set_vblank`，而真机一帧 263 行、
**第 192-262 行才是 VBlank**，本地把 VBlank 推迟了约 27% 帧长。

**修复**：`io_set_vblank` 只负责「置两核 IF bit0 + 主副 DISPSTAT bit0 + 触发
VBlank DMA」；新增 `io_frame_boundary()` 在帧边界把 VCOUNT 归零并清 VBlank 标志；
`runner_ev_line` 在 `vcount == 192` 时触发 VBlank。

**效果与验证**：

- IF 模式与参考一致（VBlank 帧内被消费，`if9=00280004`）；
- **标题画面重新截图比对**：frame1900 与已验收图 `build\title3_A_x2.png`
  **12288 采样点 0 差异**（里程碑保持）；
- 全量 **885 项检查 0 失败**（新增 `[case 21-B9xc]` 6 项）。

**卡点未变**：frame≈2100 后 ARM9 仍进 `0x0200760C` 深睡例程、3D 段无顶点。
下一步仍按 B9xb 的线索（队列任务内容 0x17 vs 0x1A+0x3000）追。

### 21-B9xd（2026-09-12 分析）：两核吞吐差被解释清楚 + 深睡例程的调用来源

**“本地 ARM9 指令数是参考 2.2 倍”不是缺陷**：把帧延迟循环反汇编清楚后可以确认
（`0x020855F8` 起）：

```
0208560C  ldr r2,[r1,#0x18]   ; 等帧计数器非 0
0208561C  sub r0,r0,#1        ; ← 热点：r0 与计数器同时递减
02085620  ldr r2,[r1,#0x18]
02085624  sub r2,r2,#1
02085628  str r2,[r1,#0x18]
02085634  bls 0x02085660      ; 计数器 ≤ r0 才退出（靠 ISR 每帧把它加回来）
```

即这是一个**空转等待**：每帧 ISR 把计数器推进 1，循环本体纯烧指令。参考核
（带访存等待）每秒烧得慢、本地烧得快，于是同一帧数下本地指令数天然多 2 倍，
但*退出所需帧数一致* —— 之前记录的“2.4 倍吞吐差”因此可以排除。

**深睡例程的来源**：卡住时读 ARM9 栈得到调用链 ——
`0x02007A3C`（关中断 → 取 `[0x02076F18]` 当参数）→ `0x020079CC`（队列处理）
→ `0x0200760C`（关中断 + `loop: WFI`）。也就是说：**游戏的队列处理函数自己
决定“没活了，停机”**，不是跑飞。下一步就查 `0x020079CC` 里那条分支条件
（为什么本地判成“无活可做”而参考核判成“有活”）。

**任务块指针的差异（B9xd 的后续证据）**：`0x020079CC` 的判据是
`[0x02076EFC]`（两边都是 0，无差异），真正的差别在它传入的**当前任务指针**
`[0x02076F18]`：

| | 本地 frame2300 | 参考 frame2600 |
|---|---|---|
| `[0x02076F18]` | `02076F24`（指向结构内部，内容是 `0000001F 00000000 0000001F …`） | `02076FE4` |
| 该指针处内容 | `0000001F 00000000 0000001F 00000000 …` | `8000009F 00000001 02076FE8 8000009F 03FF7620 02076F14 …` |

参考的指针指向一个“保存的现场块”（首个字 `0x8000009F` 像 CPSR：System 模式 +
IRQ 关；后面跟着 PC/栈指针），本地的指针却指向那张只含 `0x1F` 的静态表内部。
`0x020076EF0-0x020076F20` 其余字段两边逐字相同，说明**差别就集中在“当前任务
块”的挂接/切换上** —— 下一步顺着 `0x020079CC` 的两个分支
（`0x02007A20` → `0x02007988`：空闲/停机；`0x020079EC` → 正常派活）追。

**21-B9xe 更正（继续追证）**：

1. `0x0200760C` **不是死机例程**：它先 `bl 0x0200926C`（`bic r1,r0,#0x80` =
   **打开 IRQ**），再 `loop: bl WFI` —— 是**正常的空闲等待**，每帧照样被中断叫醒
   （`irq9` 持续增长）。所以「ARM9 停机」的说法要修正为「ARM9 空闲、等队列里有活」。
2. `0x02007988`（真正的“派活”函数）逻辑：取 `[0x02076F0C] → [0x02076F18]`
   当任务块，读 `task+0xB4` 的**待执行回调**；非 0 就清掉并 `blx` 调用它，然后走
   `0x02007928`。**两边这个回调字段此刻都是 0** —— 即“队列里确实没活”。
3. `[0x02076F18]` 在两边都是**两个任务块之间来回切换**（ITCM `0x01FF8120` 写
   `0x02076FE4`、`0x020077D4` 写 `0x02076F24`，本步实测 12168/7820 次交替），
   因此“指针值不同”只是采样时刻不同，**不是分歧点**。

结论：分歧在**ARM7 侧没有产出工作**（它每帧照常跑声音驱动 16 通道音量写入 +
4 条心跳报文，但没有 `0000C187` 这类文件/任务报文），ARM9 因此一直空闲。
下一步从 ARM7 的“任务产出”入口查（`0x037FEA78` 发送例程的调用者、
`0x037FE3xx` 队列状态）。

### 21-B9xf（2026-09-12 分析）：ARM7 协作式调度器的时钟与派发点

在 ARM7 WRAM（`0x0380xxxx`）里反汇编出这套调度器的骨架：

```
038070CC  ldr r3,[r1]        ; r1 = 时间戳槽（0x0380…，初值 -1）
038070DC  ldreq r0,[0x027FFC3C]  ; 首次：取“共享帧计数器”（主存系统区）
038070E4  addeq r0,r0,#0xa   ; 时间戳 = 当前帧 + 10
03807114  cmp r0,r3          ; 时间戳 >= 目标？
0380712C  bl 0x3806f74       ; 取下一项
03807138  bl 0x3806e50       ; 取任务
0380715C  …  [0x03808240] 非 0 → [0x0380BA7C] = 1
038071A0  mov r0,#0xD / mov r1,#0x11 / mov r2,#0
038071AC  bl 0x037FE9EC      ; ← 派发入口（任务号 13，参数 17/0）
```

对照两边 ARM7 WRAM：

| 地址 | 本地 frame2300 | 参考 frame2200 |
|---|---|---|
| `0x03808240`（待办标志） | `00000000` | `00000001` |
| `0x03808244`（时间戳/期限） | `00000906`（≈ 当前帧+10 ✓） | `00000018`（24） |
| `0x027FFC3C`（共享帧计数器） | `000008FC`（2300 ✓） | `00000895`（2197 ✓） |

即：**共享帧计数器两边都在走**（调度器的时钟源没问题），但待办标志
`0x03808240` 在参考里是 1、在本地是 0 —— 这正是 `0x038071A0` 派发
`0x037FE9EC(13,17,0)` 的门。下一步查谁负责把 `0x03808240` 置 1
（大概率是 ARM7 侧的另一个任务或 ARM9 的 IPC 报文触发的）。

**21-B9xg：ARM7 的 IRQ 服务与“待办位”**。ARM7 的中断服务代码在共享
WRAM 低区（`0x03003A6C` 起）：

```
03003A74  ldr r0,[0x03003AB8]   ; r0 = 0x03808250
03003A78  ldr r2,[r0,#0x60]     ; 用户 handler 指针
03003A80  ldr r0,[0x03003ABC]   ; r1 = 计数器槽
03003A84  add r0,r0,#1 / str     ; 中断计数 ++
03003A94  mov lr,pc / bx r2      ; 调用用户 handler
03003A9C  ldr r1,[0x03003AC0]
03003AA4  orr r0,r0,#1 / str     ; ← 待办位 |= 1
```

另在 `0x03003A40-0x03003A60` 有任务恢复片段（`ldmib r1!,{r0..lr} ^` +
`stmda sp!,{r0,r1,r2,r3,ip,lr}` + `ldm sp!,{pc}`），就是 ARM7 任务切换的
“换栈返回”路径。下一步把 `0x03808240/44/48/4C/50` 这片调度器状态与
参考核逐步对齐（谁置位、谁清位、每次 IRQ 后怎么变），找出本地 ARM7
“不产生活”的那一处。

### 21-B9xh（2026-09-12 分析）：ARM7 调度器为什么清掉待办位——**任务队列为空**

给 `0x03808240-47` 加写监视后，写者只有三类：

1. `0x02380024/0x0238012C`：ARM7 镜像装载时初始化（`[0x03808240] = 1`、
   `[0x03808244] = 0xFFFFFFFF`）；
2. `0x038070E8` / `0x03807128`：调度器 tick 写“下一个期限 = 共享帧计数 +
   10”（实测写 `0x0A`、`0x14`）；
3. `0x03807174`：tick 末尾把 `[0x03808240]` **清 0**。

而 `0x03807140` 是否走到清 0，取决于 `0x03806E50()` 的返回值：

```
03806E8C  ldr r0,[0x0380BA68+?]      ; 队列状态
03806E94  add r1,sp,#0
03806E98  bl 0x3806D74               ; 出队（返回 1=取到任务）
03806E9C  cmp r0,#0
03806EAC  mov r0,r4                  ; r4=1 → 返回“有任务”
03806F58  …返回 0（无任务）
```

**结论**：本地 ARM7 的**任务队列是空的**（`0x03806E50` 返回 0），所以才把待办位
清零、ARM9 也拿不到活。这与“参考核每帧多发 4 条 `0000C187`”完全对上。
下一站：查 `0x03806D74` 出队函数所用的队列由谁入队（它读
`0x0380BA68` 一带的状态字与 `0x027FFFE8` 处的系统区字），把“入队源”找出来。

### 21-B9xi（2026-09-12 分析）：直启表签名、IPC 报文账本、TIMER1 —— 分歧链锁定

这一轮先补齐两处“寄存器口径”差异，再用参考核探针对账，把“本地 ARM7 不产生活”
收敛成一条可复现的分歧链。

**1) 补上的两处寄存器口径（都已单测）**

| 地址 | 参考核（melonDS） | 本地旧值 | 现在 |
|---|---|---|---|
| `0x04000308/09`（ARM7） | `ARM7BIOSProt = 0x1204`（直启） | `0000` | `1204`（只 ARM7 可见；NDS9 侧未映射读 0） |
| `0x04000504`（ARM7） | `SOUNDBIAS` 10 位、上电 `0x200` | `0000` 且低字节写被 `&0x03` 截断 | `0x200`、按 10 位读写 |

两处改完，`--dump` 的两核 IO 快照里这两项与参考核 `ref_f2200_io*.bin` 一致。

**2) 直启表 `0x027FFC30` 的值是谁写的**

给参考核 harness 加了“`0x027FFC00-0x027FFC7F` 写入探针”（两核 × 8/16/32 位），
6000 帧跑完得到完整账本：

```
frame4  w0 pc=02012714 a=027FFC30 v=0000FFFF   ← ARM9 把签名拷进直启表
frame4  w0 pc=0201272C a=027FFC32/33/34 v=000000FF  ← 3 字节 0xFF（循环）
frame4  w0 pc=02012744 a=027FFC36 v=0000FFFF
frame4  w0 pc=02012754 a=027FFC38 v=FFFFFFFF
frame4  w1 pc=037FBDC8 a=027FFC3C v=00000000   ← ARM7 清帧计数
每帧    w1 pc=037FBA90 a=027FFC3C v=+1         ← ARM7 递增共享帧计数
```

反汇编 `0x020126FC-0x0201272C`：`ldr r2,[0x020127CC]=0x020798A0` →
`ldrh r1,[r2,#0xBE]`（= `0x0207995E`）→ `strh r1,[r0]`（`r0=0x027FFC30`），
然后循环把 `0x02079950` 起的 3 个 `0xFF` 字节搬到 `0x027FFC32/33/34`。

对照两边主存：

| 地址 | 参考 f2200 | 本地 f2200 |
|---|---|---|
| `0x02079950-0x0207995F`（签名表） | `FFFFFFFF…` | `00000000…` |
| `0x0207995E`（拷贝源半字） | `FFFF` | `0000` |
| `0x027FFC30`（直启表） | `FFFF` | `0000` |
| `0x020798A0+`（表体） | 有数据 | 全 0 |

即：**参考核里 ARM9 早就把签名表 `0x02079950` 写好（副本落到直启表），
本地这张表一直是 0**，于是 `0x027FFC30=0000`。

这不是无害差异：ARM7 调度器的取任务函数 `0x03806E50` 开头就是
（字面量池 `0x03806F64/68/6C` 已反汇编确认）

```
03806E5C  ldr r2,[pc,#0x100]   ; r2 = 0x027FFC30
03806E60  ldrh r1,[r2]
03806E64  ldr r0,[pc,#0xFC]    ; r0 = 0x0000FFFF
03806E68  cmp r1,r0
03806E6C  moveq r0,#0
03806E70  beq 0x3806F58        ; [0x027FFC30]==0xFFFF → 直接“没有任务”
03806E8C  ldr r0,[0x0380BA68+2]; 队列状态字
03806E98  bl 0x3806D74         ; 出队
```

**参考核每帧都在这条“提前返回”的分支上**，本地拿不到 `0xFFFF`，会继续往下走出队
（队列空 → 同样返回 0，但顺手把待办位清了，见 21-B9xh）。要让 ARM7 调度器与参考
同轨，得先让 `0x020798A0/0x02079950` 这张表按参考核的内容生成出来。

**3) IPC 报文账本（参考 6000 帧 vs 本地 2300 帧）**

| 报文 | 参考核（帧≥2050） | 本地 2300 帧 |
|---|---|---|
| `C0240006/C0240046/C0240086/C02400C6`（每帧各 1 条心跳，PC=037FEA78） | 各 ~3950 条 | 各 ~2300 条（对齐） |
| `0000C187` | **3799 条，首条 frame=2128** | **0 条** |
| ARM9→ARM7（`fifo9`，PC=0200B81C） | 127 条（2099/2128/3038/…） | 78 条，**末条 frame=2068** |

参考核首条 `0000C187` 的栈现场（harness `st=` 数组）里正好有第六条
`0x81DFCE07` —— 与**同一帧** ARM9 发出的 `fifo9 v=81DFCE07` 完全相同，说明
`C187` 是 ARM7 对 ARM9 报文的应答。本地 ARM9 在 frame2068 之后不再发这类报文，
ARM7 也就永远不发 `C187`。

**4) TIMER1：分歧的直接现场**

新增 `timer-change` 探针（只记 TM1CNT/IE9 控制字，不记计数器）后，本地得到：

```
timer-change f=2064 tm1=00005594 (was 00C1BBC5) ie9=00042009 (was 00042019)
             ARM9=02011938 lr9=0201190C
```

`0x04000104` 低半字是 TM1D、高半字是 TM1CNT：`00C1` = 使能 + IRQ；
本地在 frame2064 由 **ARM9 自己**把 TIMER1 停掉（并把 IE bit4 一起清），
而参考核 frame2200 的 io9 快照里 `0x04000104=00C19920`（TIMER1 仍在跑）。
同一时刻 DISPCNT 也从 `80111F18` 被改回 `80111418`（`disp-change` 探针），
说明这是游戏“切显示模式”例程（`0x020119xx`）被调用的结果。

**5) 帧级对照：分歧其实从 frame≈2061 开始**

用参考核每帧 `disp=` 与本地 `disp-change` 对齐后：

| 帧 | 参考核 DISPCNT | 本地 DISPCNT |
|---|---|---|
| ≤1925 | `00121F10`/`00161F10`（闪烁） | 同（本地在 516 帧起就进入，节奏一致） |
| 1928→1930 | `00111418` → `80111F18` | 无变化 |
| 2061/2077 | — | `80111F18` → `80111418`（**倒退**） |
| ≥2150 | `00121F10` | `80111418` |

也就是说：**本地在第 2061-2077 帧执行了一次“切模式”例程（`0x0201E530` 调用链 →
`0x020119xx`），把 DISPCNT/TIMER1 改回了旧状态**，此后 ARM9 进入 `0x0200957C`
空闲、ARM7 空转，游戏停在标题画面不再产出任务。

**下一步**：反汇编 `0x02011900-0x02011960`（显示模式/定时器配置例程）与它的调用者
`0x0201E530`，找出“本地位为什么会在 2061 帧按另一套参数调用它”，并与参考核
同一帧的寄存器现场对照。

### 21-B9xj（2026-09-12 分析续）：ARM7 的 DISPSTAT 把 ARM9 的 VCount 中断打开了

**1) 新增的 bring-up 设施（本步一起提交）**

- `--watch LO-HI`（最多 4 组）：总线写监视，打印写入者身份、PC、LR、SP 与栈顶 4 字。
  PC 取值口径与 melonDS 解释器一致（ARM=指令+8、Thumb=+4），本地与参考 harness
  的探针输出可直接对照。
- `evt: vblank=… scanline=…`：每帧事件计数（300 帧实测 `vblank=300 scanline=78900`
  = 300×263，帧时序正确）。
- 本地离线反汇编：装 `capstone`（`pip install --target …\pycaps capstone`）+
  临时脚本 `armdis.py <dump> <base> <start> <end> [arm|thumb]`，
  对 `--dump` 出来的 mainram/ITCM 做 ARM/Thumb 反汇编（比手算编码快得多）。

**2) 现场：ARM7 在写 0x04000004**

`--watch 04000004-04000006` 的第一屏输出就是 ARM7 的刷屏式写入：

```
watch: arm7 w16 a=04000004 v=0000A009 pc=037FE0B0 f=0
watch: arm7 w16 a=04000004 v=00001C2D pc=037FE0BC f=1
watch: arm7 w16 a=04000004 v=0000E10D pc=037FDE14 f=1
...
```

值的高字节（0xA0/0x5E/0x1C/0xE1）是它按扫描线分段设的 VCount 比较值，
低字节含 bit3/bit5（VBlank / VCount 匹配中断使能）。

**3) 本地把两核的 DISPSTAT 做成了同一个寄存器**

参考核（melonDS）里 DISPSTAT 是**每核一套**：`ARM9IORead16(0x04000004)=DispStat[0]`、
`ARM7IORead16(0x04000004)=DispStat[1]`，各自有自己的 `VMatch` 与中断（`SetDispStatIRQ(cpu,n)`
把 IF 挂到对应核）。本地 `disp_t` 只有一份主 `dispstat`（外加副引擎那份），
于是 ARM7 的写入直接改掉了 ARM9 的 VCount 比较值/使能位：

| 帧 2200 快照 | 参考核 | 本地（改前） |
|---|---|---|
| io9 `0x04000004` | `000A`（VCount=0，仅 VBlank IRQ 使能） | `1C28`（VCount=0x1C，bit5 使能） |
| io9 `IF` | `00280000` | `00280004`（bit2 常驻） |

bit2 常驻意味着 ARM9 每帧多领一次 VCount 匹配中断——正是"游戏按帧推进的计数
比参考核快一倍"的候选原因（标题闪烁周期：参考核 10 帧，本地 5 帧）。

**4) 修复**

- `disp_t` 增 `dispstat7`；`disp_read8/write8` 增 `is_arm7`，`0x04000004/05` 按核分流。
- 可写位改成 melonDS 口径「全部 − 只读位（0x0047，即 bit0/1/2/6）」，
  即 bit3/4/5/7-15 可写（bit7 是 VCount 比较值的 bit8）。
- `io_advance_scanline` 对三份寄存器（ARM9/ARM7/副引擎）各自比较 VCount，
  匹配改为**边沿触发**（标志已置位则不再触发中断），中断挂给该寄存器所属核。

**效果**：`IF9` 里常驻的 bit2 消失；`0x04000004` 不再出现 ARM7 的 VCount 值
（帧 2200 为 `0000000C`）。标题闪烁周期仍是 5 帧（下一处待查），但 ARM9 的
执行路径已经改变（帧 600-800 从"帧延迟空转循环"变成 ITCM 里的例程）。

**5) 顺带确认的两个调用链**

- DISPCNT 助手 `0x020027F4`（RMW 合成：`(cur & mask) | (r0<<16) | r1 | (r2<<3)`）
  被 `0x02051490 / 0x02051694 / 0x0205851C` 三处调用；它内部把 `lr` 当临时寄存器
  用（所以"写监视打印的 LR = DISPCNT 旧值"是正常现象，不是异常）。
- 标题闪烁的实现：`0x02085080` 每次调用把 `[0x02088924+0x1C]` 取反并按状态写
  DISPCNT（状态 0 → `r0=6`，状态 1 → `r0=2`），旁边 `0x020850F4` 负责把
  `[0x02088924+0x18]` 加 1；`0x02085618` 的延迟循环则在减这个计数。
  这三个函数都不是 `bl` 直接调用（走任务回调表），下一步用写监视追踪
  `0x02088924+0x1C` 的写入者，找出"谁在按帧调用它"。

**6) 追下去的结论：标题闪烁其实是"IPC 心跳驱动的显示重初始化循环"**

继续用 `--watch` 追三个地址，链条就闭合了：

```
--watch 02076E3C-02076E40   ; DISPCNT 助手的 r0 参数落地处（0x02002814 写入）
  arm9 w16 v=00000002 pc=0200281C st=…/020850C4/…/02084F38 f=730
  arm9 w16 v=00000006 pc=0200281C st=…/020850A4/…/02084F38 f=733
  → DISPCNT 助手确实是 0x02085080（闪烁函数）在用参数 2/6 交替调用

--watch 02088944-02088948   ; “显示已初始化”标志（0x02088924+0x20）
  arm9 w32 v=00000000 pc=020856A8 …   ; 0x020856A0 清标志 → 下次又走重初始化
  arm9 w32 v=00000001 pc=02084F38 …   ; 0x02084F30 置标志（0x02084F34 调闪烁函数）

--watch 0208893C-02088940   ; 循环计数（0x02088924+0x18）
  arm9 w32 v=00000001 pc=02085108 lr=02008DEC st=C0240046/0000003C/00000010/02008CCC f=525
  arm9 w32 v=00000002 pc=02085108 lr=02008DEC st=C02400C6/0000003C/00000010/02008CCC f=527
  arm9 w32 v=00000000 pc=02085670 lr=02085FEC …                    f=527
```

即：

1. ARM7 每帧发 4 条心跳（`C0240006/46/86/C6`）；
2. ARM9 的 **IPC 接收处理**（`lr=0x02008DEC`，调用点 `0x020850F4`）每收到一条就把
   `[0x02088924+0x18]` **加 1**；
3. 延迟循环 `0x02085618`（热点 `0x0208561C`）每次把该计数减 1 并比较目标值，
   满足后走到 `0x02085660`：清零计数、清 `+0x20` 标志、并把画面缓冲拷来拷去；
4. `+0x20` 被清 → `0x02084F18` 的重初始化分支又执行一次 → 调 `0x02085080`
   写 DISPCNT → 于是看到"闪烁"。

**这解释了本地闪烁周期是参考核一半的成因方向**：本地这个循环每约 2.5 帧跑一次
（在 f525/f527/f530… 清计数），参考核是每 5 帧一次。两边 ARM7 的心跳条数相同，
所以下一步要对照的是"ARM9 侧每帧消费了几条心跳 / FIFO 接收中断在帧内的什么位置
被处理"——也就是 IPC FIFO 的**交付时机**，而不是游戏逻辑本身。

### 21-B9xk（2026-09-12 分析）：心跳报文的**发送时间表**才是分歧点

给两边都加上"IPC FIFO 收取探针"（本地 `--watch-r 04100000-04100004`，
参考核在 `ARM9Read32/ARM7Read32` 里挂同名钩子）后，把帧号对齐就一目了然：

| 帧号 | 参考核 ARM9 每帧收取条数 | 本地 |
|---|---|---|
| 500-538 | **3** 条（C02400C6 / C0240006 / C0240046） | 4 条（多一条 C0240086） |
| 539 起 | 4 条（C0240086 也来了） | 4 条 |
| 2050-3000 | 7.5-8.2 条/帧（多了 C187 等） | 4 条/帧（没有 C187） |

参考核 ARM7 的**发送**时间表（把发送探针提前到帧 400 后重跑）：

```
C0240006 / C0240046 / C02400C6   帧 400 之前就在发（每帧各 1 条）
C0240086                          首帧 539
0000006B                          首帧 1725
00240005                          首帧 1887
0000C187                          首帧 2128
```

本地对应的是：**C0240086 从第 0 帧起就每帧发**（`ipc:` 环形记录 #7 就是
`C0240086`，帧 0-1），C187 一条都没有。

也就是说，**分歧在 ARM7 侧"什么时候开始发第 4 条心跳"**：参考核是帧 539
（正好是标题进入闪烁状态的时刻），本地提前到帧 0。第 4 条报文的内容
`C0240086`（低字节 0x86、与 0x46/0xC6/0x06 同族，步长 0x40）像是"通道 N
已结束/状态变化"的通知，最可能来自 ARM7 的声音驱动（21-B9wu 已确认 ARM7
在 `0x037FEFD0` 一带轮询 `SOUNDxCNT` 高字节的通道状态位）。

**下一步**：对照两边的"声音通道结束事件"时间线——
本地给 `snd` 加通道开始/结束打点（帧号 + 通道号），参考核在 melonDS
`SPUChannel` 的结束处打点，确认是不是"某通道在本地提前结束"导致第 4 条
心跳提前发出。

### 21-B9xl（2026-09-12 分析）：心跳报文的**编码方式**与发送者定位

**1) 先排除了"声音通道提前结束"这个猜想**

给本地 `snd` 加了通道打点（`snd: ch=N start/end f=…`，上限 240 条）后，800 帧里
通道最早在 **帧 522** 才开始启动（ch2/ch4/ch5/ch6/ch7 @522，ch0/ch3 @524，
ch1/ch12/ch13 @527，…），而本地 ARM7 的 `C0240086` 在 **帧 0-1** 就发出去了。
所以第 4 条心跳与"某个声音通道结束"无关。

**2) 报文是怎么拼出来的（反汇编共享 WRAM 发送例程）**

```
030069F4  ldr r3,[sp]           ; 拼装缓冲
030069F8  bic r3,r3,#0x1f       ; 清 bit0-4
030069FC  and r0,r0,#0x1f       ; ← 参数0 的 bit0-4 放进去
03006A0C  and r0,r2,#1
03006A10  orr r0,r3,r0,lsl #5   ; ← 参数2 的 bit0 → bit5
03006A1C  bic r0,r1,#0xfc000000 ; ← 参数1 只留 26 位
03006A20  orr r0,r2,r0,lsl #6   ; 报文 = (参数1<<6) | (参数0&0x1F) | (参数2&1)<<5
03006A28  …读 IPCFIFOCNT7(0x04000184) bit14 → 非满则置 bit13 清空，再写 SEND
```

代入四条心跳：

```
0xC0240006 = (0x03009000<<6) | 0x06      → 参数0=6, 参数1=0x03009000, 参数2=0
0xC0240046 = (0x03009001<<6) | 0x06
0xC0240086 = (0x03009002<<6) | 0x06
0xC02400C6 = (0x03009003<<6) | 0x06
```

也就是 ARM7 在按 **共享 WRAM 里四个相邻地址 `0x03009000-0x03009003`** 逐个发
"命令 6" 报文。参考核里第 3 个（`0x03009002`）到帧 539 才开始发，
本地从帧 0 就在发——**分歧就在"这四个槽什么时候被登记/什么时候开始被扫描"**。

（26 位参数左移 6 位正好落进报文的 bit6-31，所以报文里能读出地址：
`报文>>6 = 0x0300900n`。这对后续定位很有用。）

**3) 已经找到的相关例程**

```
0300647C  push {r4,r5,r6,lr}    ; “发命令 0x0C + 载荷”的循环重试版
0300648C  mov r0,#0xC / mov r1,r6 / mov r2,#0
03006498  bl 0x30069EC          ; 发送（返回非 0 继续重试）

03006428  … mov r0,#0 … bl 0x3006554（ch=0..3）  ; 初始化 4 条通道
03006554  push {r4,lr}          ; 按 ch 计算 0x040000BA+ch*12
03006574  ldrh r2,[r1,#0xb0] / bic … / strh    ; 关掉 DMA0-3 的控制字
```

**下一步**：找到"扫描 `0x03009000-0x03009003` 并发命令 6"的那段 ARM7 代码
（在共享 WRAM 里继续往前追调用链），确认这四个槽是**由谁、在什么条件下登记**的；
本地是帧 0 就登记齐了，参考核第 3 槽要到帧 539。

**4) 用参考核的栈快照定位到"队列表"（本次收尾）**

比较参考核两条心跳报文发送时的 20 字栈快照：

```
C0240086: … 03804540/0380A8A4/00000000/00000000
C0240006: … 03804540/0380A934/00000000/00000000
```

除了倒数第 3 个字（0x0380A8A4 vs 0x0380A934，差 0x90）之外**完全相同**，
说明两条报文出自**同一段循环**、只是当前处理的"条目指针"不同。这两个地址落在
ARM7 WRAM 的同一张表里（早期 heap dump 显示 0x0380A7BC 起每 0x18 字节一个条目：
`0380A7FC/0380A814/0380A82C/…/0380A964`）。

也就是说：**ARM7 侧那张"任务/条目表"里有几项，它就每帧发几条心跳**。
本地从帧 0 起就有 4 项，参考核在帧 539 之前只有 3 项（缺的是让指针落在
`0x03009002` 那一项）。

**下一步（下一轮直接做）**：在参考核里加一个 **帧 120 的 ARM7 WRAM dump 点**
（harness 现在只 dump 1500+ 的帧），把 `0x0380A780-0x0380A980` 与本地同一帧逐字对照，
找出"多出来/提前生成"的那一项是谁写的（配合 `--watch` 追它的写入者）。

### 21-B9xm（2026-09-12 分析）：帧 120 的双核快照对照

harness 加了帧 60/120/300 的 dump 点后，同帧对照结果：

| 区域（帧 120） | 结果 |
|---|---|
| **共享 WRAM（32KB）** | **0 处差异** |
| ARM7 WRAM 队列表 `0x0380A740-0x0380A964` | **0 处差异**（条目内容一致） |
| 声音寄存器 `0x04000400-0x04000520`（ARM7 视角） | **0 处差异** |
| ARM7 WRAM 零散计数器 | 多处（见下） |
| ARM7 视角的若干 IO | 多处泄漏（见下） |

也就是说，"本地多出第 4 条心跳"**不是**队列表内容不同、也不是声音寄存器状态不同。

**1) 计数器差异 = 启动时刻差**

```
0x03808480（每帧 +1，看门狗/帧计数）  ref=0x6F(111)  loc=0x77(119)
0x0380A9CC = 上面那个 +1              ref=0x70(112)  loc=0x78(120)
0x0380A6D8（另一个计数）              ref=0xB7(183)  loc=0xD6(214)
```

用 `--watch 03808480 / 0380A9CC` 在本地看到：两者都由 ARM7 每帧 +1
（`pc=037FDD80` 与 `pc=037FDF24`，后者在处理 ARM9 的 `81DD5807` 报文时递增）。
帧 120 时本地比参考核多 8 —— 即 **本地 ARM7 的"每帧 tick"比参考核早开始约 8 帧**，
属于启动时刻差，而不是节拍差。

**2) ARM7 视角的寄存器差异（逐条核对后：多数是对的，少数待查）**

参考核里 ARM7 读 `0x04000240`（VRAMCNT_A）等 **ARM9 专属**寄存器返回 0，
本地却把 ARM9 的值泄给了 ARM7：

```
04000240: ref=00000000 loc=00000300   ; VRAMCNT_A（ARM9 专属）
04000204: ref=00000000 loc=0030E880   ; EXMEMCNT（ARM7 侧应是 ExMemCnt[1]）
040001A0/A4/A8: ref=0 loc=0000C000/27416000/000000B8  ; 卡带总线寄存器待查
04000214: ref=00000000 loc=00080000   ; ARM7 IF 常驻 bit19（IPC recv 未读）
04000300: ref=00000000 loc=00000001   ; POSTFLG7（已知差异）
04000130: ref=000003FF loc=0000FFFF   ; KEYINPUT 高字节
04000134/38: RCnt/RTC 差异（游戏在轮询 RTC）
```

逐条核对 melonDS 语义后：

- `0x04000240`：ARM7 侧应该读 **`GPU.VRAMSTAT`**（bank 归属状态），参考核此时为 0，
  本地也返回 0 —— **这条是对的**（本地没实现 VRAMSTAT，但值恰好一致）。
- `0x04000241`：ARM7 的 WRAMCNT 视图，本地返回 0x03（`wramcnt`）——**对的**。
- `0x04000204`：ARM7 侧是 `ExMemCnt[1]`，参考核此刻 0x0000、本地 0xE880 ——**待查**
  （melonDS 直启给 `ExMemCnt[1]` 写的是 0x0080，两边都不是这个值，需要确认游戏是否改过）。
- `0x040001A0/A4/A8`（卡带总线）与 `0x04000214`（IF7 bit19）、`0x04000300`（POSTFLG7）、
  `0x04000130`（KEYINPUT 高字节）：**待查**，其中 IF7 bit19 与本地 ARM7 队列里
  未被取走的报文有关（参考核此刻队列是空的）。

**下一步**：先查 `IF7 bit19`/队列未读这一条（是不是 ARM9→ARM7 报文在本地没有被及时取走），
再查 EXMEMCNT 与卡带总线；这些差异里任何一处都可能让 ARM7 的分支走偏，
从而多登记一条心跳。

### 21-B9xn（2026-09-12 分析）：**ARM7 调度器 tick 的次数**才是真正分歧

**1) 先排除 IPC 那条**：帧 120 的两核 IPC 寄存器几乎完全一致

```
0x04000184 (FIFO CNT7)  ref=8501  loc=8501   （含“接收非空”状态位，两者都非空）
0x04000180/88/8C        ref=0     loc=0
0x04000210 (IE7)        ref=0104009D loc=0104009D
```

本地那条常驻的 `IF7 bit19` 读出来看：**IE7 的 bit19 = 0**（两边一样），
所以它被屏蔽、不会产生中断——只是"边沿时刻比游戏清标志晚一点"的记账差异，**无害**。
另外统计 ARM7 每帧取走报文数：参考核帧 400-800 取 2 条、本地也是 2 条，**节奏一致**。

**2) 真正分歧：`0x03808244`（调度器期限）被写的次数**

用参考核 harness 里已有的 ARM7 WRAM 写跟踪（覆盖 `0x8240-0x8260`）拉出完整账本：

```
参考核 6000 帧里对 0x03808244 只有 3 次写：
  pc=02380134 lr=02380094 v=FFFFFFFF   （镜像装载初始化）
  pc=038070F0 lr=037F8518 v=0000000E   （≈帧 4，期限 = 帧+10）
  pc=03807130 lr=037F8518 v=00000018   （≈帧 14，期限 = 帧+10）
之后 5980 帧再没有任何写入。
```

本地用 `--watch 03808244-03808248` 全同口径：

```
pc=038070F0 lr=037F8518 v=0000000A  f=0
pc=03807130 lr=037F8518 v=00000014  f=9  + 清 0x03808240
pc=03807130 lr=037F8518 v=0000001E  f=19 + 清 0x03808240
pc=03807130 lr=037F8518 v=00000028  f=29 …
… 每 10 帧一次，2300 帧里 200+ 次
```

两边的**代码路径完全相同**（pc/lr 一样，lr=0x037F8518 是 ARM7 主循环里的
`bl 0x300C17C` 调用点），但参考核只调用两次、本地每 10 帧调用一次。

**3) ARM7 主循环（共享 WRAM）**

```
030004F8  bl 0x300052C      ; 内部等待/服务（含 Halt 相关）
030004FC  bl 0x30064E8 / cmp r0,#0 / beq …
03000514  bl 0x300F0C4
03000518  bl 0x300C17C      ; ← 调度器 tick（写 0x03808244 的就是它）
0300051C  b  0x30004F8      ; 回到循环头
0300052C  ldr ip,[pc] / bx ip → 0x038043E3   ; 跳进游戏 ARM7 主体（Thumb）
```

也就是说：参考核的 ARM7 主循环**在帧 14 之后就不再（或极少）走到 tick**，
本地每 10 帧走一次。tick 内部的第一次写（`0x038070F0`）是"首次初始化期限"，
第二次（`0x03807130`）是"到期后重新设期限"；参考核第二次之后再没到期，
本地则持续到期。

**下一步**：给参考核也加"ARM7 执行到 0x038070C0 段"的探针（或对 ARM7 主循环
`0x030004F8/0x03000518` 计数），确认参考核是**根本没进循环**还是**进了但走提前返回分支**；
这一步会直接决定是修"Halt 唤醒后的返回路径"还是"tick 的时限判断"。

### 21-B9xo（2026-09-12 分析）：根因锁定 = **卡带读取路径**（`0x04100010` CARD_DATA）

**1) 参考核 ARM7 其实一直在跑 tick，只是走了提前返回**

给 melonDS 的 ARM 解释器加了三个 PC 区间计数（主循环 / tick / Halt 段）后：

```
帧      主循环(037F84F0-8520)  tick(038070C0-71C0)  Halt段(00001140-1160)
10      150                    534                   6195
100     7871                   21036                 12813
1000    82253                  212304                153121
5000    429334                 1104798               1021611
```

即参考核的 ARM7 **每帧执行 tick 约 220 次**，但里面**几乎从不写期限**——所以差别不在
"有没有跑"，而在 tick 内部的**分支**。

**2) tick 的分支结构（反汇编 `0x038070C4-0x038071BC` + 字面量池）**

```
038070CC  ldr r1,[0x038071C4]=03808244   ; 期限槽
038070D0  ldr r3,[r1]
038070D8  cmp r3,#-1
038070E8  streq r0,[r1]                  ; 首次：期限 = 帧计数+10 → 直接返回
038070F0  ldr r0,[0x038071CC]=0380BA7C
038070F8  cmp r0,#0 / bne 0x38071B8      ; ← 闸门 1：非 0 直接返回
03807100  ldr r0,[0x038071D0]=0380BA78
03807108  cmp r0,#0 / bne 0x38071B8      ; ← 闸门 2：非 0 直接返回
03807110  ldr r2,[0x038071C8]=027FFC3C   ; 当前 = 共享帧计数
03807118  cmp r0,r3 / blo 0x38071B8      ; 未到期 → 返回
03807128  str r0,[r1]                    ; 到期：期限 = 当前+10
0380712C  bl 0x3806F74
03807138  bl 0x3806E50                   ; 取任务
03807150  movne/strne → [0x0380BA7C]=1   ; 取不到任务且待办位非 0 → 置闸门 1
03807174  str 0 → [0x03808240]           ; 取到任务 → 清待办位
```

帧 120 的实测值：

```
0x0380BA7C（闸门1）  ref=1  loc=0     ← 参考核被闸门挡住，本地畅通
0x0380BA78（闸门2）  两边都是 0
```

本地 `--watch 0380BA7C-0380BA80` 显示**只在启动时被写两次 0、之后再没被写过**
（即本地从未置 1），所以本地每 10 帧都续期 + 走一次派发。

**3) 为什么参考核会置 1（B9xh 的伏笔在这里回收）**

置 1 的条件是：`0x3806E50`（取任务）返回 0 **且** `[0x03808240] != 0`。
而 `0x03806E50` 开头就是（B9xi 已确认）：

```
03806E5C  ldr r2,[0x038071C4?…] ; r2 = 0x027FFC30
03806E60  ldrh r1,[r2]
03806E68  cmp r1,#0xFFFF
03806E6C  moveq r0,#0         ; [0x027FFC30]==0xFFFF → 直接“没有任务”
```

参考核 `[0x027FFC30] = 0xFFFF`（帧 4 由 ARM9 从签名表拷贝） → 取任务恒返回 0 → 闸门置 1；
本地 `[0x027FFC30] = 0x0000` → 走另一条路。

**4) 签名表的数据来自卡带读取例程（真正的最上游）**

给参考核加"`0x02079800-0x020799FF` 写入"探针后：

```
sigtbl w0 a=02079800 v=00000000 pc=020008B4 lr=02000894 frame=0   ; 这是 .bss 清零（两边都有）
…
sigtbl w0 a=0207982C v=F03002DB pc=02011934 lr=0201190C frame=23  ; ← 参考核帧 23 写入真实数据
sigtbl w0 a=02079830 v=000032F0 pc=02011934 lr=0201190C frame=23
sigtbl w0 a=02079834 v=F03002E9 pc=02011934 lr=0201190C frame=23
```

反汇编 `0x020118A4` 起的例程，它的三个基址字面量是：

```
0x02011994 → 0x02079000   ; 游戏结构 sb
0x02011998 → 0x04100010   ; r5 = CARD_DATA（卡带数据口）
0x0201199C → 0x040001A4   ; r6 = CARD_CMD（卡带命令口）
```

循环体（`0x02011918-0x02011938`）：读 `[r6]` 的状态位（bit23/bit31）、从 `[r5]`
取一个 32 位字、按索引存进 `[[sb+0x20]]` 指向的表 —— 就是**"发卡带命令 → 从
CARD_DATA 读数据 → 存进 RAM"**。

所以：**参考核从卡带读到了真实数据（表里 0xF03002DB…、尾段 0xFFFF），本地这条
卡带读路径返回 0**，于是表是空的 → `0x027FFC30` 变 0 → ARM7 调度器不设闸门 →
本地多登记一条心跳 → ARM9 显示重初始化循环快一倍 → 游戏停在标题且状态与参考核不同。

**下一步**：修 **ARM9 侧的卡带读取路径**（`0x040001A4` 命令 + `0x04100010` CARD_DATA
读数据、地址自增、`ROMCTRL 0x040001A2` 的位语义），与参考核 `NDSCart`/`NDS::ARM9Read32(0x04100010)`
对齐；随后验证 `0x020798xx` 表内容与 `0x027FFC30` 是否变成 `0xFFFF`，再看 ARM7 闸门
`0x0380BA7C` 是否与参考核一致置 1。

**5) 本地这条路径的实际现场（`--watch` + `--watch-r`）**

```
--watch 040001A4-040001AC（本地）：
  w8  a=040001A8 v=B8          pc=02011C7C  f=8   ×309
  w8  a=040001A9/AA/AB v=00    pc=02011C88/94/9C f=8
  w32 a=040001A4 v=A7416000    pc=0201187C  f=8   ×309
  …同样的一组在 f=12 又出现 129 次…

--watch-r 04100010-04100014（本地）：**0 次读取**
```

解读：

- `0xA7416000` → bit31=1（启动）、bits24-26=7（4 字节块）、cmd delay=0x41、
  bit27=0（5 周期传输）——就是"读 4 字节"的 ROMCTRL 写法；
- 命令口写的是 `B8 00 00 00 …`（读芯片 ID 路径）；
- **每帧几百次重试、且 ARM9 一次都没从 `CARD_DATA` 读数据**，
  说明本地 ROMCTRL 的 DRQ（bit23）没有被游戏看到，或 `CARD_DATA` 端口没被走到。
- 对照参考核：同一段例程（`0x020118A4`）在帧 23 正常把数据写进
  `0x0207982C…`（`F03002DB/000032F0/…`），并且写到尾段时是 `0xFFFF`。

本地 ARM9 的 hot9 里 `pc=0201191C/02011920/0201193C` 各 2400 万-4300 万次，
正是**卡在读 ROMCTRL 状态的轮询循环里**。

**下一步（具体的修法）**：按 melonDS `NDSCart::ReadROMCnt/WriteROMCnt` + `CartCommon::ReadROM`
对齐三件事：

1. `WriteROMCnt`：bit31 启动时锁存命令、按块大小/时序置 DRQ（bit23）与忙碌（bit31）；
2. `CARD_DATA`（`0x04100010`）读：返回当前传输地址处的 ROM 数据并自增地址；
3. `CARD_CMD`（`0x040001A8`）：确认命令字节顺序（`B7`=读 ROM / `B8`=读芯片 ID），
   以及 `B7` 的地址提取与 <0x8000 的重定向规则。

修完后的验收点：本地 `--watch-r 04100010` 应能看到 ARM9 读取、`0x020798xx` 表
出现与参考核相同的 `F03002DB/000032F0/…` 与 `0xFFFF` 尾段，`0x027FFC30` 变 `0xFFFF`，
ARM7 `0x0380BA7C` 置 1。

**6) 更正（同日复查）：卡带读取路径其实在工作；真正的差异是"填表的值"**

补上"读监视支持 IO 区间"（此前 `--watch-r` 只挂在 `bus_read32` 的两个特例上，
所以看不到 `0x040001A4` 的读）后重测：

```
--watch-r 04100010-04100014（本地 20 帧）：161,822 次 ARM9 读，pc=0201192C（卡带循环）
  值包含真实数据：00000270 / 004E0006 / 0000028F / F0000006 / 00007FC2（=卡带 ID）
  也有 FFFFFFFF（未就绪时读）与 0C010C0C 等
```

也就是说本地 **CARD_CMD/ROMCTRL/CARD_DATA 这条路径是通的**（`B8` 读芯片 ID、
`B7` 读 ROM 都有数据回来），前一轮"卡带读路径坏了"的判断需要更正。

真正剩下的差异在 **帧 4 的一次"填表"**：

```
参考核 sigtbl: a=02079940 v=0000FFFF pc=01FF828C lr=01FF8268 frame=4   （16 位写，步长 2）
本地   watch : a=02079956 v=00000000 pc=01FF828C lr=01FF8268 f=0        （同样 pc/lr、同样步长）
```

**同一条 ITCM 例程（`0x01FF824C` 起，调用点 `lr=0x01FF8268`）、同一批地址，
参考核填 `0xFFFF`、本地填 `0x0000`** —— 值来自该例程的输入（栈现场里能看到
`027FFFE8 / 040000C4 / 00000040 / 02079920`，但这两个内存点两边实测都是 0）。
这一步的差异才是 `0x0207995E`（→ `0x027FFC30`）为 0 的直接原因。

**下一步**：把 `0x01FF824C` 这段例程的完整反汇编（含它前面的调用者
`lr=0x01FF8268` 所在函数）读清楚，找出那个"该是 0xFFFF 的输入寄存器/内存值"，
再在本地补上；之后 `0x027FFC30` → ARM7 闸门 → 心跳条数 这条链应当自然对齐。

**7) 再更正：那不是普通写入，是 DMA 填充**

给写监视加上 CPSR 后发现：那批 16 位写发生时 **CPSR = 0x6000009F（ARM 态、I=1）**，
而按 `pc-8 = 0x01FF8284` 对应的指令其实是 **32 位 `str r4,[r3,#8]`**
（ITCM 原始字节 `08 40 83 E5` = `0xE5834008`）——**一条 32 位存储不可能产生
"两个 16 位写 + 地址步长 2"**。

结合例程本身的语义：

```
01FF8254  mov r7,r0 / mov r6,r1 / mov r5,r2 / mov r4,r3
01FF8264  bl 0x2009280                 ; 同步/清标志
01FF8268  mov r1,#0xC
01FF826C  mul r3,r7,r1                 ; r3 = ch * 12
01FF8270  add r1,r3,#0x04000000
01FF8278  str r6,[r1,#0xb0]            ; ← 0x040000B0 + ch*12 = DMAn SAD
01FF827C  add r3,r3,r2                 ; 表 + ch*12
01FF8280  str r5,[r3,#4]               ; DMAn DAD
01FF8284  str r4,[r3,#8]               ; DMAn CNT
01FF8288  ldr r1,[r2]
```

这是一段 **DMA 通道配置例程**（栈里还压着 `0x040000C4` = DMA1CNT）。而
`watch` 打印的 `pc` 是"CPU 最后一次取指的地址"——**DMA 搬运期间 CPU 不动**，
所以 pc 会一直是那条配置指令。也就是说：

> `0x02079940-0x0207995F` 是由 **DMA（16 位单位）填充**的：
> 参考核填 `0xFFFF`、本地填 `0x0000`。

这与"两边 ITCM 完全相同、CARD_DATA 读取正常"并不矛盾——差的是 **DMA 的源/模式**：
要么源地址落在我们没有正确映射的区域（本地读 0、参考核读 0xFFFF），
要么"源地址固定/自增"的模式位处理不同。

**下一步**：给 `--watch` 之外再加一个 DMA 探针（在 `dma_transfer` 里打印
通道号/属主核/SAD/DAD/CNT/单位大小/控制位），把帧 4 前后所有 DMA 搬运列出来，
找出填充 `0x02079940` 的那次搬运的 **SAD/CNT**，再对照参考核 `NDS::CheckDMAs/DoDMAs`
的同一笔；很可能是某个 IO/内存区域（例如卡带总线窗口或 ARM7 侧地址）在本地的读值不对。

### 21-B9xq（2026-09-12）：**根因修复 = GBA 扩展槽空槽应读 0xFF**

加了 DMA 探针（`dma_transfer` 里打印属主核/通道/SAD/DAD/长度/单位/模式）后，
帧 0-8 的搬运清单一目了然：

```
dma: arm9 ch=1 sad=08000080 dad=02079920 n=32 unit=16 srcmode=0 dstmode=0 cnt=8000
```

**这就是填充 `0x02079920-0x0207995F` 的那一笔**（32 个半字 = 64 字节），
源地址 `0x08000080` 是 **GBA 扩展槽**（slot-2）窗口。

对照参考核：空槽在 melonDS 里读出来是 `0xFF`（open-bus 填充），于是 DMA 填出
`0xFFFF`；本地之前把 `0x08000000` 起的区间当"未映射"（读 0），于是填出 `0x0000`。

**修复**（`src/bus/bus.c`）：`0x08000000-0x0BFFFFFF`（slot-2 窗口）在无卡时
统一返回 `0xFF`。

验证（同一帧对比）：

| 地址 | 参考核 | 修复前 | 修复后 |
|---|---|---|---|
| `0x02079940` | `FFFFFFFF` | `00000000` | `FFFFFFFF` ✓ |
| `0x02079950` | `FFFFFFFF` | `00000000` | `FFFFFFFF` ✓ |
| `0x027FFC30`（直启表闸门） | `FFFFFFFF` | `00000000` | `FFFFFFFF` ✓ |
| `0x03808240`（ARM7 待办位） | `1` | `0` | `1` ✓ |
| `0x0380BA7C`（调度器闸门） | `1` | `0` | `1` ✓ |
| `0x03808244`（期限） | `0x18` | `0x82` | `0x14`（同一量级） |

**这是本轮的关键进展**：从"ARM7 不产生活"一路追到的**最上游根因**（GBA 槽空槽
读值）已经修掉并按参考核逐项验证；ARM7 调度器的两道状态位现在与参考核一致。

仍然存在的差异（下一步）：

- ARM7 每帧仍是 4 条心跳（参考核帧 539 前是 3 条），`c187` 仍为 0；
- 本地 DISPCNT 在帧 2061/2077 走到 `80111F18 → 80111418` 后停住，
  参考核在帧 1930 走到 `80111F18`、2150 后稳定在 `00121F10`；
- 需要重新对照"心跳接收速率"与"ARM9 显示重初始化循环周期"，确认这次根因修复
  是否已经把节拍搬正（可能还需要更长的帧数才能看到 C187 起步）。

### 21-B9xr（2026-09-12 分析）：心跳发送 API 与调用点清单

**1) 工具补充**：`bus_write32` 的两个特例（`IPC FIFO SEND 0x04000188`、
`CARD_DATA 0x04100010`）此前**绕过了写监视**，现在也挂上 `bus_dbg_watch`——
否则看不到 ARM7 发报文时的 PC/LR/栈。

**2) 本地 ARM7 心跳的发送链**

```
watch: arm7 w32 a=04000188 v=C0240086 pc=037FEA78 lr=037FEA4C sp=0380A6F8 f=1
       st=C0240086/03804798/00000000/0380A740
```

`0x037FEA78` 是共享 WRAM 里的通用 IPC 发送例程（`0x030069EC` 的返回点），
它的调用者返回地址 `0x03804798` 落在 ARM7 WRAM 的**发送包装函数**里：

```
038046D4  push {r4,r5,r6,lr}
038046D8  and r2,r0,#0x70         ; 类型字段
038046DC  … 按类型分支设置 r4（命令号）：
          0x10 → r4=6   0x20 → r4=4   0x40/0x50 → r4=9   0x60 → r4=8 …
0380476C  and r0,r0,#0xff
03804770  orr r0,r0,#0x80
03804774  lsl r0,r0,#8
03804778  orr r2,r0,#0x03000000   ; r2 = 0x03000000 | ((val|0x80)<<8)
0380477C  and r0,r1,#0xff
03804780  orr r6,r2,r0            ; 槽号 = 第二参数低字节 → 0x0300900n
03804788  mov r0,r4 / mov r1,r6 / mov r2,#0     ; (命令, 槽地址, 0)
03804794  bl 0x37FE9EC            ; 发送（失败重试到返回值 ≥0）
```

代入后正好得到四条心跳：`(0x0300900n<<6)|6` =
`0xC0240006/C0240046/C0240086/C02400C6`（n=0..3）。

**3) 调用点清单（下一步的靶子）**

在 ARM7 WRAM 里对该包装函数（`0x038046D4`）的 `bl` 调用点共 12 处，
都落在 ARM7 声音/事件驱动代码里：

```
038049AC  038049C8  03804A3C  03804AD0  03804B38  03804B58
03804BEC  03804C0C  03804C3C  03804C5C  03804E28  03804E44
```

**下一步**：给这 12 个调用点各加一个计数器（或临时改写成带编号的探针），
跑参考核与本地各一小段帧数，直接看出**哪一处负责发槽 2（0xC0240086）**，
再回到那段代码找出"帧 539 之前不发的条件"——它多半就是本地与参考核的最后一个节拍差。

**4) 结论（同轮完成）：四条心跳其实出自**同一个调用点** `0x03804B5C`

把写监视的栈快照从 4 字扩到 8 字后，四条心跳的调用链完全相同：

```
C0240046 f=0 st=C0240046/03804798/00000000/0380A740/0380A79C/03804B5C/…
C0240086 f=1 st=C0240086/03804798/00000000/0380A740/0380A79C/03804B5C/…
C02400C6 f=1 st=C02400C6/03804798/00000000/0380A740/0380A79C/03804B5C/…
C0240006 f=1 st=C0240006/03804798/00000000/0380A740/0380A79C/03804B5C/…
```

即发送代码是（反汇编 `0x03804B20-0x03804B60`）：

```
03804B20  ldr r0,[sl,#4]
03804B24  cmp r0,#0
03804B28  bne 0x3804B40
03804B34  mov r1,#0
03804B38  bl 0x38046D4          ; 分支 A：槽字段为空
03804B3C  b 0x3804B5C
03804B40  …
03804B48  ldr r1,[sl,#8]
03804B4C  and r1,r1,#0xff       ; 槽号 = [sl+8] 的低字节
03804B58  bl 0x38046D4          ; 分支 B（正常槽）
03804B5C  mov r0,#0 / bl 0x380465C
```

**四条心跳来自同一次"槽结构链表遍历"**（每次遍历处理一个槽，四个槽 → 四条报文），
链表节点指针在栈里可见（`0x0380A740/0x0380A79C/…`）。所以"本地多一条"等价于：

> **本地这张槽链表在帧 0 就是 4 个节点，参考核在帧 539 之前只有 3 个。**

而帧 120 的 ARM7 WRAM 对照显示：槽结构体的**内容**两边一致，差异集中在
`0x03808470/74`（`{038085B0,0380851C}` vs `{0380851C,038085B0}`）与
`0x03808530/34`（`{038085B0,0}` vs `{0,038085B0}`）这两对"链表指针"上。

**下一步**：给槽链表加"节点数/遍历次数"探针（本地在 `0x03804B5C` 处计数即可，
参考核可用同一 PC 计数），确认两边每帧遍历到的节点数；再把
`0x03808470/74`、`0x03808530/34` 这对指针的**写入者**用 `--watch` 抓出来，
看是谁在帧 539（参考核）才把第 4 个节点挂进链表。

**5) 再往上追：心跳来自 ARM7 的"消息队列处理循环"**

槽链表指针对的写入者已经抓到（都在共享 WRAM 的列表管理代码里）：

```
watch: 03808470 v=038085B0 pc=037FD904 lr=037FD8C8 f=5   （0x03005904 / 0x030058C8）
watch: 03808530 v=00000000 pc=037FDBD4 lr=037FDB74 f=5   （0x03005BD4 / 0x03005B74）
watch: 03808534 v=038085B0 pc=037FDBDC lr=037FDB74 f=5
```

而"每槽发一条心跳"的那个槽处理函数 `0x03804A54` 是被**消息队列循环**调用的：

```
038044F4  push {r4,r5,r6,lr}          ; 消息处理循环
03804508  mov r0,r6 / mov r1,r5 / mov r2,r4
03804514  bl 0x37FCBCC                ; ← 从队列取一条消息
03804518  ldr r0,[sp] / ldr r1,[r0]   ; 消息类型
03804520  cmp r1,#3 / addls pc,pc,r1,lsl #2
03804528  类型0 → 回循环
0380452C  类型1 → 0x0380453C  bl 0x03804A54   ; ← 槽处理（发心跳）
03804534  类型3 → 0x03804544  bl 0x038062FC
03804538  类型4 → 0x0380454C  bl 0x038055F0
03804530  类型2 → 0x03804554  bl 0x027F5A24
```

也就是说：**心跳不是"每帧定时发"，而是由队列消息驱动**——队列里有多少条
类型 1 消息，就会走多少次槽处理、发多少条心跳（本地 4 条/帧、参考核帧 539 前 3 条/帧）。

**下一步**：给 `0x03804514`（出队）与 `0x0380453C`（类型 1 分支）各加计数器，
对照两边的"每帧出队条数 / 类型分布"，找出参考核帧 539 前少的那一条类型 1 消息
是**谁没有投递**（多半在 ARM9 侧或 ARM7 的定时/声音事件里）。

**6) 计数对照（本轮已做）**

本地新增 `--pchit ADDR`（最多 4 个 PC，命中计数打印在摘要里）；参考核在
`ARM.cpp` 的解释器里加了同样三个 PC 的计数。结果：

| 计数点 | 本地（120 帧） | 参考核 | 结论 |
|---|---|---|---|
| `0x03804514` 出队调用 | 480 → **4.0/帧** | 帧 50：168（3.36/帧）→ 帧 1000：3968（**3.97/帧**） | 参考核从 3.3 爬升到 4.0 |
| `0x0380453C` 类型 1 分支 | 478 → 3.98/帧 | 帧 50：166 → 帧 1000：3966 | 同上 |
| `0x03804A54` 槽处理 | 478 | 同上 | 每条类型 1 消息 → 一次槽处理 → 一条心跳 |

**两边最终都是 4.0/帧**，差别只在**爬升过程**：参考核在帧 50-1000 之间从 3.3 涨到
4.0（拐点约在帧 539，与心跳 `C0240086` 出现的时间一致），本地从帧 0 就是 4.0。
所以问题不在"消息处理机制"，而在**第 4 个槽什么时候开始投递类型 1 消息**。

**7) 投递端（入队 API）**

```
03804588  push {r0,r1,r2,r3}      ; 4 个参数
0380458C  push {r4,r5,r6,lr}
03804598  ldrh r0,[sp,#0x18]      ; 第 5 个参数 = 消息类型（>4 直接丢弃）
038045A8  bl 0x37FE3A4            ; 进入临界区
038045B0  ldr r3,[r2,#0x48C]      ; 环形缓冲写指针
038045B4  mov r1,#0x18 / mul ip,r3,r1     ; 每条 0x18 字节
038045C0  str r5,[r3,ip]          ; 参数 1 起依次存入
```

对该入队函数（`0x03804588`）的 `bl` 调用点共 12 处：

```
038049F8  03804E14  03804EA4  03804EF4  03805834  03805870
038058A0  038058DC  03805F24  03805F60  0380600C  03806274
```

**下一步**：在这 12 个入队点各挂一个 PC 命中计数（本地 `--pchit` 现成可用，
参考核同一套），跑同一段帧数后比对——**哪个点每帧多投一条（本地）**，
那段代码的触发条件就是最后一个节拍差的所在（大概率在 ARM7 的定时/声音事件侧）。

**8) 入队点计数结果：只有一处活跃**

本地 200 帧、12 个入队点全部计数（`--pchit` 上限已扩到 16）：

```
pchit: 038049F8 = 797     ← 唯一活跃（4.0/帧）
pchit: 03804EA4 = 1
pchit: 03805F24 = 1
其余 9 处 = 0
```

也就是说：**全部"类型 1"消息都出自 `0x038049F8` 这一个入队点**，
它位于函数 `0x038049DC` 内：

```
038049DC  push {r4,lr}
038049E4  mov r4,r0          ; 参数 = 槽指针
038049E8  mov r0,#0
038049EC  mov r1,#0x10
038049F0  mov r2,#1
038049F4  mov r3,r4
038049F8  bl 0x3804588      ; 入队（类型挂在栈上）
038049FC  cmp r0,#0 / bne 0x3804A40   ; 失败处理
03804A14  ldrh r1,[sp] / strh r1,[r0] ; 写回槽状态
```

即**"槽指针 → 入队一条事件 → 消息循环处理 → 槽处理发心跳"**这条链的源头是
`0x038049DC`（每个槽调用一次）。

在 ARM7 WRAM 里**找不到**对 `0x038049DC` 的直接 `bl` 调用（两个镜像都搜过），
说明它由**函数指针/任务表**间接调用（与之前"槽/通道结构链表"的遍历方式一致）。

**下一步**：给"PC 命中探针"加"首次命中时打印栈快照"（本地 `--pchit` 扩展一点），
命中 `0x038049DC` 时把栈上的返回地址打出来，就能定位是哪个任务/定时回调在
每帧投这 4 条（参考核侧同样处理），从而找出第 4 条在参考核里为什么到帧 539 才出现。

**9) 找到间接调用点（本轮已做）**

给 `--pchit` 加了"首次命中打印调用链"后：

```
pchit-hit: 038049DC arm7 lr=037FDEF8 sp=0380FF3C st=40402806/0000001F/00001968/027F9C08
pchit-hit: 038049DC arm7 lr=037FDEF8 …
```

返回地址 `0x037FDEF8` → 共享 WRAM `0x03005EF8`，正好是下面这段**任务运行器**里
`bx r7` 的下一句：

```
03005ED4  ldr r7,[sl]          ; r7 = 任务结构里的回调指针（旧值）
03005EDC  bl 0x3006214         ; 取下一个任务/更新状态
03005EE0  str r4,[sl]
03005EE4  cmp r7,#0 / beq 0x3005EF8
03005EEC  ldr r0,[sl,#4]
03005EF0  mov lr,pc            ; lr = 0x03005EF8
03005EF4  bx r7                ; ← 间接调用（本机回调就是 0x038049DC）
```

所以"槽事件投递"是**任务回调**：ARM7 的任务结构里存着 `0x038049DC` 这个函数指针，
由共享 WRAM 的任务运行器按激活次数调用——每激活一次投一条类型 1 消息（→ 一条心跳）。

**下一步**：找出**哪个任务/定时源**在每帧激活这个任务 4 次（本地）而参考核早期只有
3 次——可在任务运行器 `0x03005EF4` 处按 `r7` 区分计数（本地用 `--pchit` 打在
`0x03005EF0`，再打印 `[sp]` 之前的 `r7`），或在参考核侧对
`0x03005EF4` + `ARM7.R[7]` 组合计数。

**10) 计数对照（本地 vs 参考核，同口径）**

本地把 `--pchit` 的首次命中快照补上 `r0/r7` 后：

```
pchit-hit: 038049DC arm7 r0=00000001 r7=038049DC lr=037FDEF8 …
pchit-hit: 038049DC arm7 r0=00000002 r7=038049DC lr=037FDEF8 …
pchit-hit: 038049DC arm7 r0=00000003 r7=038049DC lr=037FDEF8 …
pchit: 038049DC = 117   （30 帧 → 3.9/帧）
```

参考核在同 PC（`0x038049DC`）加探针并打印前 12 次：

```
producer7: idx=00000001 lr=037FDEF8 frame_ins=771568
producer7: idx=00000002 lr=037FDEF8 frame_ins=774224
producer7: idx=00000003 lr=037FDEF8 frame_ins=776513
producer7: idx=00000000 lr=037FDEF8 frame_ins=778802
producer7: idx=00000001 …                 （索引 1,2,3,0 循环）
```

配合帧行的 `ins7/dq7`：

| 指标 | 参考核帧 500 | 参考核帧 1000 | 本地（30 帧） |
|---|---|---|---|
| ARM7 指令/帧 | 16,983 | 25,018 | — |
| 投递函数调用/帧（按 ~2,956 指令/次推算） | ≈5.7 | ≈8.5 | **3.9** |
| 实际投递（`dq7`）= 处理消息数/帧 | 3.94 | 3.97 | **4.0** |

**结论**：参考核里"投递函数被调用"的次数**多于**实际入队的消息数（存在丢弃/合并），
并且**处理消息数从 3.36/帧 爬升到 3.97/帧**；本地则是 **1:1、恒定 4.0/帧**。
也就是说最后一条待补的差异是**第 4 个槽为什么在参考核里要等到帧 539 才被计入**
——它取决于投递函数里那段 `0x0380499C-0x03804A14` 的**槽状态判断**，而不是消息机制本身。

**下一步**：把本地与参考核在 `0x038049DC` 处的 `r0`（槽索引）序列**按帧**完整统计
（不是只看前 12 次），对照"哪个索引在参考核里前期缺席"；再顺着
 `0x0380499C  cmp r0,#0` 前后的槽状态字段（`0x0380A9xx` 一带）找出判定条件。

**11) 重要更正：投递侧两边完全一致，差异在消费侧**

给两边都加了"投递索引直方图"后：

| 帧 | 参考核 total | 直方图 (0/1/2/3) | 本地 total（600 帧） |
|---|---|---|---|
| 50 | 165 | 41/42/41/41 | — |
| 100 | 365 | 91/92/91/91 | — |
| 500 | 1965 | 491/492/491/491 | — |
| 1000 | 3965 | 991/992/991/991 | — |
| 600 | — | — | 2397（599/600/599/599） |

**两边都是"每帧恰好 4 次投递、索引 0/1/2/3 各 1 次"**——投递侧（槽登记、索引选择）
**没有任何差异**，我上一轮"第 4 个槽到帧 539 才登记"的推断**不成立，已更正**。

真正不同的是**消费/入队结果**：

| 指标 | 参考核帧 50 | 参考核帧 500 | 参考核帧 1000 | 本地（稳定） |
|---|---|---|---|---|
| 投递调用/帧 | 4.0 | 4.0 | 4.0 | 4.0 |
| 实际入队并处理（`dq7`）/帧 | **3.36** | 3.94 | 3.97 | **4.0** |

即参考核的**消息循环早期排空得比投递慢**（环形缓冲 16 条，压力大时投递会失败/被丢弃，
`0x038049FC cmp r0,#0 / bne` 就是失败分支），随着帧数增加逐渐追平到 4.0；
本地则从帧 0 起就是 1:1、没有丢弃。

**下一步**：对照"消息循环函数 `0x038044F4` 每帧被进入的次数"（本地 `--pchit 038044F4`，
参考核同 PC 计数）。如果参考核早期进入次数少于 4/帧，就说明**限制它的那个唤醒源**
（很可能是 ARM7 的 Halt/IRQ 唤醒节奏）才是本地与参考核最后一个节拍差；
这也解释了为什么本地 ARM9 的重初始化循环会快一倍。

### 21-B9xt（2026-09-12）：GBA 槽修复后的长程验证（6000 帧）——**本地不再卡死**

跑与参考核等长的 6000 帧（`--headless-frames 6000 --dump build\loc6000`）：

```
f=3000  gx=7244   （修复前长期停在 2595）
f=6000  gx=20435  cmd=20435 port-wr=20435
f=6000  disp9=00161F10 ↔ 00121F10 交替（帧 5974-5999 持续变化）
f=6000  ARM9 PC=01FFD844（ITCM 里执行），ipc: sends9=118 sends7=24039
        disp3dcnt 从 00003011 变为 00000011
```

对照修复前（同帧数）：

| 指标 | 修复前（2300 帧） | 修复后（3000 帧） | 修复后（6000 帧） |
|---|---|---|---|
| GX 命令数 | 2595（卡住） | 7244 | **20435** |
| ARM9 状态 | 停在 `0x0200957C` 空闲 | 空闲 | **在 ITCM 里持续执行** |
| DISPCNT | 固定 `80111418` | 固定 `80111418` | **`00121F10 ↔ 00161F10` 交替**（与参考核 2150 之后同族） |
| ARM9→ARM7 报文总数 | 78 | 78 | **118** |

也就是说：**GBA 槽 open-bus 修复让游戏重新往前走了**——3D 命令流恢复、
ARM9 从"空闲等待"变为"在 ITCM 里跑例程"、显示状态推进到参考核过渡后的那一档。
（参考核在 6000 帧时 DISPCNT 稳定在 `00121F10`、ARM9 大多空闲，看起来停在静态画面；
本地则在交替闪烁并持续产生 GX 命令。）

**下一步**：把本地 6000 帧的状态与参考核同期的截图/VRAM 做一次并排对照（本地用
`--shot`，参考核用 `ref_fb_5900.bin` 的统计：颜色数 187、均值 `C235`），
确认现在两边各自处在游戏的哪个画面；同时继续追"消费侧 3.36→3.97 vs 4.0"这条
剩余节拍差（它可能就是本地循环快一倍的直接原因）。

**截图对照结果（本轮已做）**

本地 `--shot build\loc6000.bmp`（6000 帧）与参考核现存的各个 framebuffer 抽样对照
（把本地 RGB 转 BGR555 后逐像素求平均绝对差，MAX≈765）：

| 参考帧 | 平均绝对差 | 近似相同像素占比 |
|---|---|---|
| 120 | 205 | 2.4% |
| 1300 | 234 | 0.2% |
| 2000 | 418 | 0.1% |
| 3000 / 3400 / 4000 / 4500 / 5000 / 5500 / 5900 | 243-246 | ≈0.4% |

也就是说：**本地 6000 帧的画面与参考核任何一个抽样帧都不相同**（最好的也就是
帧 120、平均差 205）。本地画面是明亮的（中位亮度 183、86.8% 像素 >128），
不是黑屏也不是强制消隐。

结论：GBA 槽修复让游戏**重新开始产出画面与 GX 命令**，但两边目前处在**不同的画面/阶段**；
要判断"本地是领先还是落后"，需要给本地也建一条"每 500 帧截图"的时间线
（参考核已有 3400-5900 的逐百帧 framebuffer），再与参考核的对应帧对齐。

### 21-B9xu（2026-09-12）：本地画面时间线（新增 `--shot-every/--shot-prefix`）

新增工具：`--shot-every N --shot-prefix P`（每 N 帧存一张 `<P>_NNNNN.bmp`）。
本地 6000 帧、每 500 帧一张：

| 相邻两帧 | 画面差异（MAD，最大≈765） |
|---|---|
| 500→1000 | 333 |
| 1000→1500 | 254 |
| 1500→2000 | 152 |
| 2000→2500 | 351 |
| **2500→3000→3500→4000→4500→5000** | **全 0（静止画面，持续 2500 帧）** |
| 5000→5500 | 367 |
| 5500→6000 | 224 |

静止段（2500-5000）那张画面：2×2 抽样下 **186 种颜色**，主色 `#885830 / #986030 /
#905830 / #703820 / #804828`——偏棕褐色的画面（不是黑屏、不是纯色）。

与参考核**全部现存 framebuffer**（120、1300-1310、2000、2200、2600、3000、3400-5900）
逐像素对照：最好也只有 MAD≈201（`ref_fb_1305/2200`），本地 3000 与 5000 两张的
匹配排序完全相同（它们是同一张静止画面）。

即：**本地与参考核目前不在同一画面上**，且本地画面在 2500-5000 帧之间完全静止、
随后又继续变化（5000-6000 有两次明显变化）。

**下一步**：让参考核在 `500-1000` 与 `2600-3400` 这两段（现在没有 dump）也按 100 帧
粒度出图，再用同一套 MAD 对照，判断本地那张棕褐色静止画面到底对应参考核的哪个阶段
（领先 / 落后 / 分支不同）。

### 21-B9xv（2026-09-12）：**对照前提修正**——参考核一直在注入按键

重跑参考核后拿到**每 100 帧共 64 张 framebuffer**（0-5900）。用它对照本地时间线，
依旧没有好匹配（最好 MAD≈194）。但随后的回归检查发现了关键问题：

```cpp
// ref_harness.cpp 的帧循环（一直存在，之前对照时被忽略）
nds->SetKeyMask((frame >= 1700 && ((frame - 1700) % 120) < 12) ? 0x3F7u : 0x3FFu);
```

**参考核从帧 1700 起每 120 帧按下 12 帧**（`0x3F7` = 除 A/B 外的方向键+START 组合）。
本地此前所有对照都没带按键参数 → 两边输入不同，画面自然对不上。

用对齐的输入重跑本地：

```
build\nds-emu.exe tools\rom_ascii.nds --headless-frames 1900 \
    --key-frame 1700 --key-mask 0x3F7 --key-period 120 --shot build\loc1900k.bmp
→ 与 ref_fb_1900.bin 对照：**偶数列 100% 相同、奇数列 0% 相同**（整体 50%）
```

奇数列的差异看得很清楚（第 100 行、x=100..111）：

```
x100: 本地(000000) 参考(000000)
x101: 本地(000000) 参考(00C5FF)   ← 参考核每隔一列有青色 #00C5FF 竖条纹
x102: 本地(000000) 参考(000000)
x103: 本地(000000) 参考(00C5FF)
…
```

**结论**：

1. 对齐输入后，**本地的游戏状态与参考核一致**（偶数列逐像素相同），差异是
   **渲染层**的：参考核有一条 1 像素宽的青色竖条纹（50% 抖动/竖线效果），本地渲染成黑色；
2. 另外本地无头模式的按键持续帧数是 **8 帧**（`runner.c: key_release_frame = fr + 8`），
   参考核是 **12 帧** —— 这是一个应当对齐的输入差异。

**下一步**：

1. 把无头按键持续帧数从 8 改为 12（对齐参考核），重跑时间线对照；
2. 查那条 `#00C5FF` 竖条纹是哪一层产生的（melonDS 侧大概率是 3D 层的 50% 抖动或
   BG 的竖线效果），在本地渲染器里补上，然后再做一次 1900 帧逐像素回归。

**补充（同轮完成）**：

1. 按键持续帧数已从 8 改为 **12**（`runner.c`，与参考 harness 对齐）；重跑 1900 帧
   回归结果不变（偶数列 100% / 奇数列 0%）——说明该差异与按键时长无关。
2. 把参考核 `ref_fb_1900.bin` 做颜色直方图：

```
非零像素=24576/49152   颜色数=2
  0x0000 x24576
  0x7F00 x24576      ; 解码 = 绿 0x18→192、蓝 0x1F→248 → 即 #00C5FF 的青色
```

**参考核帧 1900 的顶屏就是"黑 + 青色"两色、恰好 50% 像素非零的抖动/条纹画面**
（典型的过场闪烁/擦除效果），本地这一层渲染成了纯黑。

**下一步**：定位这条 50% 青色抖动来自哪一层（3D 的 50% 抖动？BG 的竖线/马赛克？
还是 blend/亮度效果），在本地渲染器补上；随后用"对齐按键"的时间线重新与 64 张
参考 framebuffer 全面对照，确定本地当前处在游戏的哪个阶段。

### 21-B9xw（2026-09-12）：`DISP3DCNT` 的 bit12/13 是**写 1 清零**位

帧 1900 的 IO 快照对照直接给出了一处寄存器语义错误：

| 寄存器 | 参考核 | 本地（修复前） |
|---|---|---|
| `0x04000060` DISP3DCNT | `00000011` | `00003011` |

melonDS `GPU3D::Write16/32`：

```cpp
case 0x04000060:
    DispCnt = (val & 0x4FFF) | (DispCnt & 0x3000);
    if (val & (1<<12)) DispCnt &= ~(1<<12);   // ← 写 1 清零
    if (val & (1<<13)) DispCnt &= ~(1<<13);   // ← 写 1 清零
```

即 **bit12/13 是可写 1 清零位、写 0 时保持旧值**，本地此前当普通字节位处理，
于是它们一直是 1。

**同时发现本项目一个概念性偏差**：`gx.h` 把 bit13 定义为 `DISP3D_ENABLE`，
`render.c` 用它当"3D 图层开关"。但参考核在帧 1900 的 `DISP3DCNT=0x0011`
（bit13=0）时**3D 内容照样出现在顶屏**——melonDS 并不用这一位做图层使能。

**本轮改动**（`src/gx/gx.c`、`src/ppu/render.c`、`tests/test_nds.c`）：

1. `gx_write8` 按 melonDS 口径实现 bit12/13 的写 1 清零（低字节 bit0-7 可写；
   高字节 bit0-3 → 寄存器 bit8-11；bit4/5 → bit12/13 的 W1C）；
2. `render_3d()` 去掉 bit13 门控，直接合成 3D 帧缓冲（0 像素 = 无几何）；
3. 单测改成：三角形直接出现在顶屏 + 新增 `disp3dcnt w1c`（写 1 后读回 0）。

全量 **907 项检查 0 失败**；帧 1900 的 `DISP3DCNT` 现在读回 `00000011` ✓ 与参考核一致。

**遗留（下一步）**：修复后帧 1900 的画面依旧"偶数列 100% 一致、奇数列 0%"
——说明那条青色竖条纹**不是** 3D 图层（本地此刻 3D 帧缓冲为空、合成无变化）。
最可能的来源是 **DISPCNT bit16-17 = 2 的"VRAM 显示模式"**（参考核帧 1900 的
`DISPCNT=0x00121F10` 正是该模式）：本地 `render_engine` 的 VRAM 显示分支
（`render.c` 里 `dmode==2` 直接读所选 bank）可能选错了 bank/格式，
下一步对照两边同一帧的 `VRAMCNT` 与帧缓冲来源。

**同轮追加验证：VRAM 两边完全一致（9 个 bank、0 差异半字）**

给参考 harness 加了帧 1900/2000 的 VRAM dump（此前只有 1000-1500）后：

```
ref_vram_f1900.bin vs build\loc1900d_vram.bin
  bank A/B/C/D/E/F/G/H/I  → 差异半字数全部 = 0
```

也就是说：**同样的 VRAM、几乎同样的寄存器，参考核渲染出"黑+青色 50% 竖条纹"，
本地渲染成纯黑**。两边 `DISPCNT` 都是 `0x00121F10`（bit16-17 = 2 = VRAM 显示模式、
bank 选择位 = 0），而两边 bank A 都是全 0。

**结论**：差异出在**显示模式 2（VRAM/主存显示）的渲染路径本身**——
melonDS 在这种模式下并不是"直接读 DISPCNT bit18-19 选的 bank 前 98304 字节"
（否则两边都会是黑的），需要查 melonDS 的 LCD 源 bank（`VRAMMap_LCDC`）
与 `GPU2D::UpdateRegisters` 里 mode 2/3 的实际取样方式；本地 `render.c`
的 `dmode==2` 分支要按同一口径重写。

**下一步**：读 melonDS `GPU2D::UpdateRegisters`/`GPU2D_Soft` 中 dispcnt mode 2/3
的取样（含 LCD 源 bank 与像素格式），把本地 VRAM 显示分支改成同口径，再跑
1900 帧逐像素回归。

### 21-B9xx（2026-09-12）：mode2 渲染路径试验 + 帧 1900 全量 IO 对照

**1) 试验：去掉 mode2 的 VRAM-display 分支**

melonDS 只在 **mode 3** 走 display-FIFO 特例（`GPU::UsesDisplayFIFO()`：
`((DispCnt >> 16) & 3) == 3`），据此把本地 `render_engine` 的 `dmode==2`
分支去掉、改走普通 BG/OBJ 合成。结果：

* 帧 1900 输出**没有任何变化**（MAD 226.21、相同像素 50.0%，与改前一致）——
  因为本地 bank A 全 0，VRAM 显示与普通渲染都会得到黑屏；
* 但 `vramdisp bank0 red/blue、bank1 green` 三项单测失败 → 这条分支对应项目里
  另一处**已验收行为**。

因此**该试验已回退**（分支保留，注释里记录了这次实验与结论），当前 **907 项检查 0 失败**。

**2) 帧 1900 全量 IO 对照（当前唯一可信的差异清单）**

| 地址 | 参考核 | 本地 | 说明 |
|---|---|---|---|
| `0x04000004` | `0106000A` | `0000000C` | DISPSTAT（采样点：ref VCOUNT=262，本地 0） |
| `0x04000020/24` | `00000000` | `00000100 / 01000000` | BG2 仿射 PA/PB/PC/PD |
| `0x04000030/34` | `00000000` | `00000100 / 01000000` | BG3 仿射 PA/PB/PC/PD |
| `0x040000D8` | `020D3440` | `020BE240` | DMA2DAD |
| `0x04000130` | `000003FF` | `0000FFFF` | KEYINPUT 高 6 位（参考核 0，本地 1） |
| `0x040001A8` | `00000000` | `5E1600B7` | CARD_CMD（本地残留） |
| `0x040001C0` | `00000000` | `00008201` | SPI CNT |
| `0x04000204` | `00000000` | `0000E080` | EXMEMCNT |
| `0x04000214` | `00200000` | `00280000` | IF（本地多 bit19） |
| `0x04000320` | `0000002E` | `00000000` | melonDS 硬编码 46（46 = 0x2E） |
| `0x04000640/654/668/67C/680/690/6A0` | `00001000` | `00000000` | GX 区若干只读状态字 |

**下一步（优先级）**：

1. 把 `0x04000320 = 46` 与 GX 区那几个 `0x1000` 常量按 melonDS 补齐（低风险、纯寄存器口径）；
2. 补 `KEYINPUT` 高 6 位为 0（对照参考核 0x3FF）；
3. 查 BG2/BG3 仿射参数差异（参考核读回全 0、本地是单位阵）——它最可能解释
   帧 1900 那条青色竖条纹（BG 层的取样/掩码不同）。

### 21-B9xy（2026-09-12）：KEYINPUT 与 0x04000320 口径对齐

**1) KEYINPUT 高 6 位**

melonDS 的 `KeyInput` 只有低 10 位（A/B/Select/Start/右/左/上/下/R/L），
松开 = 1，**bit10-15 读回 0**；参考核帧 1900 无按键时读到 `0x03FF`，
本地此前是 `0xFFFF`（`key_set_pressed` 把高 4 位置 1）。

`src/io/key.c` 改为 `(~pressed) & 0x03FF`；4 条旧断言同步更新为新口径
（`0x03FF / 0x03FE / 0x03BD`）。

**2) `0x04000320` = 46**

melonDS `GPU3D::Read16/32` 对该地址**硬编码返回 46**（`// TODO, eventually`），
且该区间只有 ARM9 看得到。本地此前落到未映射返回 0 → 现在按同口径返回 `0x2E`。

**验证**：

```
tree 300 帧 --dump：KEYINPUT(0x04000130)=000003FF  ✓（参考核 0x3FF）
                    0x04000320 = 0000002E          ✓（参考核 0x2E）
test_nds.exe：907 项检查 0 失败
```

**剩余工作项（不变）**：GX 区矩阵只读寄存器（`0x04000640-0x040006A3`，melonDS 返回
ClipMatrix / Vector 矩阵，参考核帧 1900 对角线为 `0x1000`）、BG2/BG3 仿射参数差异
（参考核全 0、本地单位阵）——后者仍是青色条纹的最大嫌疑。

### 21-B9xz（2026-09-12）：BG2/BG3 仿射"差异"是 **melonDS 读路径缺口**（非分歧）

用 `--watch 04000020-04000038` 看本地的写入来源：

```
f=0  w32 a=04000020..04000034 v=0            pc=01FF81DC（ITCM 初始化，清零）
f=0  w16 a=04000020 v=0100 pc=02002A64       ← 游戏写 PA=1.0
f=0  w16 a=04000026 v=0100 pc=02002A6C       ← PD=1.0（BG2）
f=0  w16 a=04000030 v=0100 pc=02002A74       ← BG3 PA=1.0
f=0  w16 a=04000036 v=0100 pc=02002A7C       ← BG3 PD=1.0
```

即**本地这些值就是游戏自己写的单位阵**。再查参考核源码：

```
rg "0x04000020|0x04000022|0x04000026" melonds-ref/src/NDS.cpp   →  无任何匹配
```

melonDS 的 `ARM9IORead32` **根本没有 0x04000020 这段的分支**，所以 harness 用
32 位读拿到的是默认值 0 —— **参考核那两个 0 是它自己的读路径缺口，不是寄存器真值**。

因此这一项从"待修差异"里划掉（本地值才是对的）。

**当前帧 1900 真正剩下的差异项**：

1. `0x04000640-0x040006A3`（GX Clip/Vector 矩阵只读寄存器）：melonDS 有实现（返回矩阵
   元素），本地返回 0 —— 值得补（本项目 `gx` 里有 `proj/pos/tex` 矩阵）；
2. `0x040001C0` SPI CNT、`0x04000204` EXMEMCNT、`0x040001A8` CARD_CMD 残留、
   `0x04000214` IF bit19、`0x040000D8` DMA2DAD、定时器值 —— 多为 ARM7/ARM9 视角或
   状态残留，需逐项判断是否影响渲染；
3. **青色竖条纹本身**：两边 VRAM 逐字节一致、可比寄存器基本一致，说明差异在
   **2D 渲染算法**（BG 的 VRAM 映射解析/扩展调色板/取样），下一步改为**逐层对比**：
   用本地渲染器分别只开 BG0/BG1/BG2/BG3 渲染，和参考核在相同 DISPCNT 下的图层组合
   对照，找出是哪一层把"黑"变成了"青黑条纹"。

### 21-B9y0（2026-09-12）：条纹来源的两条线索与下一步实验设计

**线索 A：本地在帧 1900 走的是 mode2 的"VRAM 显示"分支**

本地 `DISPCNT=0x00121F10` → bit16-17 = 2 → `render_engine` 进 VRAM 显示分支，
读 bank A（全 0）→ 全黑。而 **melonDS 的 `GPU2D_Soft` 并不实现"VRAM 显示"模式**
（它按 BG mode 画 BG/OBJ；`UsesDisplayFIFO()` 只特判 mode 3），所以参考核在同一
`DISPCNT` 下画的是普通 BG 图层 → 出现青色条纹。

> 这条线索解释不了上一轮的实验结果（去掉 mode2 分支后画面没变）——说明**本地的普通
> 2D 路径在同一帧也是全黑**，还有第二处差异。

**线索 B：可能整个条纹来自 3D 层**

本项目 6000 帧的 `gx3d:` 统计一直是 **`tri=0`**——本地 3D 引擎**从未光栅化出三角形**，
而参考核（melonDS）同期有真实 3D 输出。若条纹是 3D 层内容，本地自然是黑的。

**下一步实验（按序执行）**：

1. 在参考 harness 里 dump **3D 层原始输出**（`GPU2D::Output3D` 或
   `GPU3D::GetLine()` 逐行拼 256×192），判定帧 1900 的条纹是否来自 3D 层：
   - 是 → 问题收敛到本项目的 GX/3D 光栅化路径（`tri=0` 说明几何根本没被画出来），
     回到 GX 命令解析 / 顶点装配环节排查；
   - 否 → 做 2D **逐层**对照（BG0..BG3 分别只开一层渲染，与参考核的图层组合比对）。
2. 无论走哪条，都顺带补齐 `0x04000640-0x040006A3`（GX 矩阵只读寄存器）——
   它可能是游戏用来判断 3D 状态、决定是否继续提交几何的关键读点（本地返回 0 会让
   游戏以为矩阵是零阵）。

### 21-B9y1（2026-09-12）：条纹不是 3D 层；16 位寄存器对照基本全同

**1) `NO3D` 实验：条纹与 3D 层无关**

给 melonDS 的 `SoftRenderer2D::DrawBG_3D()` 加了诊断开关（环境变量 `NO3D=1`
时直接 return），重跑参考核：

```
正常运行    ref_fb_1900：非零像素=24576  颜色数=2（0x0000 / 0xFF00）
NO3D=1 运行 ref_fb_1900：非零像素=24576  颜色数=2（0x0000 / 0xFF00）  ← 完全相同
```

即**禁用 3D 图层后帧 1900 画面毫无变化** → 那条"黑+青色 50% 竖条纹"**来自 2D 侧**
（BG/OBJ/backdrop），不是 3D。

**2) 16 位寄存器对照（melonDS 的 32 位读缺分支，必须用 16 位读）**

在 harness 里加了帧 1900 的 16 位寄存器 dump（`ref_reg16_1900.bin`，
0x04000000-0x0400007F 与 0x04001000-0x0400107F 各 128 字节）。引擎 A 对照结果：

```
04000004 ref=000A loc=000C   ; DISPSTAT（采样点：IRQ 使能位差异见下）
04000006 ref=0106 loc=0000   ; VCOUNT（ref 262 / 本地 0，采样点差异）
04000020/26/30/36 ref=0000 loc=0100  ; BG2/BG3 仿射（melonDS 连 16 位读也没实现 → 假 0）
其余 0x00-0x7F 全部相同（含 DISPCNT、BGxCNT、BLDCNT、WINxxx）
```

**3) 剩余唯一没比过的显存类数据：调色板**

两边的 VRAM（9 个 bank）逐字节一致、寄存器一致、3D 排除 ⇒ 下一个该比的是
**调色板 RAM（0x05000000，2KB）与扩展调色板**——当前 `--dump` 不含调色板，
参考 harness 也只 dump 过 `ref_pal_120.bin`（帧 120）。

**下一步**：

1. 给本地 `--dump` 增加调色板（主/副引擎各 512×2 字节）与参考核同帧 dump，
   对照帧 1900 的调色板；
2. 若调色板一致，则在本地渲染器里做**逐层二分**（分别禁用 BG0/BG1/BG2/BG3/OBJ，
   与参考核 `NO3D` 式的开关对照），定位是哪一层把"黑"变成"青黑条纹"。

### 21-B9y2（2026-09-12）：调色板也逐字节一致 ⇒ 问题锁定在**本地 2D 渲染器逻辑**

**1) 调色板对照**

给本地 `--dump` 增加了 `palette`（`bus->palette` 2KB），并让参考核在帧 1900 输出
`ref_pal_1900.bin`（melonDS `GPU.Palette` 2KB）。同帧对照：

```
调色板差异半字数 = 0 / 1024     （逐字节完全一致）
```

**2) 复核上一轮的 mode2 实验**

把"去掉 mode2 分支"那次实验的截图与改前截图直接对比：

```
loc1900k.bmp（分支在） vs loc1900q.bmp（分支已移除）：MAD = 0.00   （完全相同）
```

即**两条渲染路径在帧 1900 都输出全黑**（不是实验无效）。

**3) 结论**

帧 1900 的状态盘点：

| 项目 | 结果 |
|---|---|
| VRAM（9 个 bank） | 与参考核逐字节一致 |
| 调色板（2KB） | 与参考核逐字节一致（0/1024 差异） |
| 引擎 A 寄存器（16 位读，0x00-0x7F） | 一致（除 DISPSTAT/VCOUNT 采样点与 melonDS 自身读缺口） |
| 3D 图层 | `NO3D` 实验证明与条纹无关 |
| 本地渲染输出 | **全黑**（mode2 分支与普通路径都一样） |

⇒ 差异**只在本地 2D 渲染器的取数逻辑**（BG 的 screen base / char base / 尺寸 /
调色板寻址），不在内存或寄存器状态。

**下一步**：给 `render_tiled` 加一次性诊断——对每个使能中的 BG 打印
`screen_base = ((BGxCNT>>8)&0x1F)*0x800`、`char_base = ((BGxCNT>>2)&0xF)*0x4000`、
尺寸、以及第一个 tile 的取址/取色结果；与 melonDS `GPU2D_Soft::DrawBG_Text` 的同口径
公式逐项比对，找出本地把哪一层的取数地址算到了空区域（从而全黑）。

### 21-B9y3（2026-09-12）：**渲染时刻的 DISPCNT 与 dump 不一致**（方法论问题）

诊断已实现（`NDS_BGDBG=1` 时每个引擎打印一次 render 时刻的 DISPCNT 与 4 个 BGxCNT）：

```
bgdbg: engine=0 dispcnt=80111418 mode=0 bgcnt=0003/0006/2185/0204
bgdbg: bg=2 prio=1 bgcnt=2185 char=06004000 map=06000800 e0=0000 e1=0001 256c=1 ext=0
bgdbg: engine=1 dispcnt=40011430 mode=0 bgcnt=0003/020E/038D/0404
bgdbg: bg=2 prio=1 bgcnt=038D char=0620C000 map=06201800 e0=0000 e1=0001 256c=1 ext=1
```

同一帧的 IO dump 对照：

| 来源 | 引擎 A DISPCNT |
|---|---|
| 参考核帧 1900 dump | `00121F10`（mode 2、BG0-3+OBJ 全开） |
| 本地帧 1900 **dump** | `00121F10`（与参考一致 ✓） |
| 本地帧 1900 **render 时刻** | **`80111418`（mode 0、只开 BG2+OBJ）** |

即：**游戏一帧内多次改写 DISPCNT，而本地"截图渲染"与"dump"发生在不同时刻**
（截图在 `runner_headless_frames` 内部、dump 在返回之后），两处寄存器状态不同。
这解释了"只看到 BG2 参与合成"——那是截图时刻的状态，不是 dump 时刻的状态。

**方法论修正（下一步第一步，优先级最高）**：

1. 把本地**截图与 dump 对齐到同一时刻**（同一函数内固定顺序），并在截图时打印所用
   DISPCNT；
2. 参考核是"RunFrame 之后"取图，本地也应固定在**帧边界之后**渲染；
3. 对齐后重跑 1900 帧逐像素对照——在此之前，之前关于"奇偶列/条纹"的所有结论都要
   在这个前提下重新确认（很可能其中一部分是采样时刻差异，而非渲染 bug）。

### 21-B9y6（2026-09-12）：**本地数据解析出来就是青蓝色** ⇒ 问题在合成之后

更正上一轮的两点（已核实）：

1. **不存在运行间不确定性**：同参数两次 1900 帧运行的 `mainram/arm7wram/vram` 逐字节
   相同，`io9` 只差 `0x04000130` 与 `0x04000320`——正是两次运行之间所做的两处修复
   （A = 修复前二进制、B = 修复后）；300 帧两次运行也完全一致。
2. **帧 1900 两边 DISPCNT 都是 `80111418`**（mode 0、只开 BG2+OBJ）；此前"本地
   dump = 0x00121F10"是把**帧 2200** 的数据误当成帧 1900。

**关键一步：直接查表（不用再跑模拟器）**

帧 1900 的调色板 dump 已在手（`loc1900r_palette.bin`，与参考核逐字节一致），
把 BG2 tile0 的字节按 256 色调色板查表：

```
tile0 bytes = 8B 81 6C 6C 78 90 8A 97
  idx=8B -> pal=62AF  RGB(123,173,197)
  idx=81 -> pal=5A8E  RGB(115,165,181)
  idx=6C -> pal=564C  RGB( 99,148,173)
  idx=78 -> pal=5A6D  RGB(107,156,181)
  idx=90 -> pal=5E8F  RGB(123,165,189)
pal[0]=001F（红）、pal[1]=0000（黑）、pal[2]=0421、pal[3]=0842
```

即：**本地这层的数据+调色板解析出来正是参考核那种青蓝色调**（RGB≈(120,170,190)），
所以"全黑"**不是取址/取色问题**，而发生在之后：

1. `draw_bg()` 里 256 色路径的像素写入被跳过；
2. `comp_put()` 写入后被后续图层（OBJ，DISPCNT bit12 开着）覆盖；
3. `comp_finalize()` 的窗口/混合/亮度（MASTER_BRIGHT）把画面压黑。

**下一步（一次定位）**：在渲染管线各阶段后各打印同一采样点 `fb[100*256+50]`：
`render_tiled` 之后、`render_obj` 之后、`render_3d` 之后、`comp_finalize` 之后——
哪一步开始变黑，问题就在那一步。

### 21-B9y7（2026-09-12）：**"全黑"来自 MASTER_BRIGHT，且参考核 dump 是加亮度之前的画面**

**1) 阶段诊断结果**

```
bgdbg: stage=tiled eng=0 px=D35FEB20     ← 合成累加器里是颜色（低 16 位 0xEB20 = RGB(0,200,208) 青）
bgdbg: stage=obj   eng=0 px=D35FEB20
bgdbg: stage=3d    eng=0 px=D35FEB20
bgdbg: stage=final eng=0 fb=FF000000     ← comp_finalize 之后变黑
```

即：**BG2 的数据确实画进了合成器（青色），最后一步 `comp_finalize` 把它变成了黑**。

**2) 原因：MASTER_BRIGHT（0x0400006C）= 0x8010（两边一致）**

```
MASTER_BRIGHT: ref=8010  loc=8010     ; bit14-15 = 10b = 减暗模式, 因子/16 = 16 → 全黑
BLDCNT=2142/2142  BLDALPHA=040C/040C  WININ=0000/0000  WINOUT=0000/0000
```

**melonDS 的 `GPU.GetFramebuffers()`（harness dump 用的）返回的是"加主亮度之前"的
合成结果**（主亮度在 melonDS 里由前端在显示阶段套用），而本地 `render_frame` 把主亮度
做进了输出。于是出现"参考核有画面（青条纹）、本地全黑"的**假分歧**。

实验：给本地加了 `NDS_NOMB=1`（跳过主亮度）后画面确实变了（不再是全黑），
但与参考核 dump 仍不重合（MAD 390、相同像素 0%）——说明**除了主亮度之外，
2D 混合（BLDCNT/BLDALPHA，melonDS 在 2D 渲染阶段就应用）等效果也需要对齐口径**。

**结论 / 下一步（比较方法必须修正）**：

1. 比较时要么**在参考核侧把主亮度套用到 dump 上**（与硬件显示阶段一致），
   要么在本地输出"未套主亮度"的中间结果；两者必须选其一并固定；
2. 之前所有"奇偶列/条纹"的对比结论都要在**同一效果口径**下重做；
3. 主亮度/混合的真实实现差异要单独验证（本地 `blend_bright` 的 6bit 公式与 melonDS
   `MasterBrightness` 的实现逐行对照）。

### 21-B9y8（2026-09-12）：**对照工具重大错误更正——参考核 framebuffer 是 32 位/像素**

参考 harness 的 dump 是 `fwrite(top, 1, 256*192*4, f)`，即 **每像素 4 字节
（0xAABBGGRR 风格：低字节=R）**。而我在 21-B9xo~B9y7 的多次画面对照里**按 16 位/像素
读**，于是把 `0xFF000000`（不透明黑）拆成两个半字 `0x0000` 与 `0xFF00`，
误当成"黑 + 青色 #00C5FF 的 50% 竖条纹"。

按正确读法复核：

```
ref_fb_1900（32 位读）：颜色数=1，前 1 色 = 0xFF000000 × 49152   → 整屏纯黑
ref_fb_1800（32 位读）：颜色数=161（正常画面）
ref_fb_2200（32 位读）：颜色数=350（正常画面）
```

而本地帧 1900（套主亮度后）也是整屏纯黑 ⇒ **两边本来就一致**。

**因此以下结论作废（均为工具错误导致）**：

- "帧 1900 参考核是黑+青色条纹、本地全黑"；
- "偶数列 100% 一致、奇数列 0%";
- 以及基于 16 位读得到的所有 MAD/时间线对比数字。

**仍然成立的部分**：

- `MASTER_BRIGHT=0x8010`（减暗 16）→ 帧 1900 两边都应为纯黑 ✓（本地行为正确）；
- melonDS 的 `GPU.GetFramebuffers()` 不含主亮度（前端才套用）——比较时仍需注意口径
  （本地 `NDS_NOMB=1` 可用于对齐）；
- 前面几轮关于寄存器口径的修复（KEYINPUT 高位、`0x04000320`、`DISP3DCNT` W1C、
  GBA 槽 0xFF 等）与单测都仍然有效（与画面读法无关）。

**下一步（重新做画面对照）**：

1. 统一用 **32 位/像素** 读参考 framebuffer，并把本地 BMP 的 RGB 与之逐像素比；
2. 输入对齐（参考核从帧 1700 注入按键）；
3. 从**最早出现差异的帧**开始追（先用 `tl_*` 时间线与 `ref_fb_*` 逐帧比，找出第一个
   MAD 明显跳变的帧），再回到那一段的寄存器/内存对照。

**修正读法后的首轮时间线对照（本地 `tl_*` 无按键 vs 参考同帧）**：

| 帧 | 本地均值亮度 | 参考均值亮度 | 备注 |
|---|---|---|---|
| 500 | 238.6 | 205.3 | 都是亮屏（标题/开场） |
| 1000 | 138.0 | 137.4 | **亮度几乎相同** |
| 1500 | 221.1 | 141.1 | 明显不同（此时双方都还没按键） |
| 2000 | 220.8 | 0.0 | 参考全黑、本地亮屏（参考已从 1700 按键，本地没按 → 输入不对齐） |
| 2500 | 130.4 | 175.0 | 同上 |

注意：本轮 `tl_*` 是**无按键**运行、且**含主亮度**；参考核从 1700 起注入按键、
dump 不含主亮度 ⇒ **1700 之后不可比**，1500 之前的差异才是真实候选。

**下一步（具体）**：

1. 用 `--key-frame 1700 --key-mask 0x3F7 --key-period 120` + `NDS_NOMB=1`
   重跑时间线（与参考核输入/效果口径完全一致），再逐帧对照；
2. 重点看 **1000-1700** 这一段：亮度接近但逐像素 MAD 仍大（0% 相同），需要判断是
   "同一画面但描画细节不同"（例如 BG 混合/扩展调色板），还是 BMP 的通道顺序问题
   （已排除简单 R/B 交换：交换后 MAD 不变）；
3. 定位到具体差异后再回到寄存器/内存对照。

### 21-B9y9（2026-09-12）：对齐输入+NOMB 的逐帧对照 ⇒ 早期帧就有真实分歧

用 `--key-frame 1700 --key-mask 0x3F7 --key-period 120` + `NDS_NOMB=1` +
`--shot-every 100` 重跑 1900 帧，19 张截图与参考核同帧（32 位读法）对照：

```
f=100  MAD= 59.1   f=400  MAD=137.4   f=1000 MAD=150.9   f=1500 MAD=242.6
f=200  MAD= 59.1   f=600  MAD=266.9   f=1200 MAD=242.6   f=1700 MAD=259.6
…   全部"相同像素"≈0%
```

**采样点分析（f=100）揭示了两种成分**：

```
( 10, 10) 本地(F8,F8,F8) 参考(FB,FB,F B)     ← 背景：每通道差 ~3（RGB555→888 换算方式不同）
(200,150) 本地(F8,F8,F8) 参考(FB,FB,FB)
(100,100) 本地(E8,C0,98) 参考(20,20,20)      ← 内容：本地亮褐色 vs 参考深灰，差异巨大
(128, 96) 本地(E0,B8,80) 参考(20,18,20)
```

1. **色彩换算差异（系统性的）**：本地 `rgb555_to_888` 用位扩展（31→0xF8=248），
   melonDS 得到 251——每通道差几个单位就让"严格相同"的判据全部落空，污染了所有
   MAD/相同率统计。**先把本地换算改成与 melonDS 一致**（5bit→8bit 的正确映射），
   后续对照才有意义。
2. **真实内容差异**：帧 100（**很早、很稳定**）本地把某层画成了亮褐色，参考是深灰。
   这比之前追的 1900 帧更适合定位——是一个**早期、可复现**的分歧点。

**下一步**：

1. 修 `rgb555_to_888`（对齐 melonDS 的换算）→ 重跑逐帧对照，确认"背景差 3"消失；
2. 用 `NDS_BGDBG` 在帧 100 打印各层取址/取色，定位"亮褐色"是哪一层画出来的
   （候选：BG2 的 256 色/扩展调色板路径、或 OBJ 层）。

### 21-B9yb（2026-09-12）：色彩换算修正**已验证有效**，但测试期望需逐条更新（本轮先回退）

**1) 修正公式（对齐 melonDS 的 5bit→6bit→8bit 路径）**

melonDS 软渲染内部每通道 6bit：`c6 = c5 << 1`，再 `c8 = (c6 << 2) | (c6 >> 4)`，
等价于 **`c8 = (c5 << 3) | (c5 >> 3)`**（白 31 → `0xF8|0x03 = 0xFB`）。
本地旧实现是 `c5 << 3`（白 → 0xF8），每通道差 1-3 个单位。

**2) 验证（帧 100，32 位读法）**

```
换算修正前：MAD=59.1   相同像素=0.0%
换算修正后：MAD=51.6   相同像素=81.9%   ← 背景色差消失，只剩真实内容差异
```

**3) 为什么本轮**先回退**这个改动**

修正后有 28 条单测失败（期望值编码的是旧换算）。用脚本批量把 `0xFFxxxxxx` 字面量
按 `v|v>>6` 重写后：

- 15 条恢复正常（纯色期望，换算公式成立）；
- 但**另有 13 条被误伤**——它们不是"直接由 5bit 色转换来的输出"：
  `bhw LDRSB / LDRSH / thumb NEG`（CPU 符号扩展的值，如 `0xFFFFFF80`）、
  `alpha blend red+blue`（混合后再换算，`0x78` 而非 `0x79`）、
  `bright up/down gray`、`master bright gray`（亮度运算后再换算）。

⇒ 批量替换不可靠，**现已把 `src/ppu/render.c` 与 `tests/test_nds.c` 都恢复到 HEAD**
（`git diff` 干净、**907 项检查 0 失败**），把这项改动留给下一步逐条处理。

**下一步（明确的收尾工作）**：

1. 在 `rgb555_to_888` 应用 `(v<<3)|(v>>3)`；
2. 逐条核对 28 条受影响断言：
   - 纯色期望 → 按新公式改（白 0xFB、红 0xFB0000、绿 0x00FB00、蓝 0x0000FB…）；
   - **CPU 符号扩展类**（`bhw LDRSB/LDRSH`、`thumb NEG`）**不改**；
   - **混合/亮度类**（`alpha blend red+blue`、`bright up/down gray`、`master bright gray`）
     用实际的混合+换算结果更新（应比旧值 +1 左右，需逐条计算确认）；
3. 重跑帧 100 对照，确认"相同像素 ≥80%"，再继续查剩下的 18% 内容差异。

### 21-B9yc（2026-09-12）：色彩换算落地 + 28 条期望逐条更新 ⇒ 帧 100 相同像素 81.9%

**改动**（B9yb 遗留收尾）：

1. `src/ppu/render.c` 的 `rgb555_to_888` 改为 `(c5 << 3) | (c5 >> 3)`（等价于
   melonDS 的 5bit→6bit→8bit 路径：`c6 = c5 << 1`、`c8 = (c6 << 2) | (c6 >> 4)`），
   白 31 从 0xF8 变成 0xFB。
2. `tests/test_nds.c` **逐条**更新 28 条受影响断言。这次先跑测试拿到失败清单，
   确认失败项**全部是纯色期望**（无 CPU 符号扩展、无混合/亮度项——它们在本轮
   实现下本来就不再走 5bit 直转路径），再按
   `0xFFF80000→0xFFFB0000`、`0xFF0000F8→0xFF0000FB`、`0xFF00F800→0xFF00FB00`、
   `0xFF00F8F8→0xFF00FBFB` 精确替换：文件里这 4 个字面量**各出现次数 = 失败条数**
   （28 = 28），替换范围可证无副作用（B9yb 的批量脚本误伤 13 条的问题不复现）。

**验证**：

- `build\test_nds.exe` → `=== 共 907 项检查，0 项失败 ===`（回到全绿）；
- 帧 100 逐像素对照（本地 BMP 与参考核 `ref_fb_100.bin` 的**顶屏 32 位**读法、
  `NDS_NOMB=1`）：

```
修正前：same =  0/49152 (  0.0%)   MAD = 59.1
修正后：same = 40271/49152 ( 81.9%)   MAD = 51.50
```

  与 B9yb 实测（81.9% / 51.6）一致，说明本轮没有引入回归，且换算修正的效果可复现。
- 对照脚本落在 `build/cmpfb.py`（build/ 不入库）：读 24 位 BMP 的顶屏 192 行，
  与参考核 32 位/像素的 `ref_fb_<frame>.bin` 逐像素比，打印相同像素数与 MAD。

**下一步**：追帧 100 剩下的 ~18% 内容差异（本地亮褐 vs 参考深灰）——用
`NDS_BGDBG=1` 在帧 100 打印各层取址/取色，判断是 OBJ 层还是 BG2 的
256 色/扩展调色板路径（`ext` 位 = DISPCNT bit30）。

### 21-B9yd（2026-09-12）：ARM7 低地址换装完整 FreeBIOS 镜像 + 未建模函数体真地址执行

**背景**：B9ws 的「参考级 HLE」只影子化向量表与 SWI 表，其余低地址读 0；
未被逐条建模的 SWI（Div/CpuSet/CRC16/音频查表…）在分发器里调 C 版 HLE 算完
直接跳 `swi_complete`——结果对，但 **BIOS 内部的 PC/lr/栈帧/周期轨迹与参考核
不同**，IRQ 落在函数体内部时保存的现场也就不一样（FFXII 的 ARM7 调度器会把
这些现场当任务上下文存下来）。

**改动**：

1. `src/bios/bios7_rom.h`（生成文件）+ `bios7_image.c`：完整 0x4000 字节
   FreeBIOS ARM7 镜像（BSD-2，来源同 melonDS 参考核；有效 0x2030，其余补 0），
   低地址任意一读都返回参考核的字节；
2. `bios7_low.c`：未建模的 SWI 函数体**在真地址上执行真实字节**（C 版 HLE
   只在镜像缺失时兜底）；
3. 统计设施 `bios7_low_stats_t`（SWI 直方图 / 真地址执行数 / HLE 兜底数 /
   未建模取指数）。

**实证与口径纠正（两条测试期望改写）**：

- ARM7 `SWI 0x0E GetCRC16` 真执行 8 字节 "12345678"(init=0xFFFF) → **0x37DD**，
  等于独立实现的 CRC-16/ARC ⇒ 证明本地把真实字节码算对了；且 FreeBIOS 的
  实现按**半字**推进（⌊n/2⌋ 轮），奇数字节长度的尾巴被忽略；
- ARM7 `SWI 0x08 SoundBias` 真字节码**不看 r0**：当前电平非 0 就写回 0x200
  （旧 C 版 HLE 是按 r0 写），测试改按 FreeBIOS/参考核口径断言。

**验收内容**：`build\test_nds.exe` → `=== 共 909 项检查，0 项失败 ===`
（B9yc 后新增/改写：CRC16 ARM7 两条 + SoundBias 一条）；
真 ROM 900 帧 `ARM7=00001158`、`disp=00161F10`、`0x027FFC30=0xFFFFFFFF`。

### 21-B9ye（2026-09-12）：分发器逐条化 + 与参考核逐地址对照（93/93 一致）

**做法**：给两边核心加低地址取指直方图（本地 `NDS_BIOS7_HIST=1`；
参考核在 `ARM.cpp` 的 ARM7 执行循环里对 `_pc < 0x4000` 计数并 dump
`hist7: pc=… count=…`），跑同样帧数后逐地址比对。

**首轮（6000 帧）发现**：本地缺 0x1084-0x10A8 共 10 个字——旧实现把 SWI
分发器 0x1080 整段合成一步执行，PC 直接跳到函数体；其余 83 个地址完全一致。

**修正**：把分发器拆成 11 条独立指令等价（每条 1-4 周期），于是
`msr cpsr_fc`（0x1098）之后、I 位取自调用方 SPSR 时，IRQ 能像参考核一样
插在 0x109C-0x10A8 之间。

**修正后对照（900 帧，本地 vs melonDS+FreeBIOS）**：

```
distinct ref=93 loc=93   集合完全相同（only-ref / only-loc 均为空）
0x18 IRQ 向量 / 0x1FB0-0x1FC4 出入口：ref=11237  loc=11267  (+0.3%)
0x11xx Halt 体：                      ref=10592  loc=10889  (+2.8%)
0x1368 CRC16 循环：                   ref=112    loc=112    (一致)
0x08  SWI 向量 / 0x1080-0x10A8 分发器：ref=39823  loc=42890  (+7.7%)
```

**验收内容**：

1. `build\test_nds.exe` → `=== 共 921 项检查，0 项失败 ===`
   （分发器逐条化后 `[case 21-B9wt]` 从 1 步断言扩展为 11 步逐条断言）；
2. `build\nds-emu.exe tools\rom_ascii.nds --headless-frames 900`（`NDS_BIOS7_HIST=1`）
   → `bios7-hist(end): words=93`，且这 93 个地址与参考核 `hist7` 集合相同；
3. 帧 100 顶屏逐像素对照（`NDS_NOMB=1`，脚本 `build/cmpfb.py`）→
   `same = 40271/49152 (81.9%)   MAD = 51.50`（与 B9yc 一致，无回归）；
4. 900 帧终点：`ARM7 PC=00001158`、`disp=00161F10`、`gx=2595`、
   `irq7=11267`（参考核同帧 11,237 次，差 0.3%）。

**余下的已知差异（下一步线索）**：本地 ARM7 的 SWI 调用次数略多且随时间累积
（900 帧 +7.7%、6000 帧 +33%），来源几乎全是 `0x1CA0 GetPitchTable` /
`0x1FC8 GetVolumeTable` 这一对音频查表（6000 帧：本地 235,937 vs 参考 168,298）。
即「游戏侧声音/音乐节拍跑得比参考快」——属定时器/声音推进口径分歧，
与低地址路径本身无关，留作下一微步的入口。

### 21-B9yg（2026-09-12）：ARM9 时钟折算对齐 ⇒ 时间线差从 ~240 帧缩到 ~7 帧

**从「帧 100 内容差异」追到根因**：帧 100 有 18.1% 像素不同，差异集中在一个
矩形区（x[15..241] y[36..153]）、颜色对是「本地米色 ↔ 参考白」「本地白 ↔
参考深灰」——像同一画面处在不同动画相位。于是逐帧比对早期画面：

| 帧 | 本地（旧口径） | 参考核 |
|---|---|---|
| 10 | 100% 相同（都是纯白开机屏） | 100% |
| 20 | 已有内容（143 色） | 仍纯白 |
| 40 | 已有内容 | 仍纯白 |
| 50+ | 81.9% 相同（差一帧相位） | — |

⇒ 本地开机比参考核**早约 30 帧**，根因不是渲染，而是**时间线**：

```
第 8 帧 ARM9 累计指令：本地 7,579,029  vs  参考核 3,370,741   （2.25×）
第 26 帧 ARM9 累计指令：本地 16,332,769 vs  参考核 8,328,123   （1.96×）
```

melonDS 的记账是 **每条 ARM9 指令 ≈ 1 个系统时钟单位（33.513982MHz）**
（另有按区域的 `MemTimings` 取指代价），而本地把 `cost9`（每条指令 1 个
「硬件周期」）按 `/2` 折算成系统单位 ⇒ 等于每系统单位跑 2 条指令。

**改动**：`src/runner/runner.c` 引入 `RUNNER_ARM9_DIV`（默认 **1**；
`NDS_ARM9_DIV=2` 可切回旧口径做 A/B），三处折算（步进判据、单核等待时的
时间推进、唤醒补偿）统一使用；新增模块日志 [runner](src/runner/runnerlog.md)。

**验证（对齐效果）**：

| 观测点 | 旧口径 | 新口径 | 参考核 |
|---|---|---|---|
| 开显示（DISPCNT≠0）帧 | ~13 | 30 | 26 |
| `80111418` 帧 | ~13 | ~35 | 43 |
| `00111418` 帧 | ~300 前 | 530 | 539 |
| `00161F10`（标题）帧 | ~300 | 535 | 542 |
| 帧 10/20/30 逐像素 | 20 起分歧 | **全部 100% 相同** | — |
| 帧 1500 OCR | 与参考不同 | 同 `ILLUSTRATION RYOMA ITO` | — |

**验收内容**：

1. `build\test_nds.exe` → `=== 共 921 项检查，0 项失败 ===`（runner 改动不涉及单测路径）；
2. 逐帧画面对照：`NDS_NOMB=1 build\nds-emu.exe tools\rom_ascii.nds --headless-frames 30
   --shot-every 10 --shot-prefix build\b9yg` 后，把 `b9yg_00010/20/30.bmp` 与
   参考核 `ref_fb_10/20/30.bin` 用 `build/cmpfb.py` 比 → 三个都是
   `same = 49152/49152 (100.0%)  MAD = 0.00`；
3. 真 ROM 900 帧：`ARM7 PC=00001158`、`disp=00161F10`、
   `0x027FFC30=0xFFFFFFFF`、`0x03808240=1`、`0x0380BA7C=1`（与参考核一致）；
4. 1900 帧 OCR（`build/shot2png` + `build/ocr2.ps1`）：本地第 1500 帧与参考核
   第 1500 帧识别出同一句 `ILLUSTRATION RYOMA ITO`。

**下一步（可选细化）**：本地 ARM9 在第 26 帧仍执行 12.07M 条指令（参考核
8.33M，多 45%）——melonDS 会按取指区域加 `MemTimings` 代价（主存
`N16=8,S16=1`、ITCM/IO/WRAM 等各有基准 + 非主存 3 周期罚），本地尚未实现
「按区域计费」。做完可进一步压缩剩余时间线差。

### 21-B9yh（2026-09-12）：按取指区域计费 ⇒ ARM9 吞吐对齐到 −2.3%/−7.2%

**做法**：先量口径、再选模型。三套口径在同一 ROM、同一帧数下的 ARM9 指令数：

| 口径 | 第 26 帧 | 第 1900 帧 |
|---|---|---|
| 参考核（melonDS+FreeBIOS） | 8,328,123 | 720,588,787 |
| 本地：每条指令 1 单位 | 12,065,443（+45%） | 784,957,288（+8.9%） |
| 本地：主存一律 2 单位（试验） | 7,112,902（−15%） | 618,299,382（−14.2%） |
| 本地：**主存非顺序取指 3 单位**（落地） | **8,138,527（−2.3%）** | **668,450,486（−7.2%）** |

差额集中在启动期（ARM9 代码在主存），长程只差 8.9% ⇒ 参考核的口径是
「顺序取指便宜、非顺序取指贵」（melonDS ARM9 主存 `S16=1`/`N16=8`，
ARM 态 `S32=2`/`N32=9`），不是「主存一律贵」；直接按 melonDS 的 N16=8
取值更会过头（26 帧只剩 182 万条指令）。

**实现**：`arm_cpu_t.next_fetch_pc`（顺序取指判据，`cpu_reset` 初始化）+
`cpu_fetch_cost()`（ARM7 恒 1；ARM9：ITCM 1、其它区域 1、主存非顺序 3），
`cpu_step` 执行后取 `max(step_cycles, fetch_cost)`。

**验收内容**：

1. `build\test_nds.exe` → `=== 共 926 项检查，0 项失败 ===`
   （`[case 21-B9vv]` 增加 4 条取指计费断言）；
2. 吞吐对照：`build\nds-emu.exe tools\rom_ascii.nds --headless-frames 26`
   末行 `hot9: total=` 应 ≈ 8.1M（参考核同帧 8.33M）；
3. 时间线：`--dump` 逐帧取 DISPCNT，开显示应在第 27 帧前后（参考核 26）、
   标题 `00161F10` 在第 555 帧前后（参考核 542）；
4. 里程碑：900 帧 `ARM7=00001158`、`0x027FFC30=0xFFFFFFFF`、
   `0x03808240=1`、`0x0380BA7C=1` 与参考核逐项一致。

### 21-B9yi（2026-09-12）：卡带真机语义 + 预取 FIFO ⇒ 游戏状态首次与参考核长程同步

**动机**：按键离开标题后（第 1900 帧起）本地两核卡在 IRQ 模式、ARM9 停在
`0x02011C54`（游戏「写卡带命令前等 ROMCTRL 空闲」的轮询循环）。逐事件对照
参考核后锁定三个口径差异（详见 [cartlog](../src/cart/cartlog.md) 21-B9yi）：

| 项 | 参考核（melonDS） | 本地旧实现 |
|---|---|---|
| ROMCTRL bit31/29/23 | 对软件**只读**（写掩码 `0xFF7F7FFF` + 强制旧值） | 原样写入（“写 1 就忙”） |
| 启动传输条件 | bit31 **0→1** 且 AUXSPICNT 使能(bit15)/ROM 模式(bit13=0) | 写高字节带 bit7 就重启 |
| 数据通路 | 卡带侧按周期**预取进 2 字 FIFO**，读空 + 到块尾才结束 | CPU 读一次才取一次、读不满整块不结束 |

**改动**：`src/cart/cartbus.c/.h`（只读位/0→1 启动/AUXSPICNT 门控 + `data[2]`
预取 FIFO + 结束条件）、`src/io/io.c`（传输结束中断转两核 IRQ）、
`src/cpu/cpu.c`（取指代价默认值见下）。

**顺带定标：ARM9 主存非顺序取指代价**。B9yh 引入的“非顺序取指 3 单位”
按**指令数**校准（26 帧 −2.3%、1900 帧 −7.2%），但按**时间线**校准结果完全不同：

```
读卡带进度对齐（同一读取序号对应的帧号差，负=本地超前）
     读取序号   noseq=3   noseq=2   noseq=1
       200000      +11       -1        -13
       350000     +110      +49         -8
       500000     +160      +74         -9
```

⇒ 默认改为 **1**（`NDS_ARM9_NOSEQ` / `cpu_set_nonseq_cost()` 可调）：
本地在整个读卡带阶段与参考核稳定在 **−8~−13 帧**，这是本项目至今最长的
「游戏状态与参考核同步」区间。

**验收内容**：

1. `build\test_nds.exe` → `=== 共 929 项检查，0 项失败 ===`
   （新增卡带只读位/门控/预取/代价可调断言）；
2. 命令流与数据流对照：本地卡带命令前 5388 条、CARD_DATA 前 500,905 次读取
   与参考核**逐条/逐值一致**（`NDS_CARTLOG=1` 与参考核 `REF_CART_LOG` 的可比日志）；
3. 时间线：`build\nds-emu.exe tools\rom_ascii.nds --headless-frames 1540
   --watch-r 04100010-04100013` 的读取序号-帧号曲线与参考核相差 −8~−13 帧；
4. 里程碑不回归：900 帧 `ARM7=00001158`、`disp=00121F10`、
   `0x027FFC30=0xFFFFFFFF`。

**仍未解决（下一步入口）**：第 1900 帧附近本地仍会停在该轮询循环；已知线索是
本地在该段出现「连续两次 B8 芯片 ID 传输」的异常序列、随后某次 512 字节传输
只读了 127 字就被放弃；参考核同段正常。怀疑方向：ARM9 IRQ 在卡带读循环中途
插入时的现场/恢复与参考核不一致。

**21-B9yi（续）— 用「发起者 PC」把分歧精确到帧**（诊断设施：
`NDS_CARTLOG=1` 时 `io_write8` 打印每次 ROMCTRL 高字节写的
`val/romctrl/pc/lr/sp`；参考核 `NDS.cpp` 侧同口径打印 `Parent.NDS.ARM9.R[15]`）：

```
参考核（帧 1370-1400，等价逻辑段）：每次传输启动只有两个来源
  pc=0201191C lr=0201190C   块读函数（每块一次 B7）
  pc=0201187C lr=02011854   芯片 ID 助手（每块一次 B8）

本地（帧 ≤1900，全量）：
  第 1887 帧 —— 连续两次 B8（芯片 ID 校验失败 → 重试）  ← 第一次异常
  第 1888 帧 —— 唯一一次 IRQ 上下文发起（sp=027E39F8）
                pc=02011C38 lr=02011C24（写回 [0x027FFE64] 的 ROMCTRL 值）
  之后 —— 卡在该函数的「等 ROMCTRL 空闲」轮询；ARM9 cpsr=A00000D2（IRQ 模式）
```

参考核同段 ARM9 始终在 System 模式（`cpsr=...1F`）。⇒ 分歧链：
**芯片 ID 校验失败 → 重试 → 在上一笔块传输仍有未读数据时发起新命令 →
bit31 永不回落**。下一步对照「ARM9 IRQ（VBlank 等）插入相位」以及
芯片 ID 校验为何失败（读到的字是否被上一笔传输的残留影响）。

**同轮口径修正**：卡带时钟从「每条指令固定 1 周期」改为「按上一条指令
实际消耗的周期数推进」（`io_advance_cart(io, is_arm7, cycles)`）。实测读卡带
进度曲线不变（该阶段瓶颈在 CPU），但系统时钟口径与 melonDS 一致。

**状态级对照（分歧前）**：两边都在**第 1885 帧**（按键注入后、卡死前）dump
主存（参考核 `REF_DUMP_RAM=1885`、本地 `--dump`）：

```
主存逐字比较：差异 23,003 / 1,048,576 字 = 2.19%
差异集中在游戏任务表/调度器结构（0x02076FEC、0x02077010、0x020772xx-0x020773xx）
与几个工作计数器（如 0x02077320 本地 0x7FF vs 参考 0x7EA，+0.5%）
```

⇒ 游戏状态「只差 2.2%」，且差距是**同一逻辑只差半步**（计数器略超前），
不是逻辑走错分支。卡死属于「任务调度相位差导致的竞争」：本地在块读进行中
被 IRQ 插入、任务在同一笔卡带传输上叠用命令。

**下一步（明确）**：对照「同一个 VBlank/IRQ 在两边分别插在哪条指令之间」
（本地卡死在 IRQ 模式、参考核同段始终 System 模式），把 IRQ 受理时刻做成
可比诊断后逐帧对齐；另核对硬件定时器分频（TIMERxCNT）与 VBlank 相位是否一致。

**21-B9yi（续3）— 卡带中断风暴修复（死锁解除）**：对照发现根因是**中断语义**：
melonDS 的 `RaiseDRQ()` 只置 ROMCnt bit23 并触发 DMA，**不挂中断**；
卡带中断只在**传输结束**（`ROMEndTransfer`，AUXSPICNT bit14 使能）时挂出。
本地此前「每取一个字就挂 IF bit19」⇒ ARM9 每 ~170 周期被中断一次
（实测空闲期每帧 6 次 `0x02009580` 处的 IRQ），任务调度相位被打乱，
最终在卡带轮询里死锁。

修正后（新增 `NDS_IRQLOG` / `NDS_MODELOG` 诊断佐证）：

```
帧 1900-1905：ARM9=0x0200957C（空闲任务）cpsr=0000001F  if9=0x00200000  fifo=0/0
             —— 与参考核同帧「ARM9=0x02009584（空闲）」形态一致，不再死锁
```

里程碑不回归（900 帧 `0x027FFC30=0xFFFFFFFF`、`0x03808240=1`、
`0x0380BA7C=1`）；单测 **930 项 0 失败**（新增 DRQ 不挂中断 / 传输结束挂中断两条）。

**新的卡点（下一步）**：本地第 1900 帧之后停在空闲任务**不再恢复**
（参考核此时继续跑任务：`02007F84`/`0200EA5C`/ITCM/`02085614`），
第 2000 帧主存差异 37%（第 1885 帧只有 2.19%，说明分歧发生在 1885-1900 之间）。
已知线索：游戏的内核在 IRQ 里做任务调度（ITCM `0x01FF8000` 的中断分发器
`clz` 取优先级 → 清 IF → 跳处理器表），本地只观察到「空闲任务 ↔ IRQ」的
模式往返，没有切到其它任务。下一步对照参考核的 IRQ 处理器表/任务链表状态。

**21-B9yi（续4）— 模式级对照：本地其实和参考核同步到了第 1902 帧**：

给参考核也加了模式切换日志（`ARM::UpdateMode` 内，`REF_MODELOG=1`）后逐条对照：

```
参考核 1889-1902：只有「空闲任务(02009584) ↔ IRQ(FFFF06FC)」往返  ← 也在等
参考核 1903：     942 次切换（891 次任务相关）                      ← 被事件唤醒
本地   1888-3000：只有「空闲任务(0200957C) ↔ IRQ」往返              ← 一直等
```

⇒ **本地与参考核在 1889-1902 完全同步**（都在空闲任务里等），分歧点是
「参考核在第 1903 帧被某个事件唤醒、本地没有」。同时，四个存储区的逐字节对照
（第 1885 帧）显示本地已经和参考核几乎一样：

| 区域 | 不同比例 |
|---|---|
| ITCM 32KB（内核/分发器） | **0.00%** |
| VRAM 671KB（任务代码+图形） | **0.00%** |
| DTCM 16KB（内核数据/栈） | 0.12%（20 字节） |
| 主存 4MB | 2.19% |
| 关键寄存器（TM0-3、IPC、IF/IE/IME） | 一致（仅自由运行计数器相位差） |

DTCM 那 20 字节里最关键的是 `0x027E3FB0` 起：参考核有**一个已保存的 SVC 现场**
（`6000003F / 00001000 / 0200043E`），本地为 0；内核的「恢复任务」例程
（`0x02007F90`：`msr cpsr_c`→SVC、`msr spsr_fsxc`=任务 CPSR、
`ldm r0,{r0-r14}^`、`subs pc,lr,#4` 异常返回）本地**能正确执行**（模式确实切回
System 并落到任务 PC），只是切到的总是**空闲任务**——即就绪队列为空。

⇒ 下一步（明确）：找出「参考核第 1903 帧唤醒任务」的那个事件是什么
（本地同一逻辑点缺失）。已知本地有一笔**卡带传输停在「数据已取进 FIFO、没人读」**
的状态（`romctrl=A1C16657 rem=332 pos=180`），而该传输的完成中断
（`AUXSPICNT bit14` → IF bit19）是游戏读卡任务的唤醒源之一；
对照两侧第 1900-1910 帧的 IF/IE 变化与卡带 DMA 配置即可确认。

**21-B9yi（续5）— 唤醒事件已确认：卡带传输完成中断（IF bit19）**：

参考核第 1898-1911 帧逐帧（`REF_CART_TRACE`）：

```
f=1899..1902 ARM9=02009584(空闲) romctrl=A1416657(忙) dma3=04100010/…/AF000001(已武装+使能)
             if=00200000 ie=000C2009(含 bit19)
f=1903       ARM9=02011920(读卡循环跑起来) if=00280000(bit19 置位) ie=00042009(bit19 关)
f=1904..     ARM9=02011920/02011938/02063F1C…（任务继续跑）
```

⇒ 参考核在空闲期**把卡带 DMA 武装好并置使能**（`dma3 … AF000001`），
于是卡带侧每取到一个字，DMA 就自动搬走，整笔 512 字节传输**无需 CPU 参与**
就走到结束（`ROMEndTransfer` → AUXSPICNT bit14 → **IF bit19＝IRQ_CartXferDone**），
这个中断把游戏任务唤醒 ✓。

本地同一时刻：`dma3=04100010/…/05000001`（**使能位已清**，DMA 早已用完一轮
且没被重新武装）、`if=00200000`（bit19 **从未置起**）⇒ 卡带传输的 FIFO 取满
2 字后停下、又没人读 ⇒ 永远不结束 ⇒ 任务永远等不到唤醒（本地 IF bit19 从
1888 到 3000 帧一直是 0）。

**下一步（精确）**：对齐「已经就绪（DRQ 置位）时武装 DMA」这条**电平敏感**语义
——melonDS 源码里明确写着这是靠 hack 处理的（`RaiseDRQ → CheckDMAs(0,0x05)`，
且「DMA 在 DRQ 已置位时被设好会立刻开始」）；本地要保证：
① 武装且使能的卡带 DMA 在每个 DRQ 边沿搬走**恰好一个字**；
② DMA 用完（使能位清零）后若 FIFO 仍有数据，卡带侧照常维持 DRQ；
③ 传输结束条件只在「取数位置到块尾且 FIFO 被读空」时满足。
对齐后本地应能像参考核一样在第 1903 帧被 bit19 唤醒。

**21-B9yi（续6）— DMA 寄存器写序列对照（两边其实是同一段代码、同一个值）**：

给两边都加了 DMA 寄存器写日志（本地 `NDS_DMALOG=1`、参考核 `REF_DMALOG=1`，
参考核侧在 `RefNDS::ARM9Write32` 里记录 0x040000B0-0xDF）：

```
参考核 1903 帧：pc=01FF82F4/FC/00（ITCM 的卡带 DMA 循环）
               SAD=04100010 DAD=… CNT=AF000001（StartMode=5 卡带 + 重复位 bit25 + 使能）
本地   1888 帧：同一段 ITCM 代码、同样的 SAD/DAD、同样写 CNT=AF000001
               —— 之后本地再无 DMA 写、无 CARD_DATA 读、无传输结束事件
```

⇒ 两边的**代码路径与寄存器值完全一致**，差别只在后续推进：参考核的「卡带 DMA
＋重复位」在空闲期把整笔传输自动搬完（DMA 在 `Run9` 结束时按 melonDS 的
`CheckDMA()` 再检查一次 ⇒ DRQ 仍置位就继续搬），本地则停在「FIFO 里还剩一个字」
（`rem=332 pos=180` 那类现场），于是传输不结束、bit19 不来。

**已尝试并回退**：① 在 `dma_fire_card` 里加「搬完再检查，直到 FIFO 取空」的循环
＝语义方向正确，但本地 `dma_transfer` 里为卡带源保留了「瞬间推进卡带时钟
（`cartbus_advance(…,100000)`）」的旧 hack，会让 DMA 一次抽干整笔传输、时序
完全错开（实测游戏反而更早卡在 `0x02011C50` 轮询，且卡带用例失败）；② 去掉该
hack 后，卡带用例（一次武装 8 字）暴露的是旧用例本身不符合真机节奏
（真机 DMA 每字由 DRQ 推动），但改动牵连较大，本轮先回退保持 930 项全绿，
把「按 DRQ 逐字推进 + 搬完再检查」作为下一步的完整改造（同时改写卡带 DMA 用例
为逐字武装的真实用法）。

**③ 两者同时改（循环重检 + 去掉瞬间推进）也实测过**：卡带按 FIFO 节奏逐字推进、
游戏在第 1903 帧仍进 `0x02011C50` 轮询（比回退版更早卡住）。⇒ 说明「DMA 独立
搬完整笔」与「游戏自己的 CPU 读循环」之间的**分工**本地还没对齐：参考核里
两者是互补的（DMA 按 DRQ 搬、CPU 只在需要时读少量字），而本地先让 CPU 读了
127/128 个字、再让 DMA 接手，任务侧的计数与缓冲状态就对不上了。
下一步要先把「游戏对同一笔传输的 CPU 读次数 / DMA 搬运次数」在两边分别统计出来
（各自的读计数 + DMA 计数），确认分工模型，再动 DMA 语义；本轮保持 930 项全绿。

**21-B9yi（续9）— 读来源统计 + 两个 DMA 真 bug 修复**：

给卡带读日志加了来源标记（`src=cpu|dma`，`NDS_CARTLOG2`）后统计第 1888 帧那一笔
512 字节传输：**127 次读全部来自 DMA**（CPU 的 4744 次读发生在更早的阶段）。
据此查出两个**确实错误**的 DMA 行为并修好（都对齐 melonDS）：

1. **重复模式（CNT bit25）下地址不前进**：`dma_transfer` 只改局部 `src/dst`
   不写回通道 ⇒ 卡带 DMA（`AF000001`：模式 5 + 重复 + 源固定/目的递增）每个字
   都写到同一个 dword，整块数据被覆盖成最后一个字。现在搬完把推进后的地址写回
   通道（`dma->sad = src; dma->dad = dst;`）。
2. **DRQ 未置位也触发卡带 DMA**：melonDS 的 `NDSCartSlot::CheckDMA()` 第一行是
   `if (!(ROMCnt & (1<<23))) return;`；本地无条件触发 ⇒ 数据没就绪时把 FIFO 的
   残留值（`0xFFFFFFFF`）搬进游戏缓冲。现在 `dma_fire_card()` 先查 DRQ。

验证：`build\test_nds.exe` **930 项 0 失败**；900 帧里程碑标记
`0x027FFC30=0xFFFFFFFF`、`0x03808240=1`、`0x0380BA7C=1` 与参考核一致；
2000 帧无回归（仍停在空闲任务 `0200957C`，与上一版一致）。

**仍待解决**：把「DMA 搬完再检查一次（电平敏感）」补上后，游戏会改在第 1903 帧
进 `0x02011C50` 轮询而不是停在空闲——说明游戏侧对「这 512 字节由谁搬」的计数
仍未对齐。下一步：对比两边同一笔传输里 **DMA 目标缓冲的最终内容**（各 512 字节），
确认本地 DMA 搬出的数据与参考核是否逐字节相同；若相同，则问题在游戏任务的
收尾判断（它在等 bit19 却先看到 bit31 落 0），再据此决定是否需要让「传输结束」
与「任务唤醒」的先后关系与参考核一致。

**21-B9yi（续10）— 完整组合实验的结论（重要）**：

把「搬运后再检查 + 去掉瞬间推进 + 地址前进 + DRQ 门控」四件事**同时**实现
（单测 930 项全绿、卡带用例同步改成真机节奏的逐字武装）后，实测游戏行为：

```
恢复版（已提交）：f=1899..1905  ARM9=0200957C 空闲任务 cpsr=0000001F
                  ← 与参考核 1889-1902 的空闲状态逐帧一致
四件套组合版：    f=1900..3000  ARM9=02011C50 卡在 ROMCTRL 轮询、cpsr=A00000D2(IRQ)
                  ← 传输确实能结束了（IF bit19 置位），但死锁搬到了「IRQ 处理
                    器内的卡带命令路径」：处理器等一个只有任务侧才会推进的传输
```

⇒ 结论：**「去掉瞬间推进」这一步会把游戏从「与参考核一致的等待」推向硬死锁**，
因此本轮**只保留两个确定无疑的 bug 修复**（重复模式地址前进、DRQ 门控），
节奏/重检两件事回退；当前代码回到 `51af6b6` 状态（930 项 0 失败、1900 帧空闲
行为与参考核一致）。

**下一步（重新定向）**：不要先动 DMA 节奏，而是先解释「参考核为什么不需要
那一步」——即参考核在同一时刻的 **CPU 读 / DMA 搬运分工**（已加 `src=cpu|dma`
标记，可直接统计）以及**任务阻塞在哪个事件上**（任务表里的 wait 字段）。
把这两点对齐后，再决定是否需要「搬运后再检查」。

**21-B9yi（续11）— 参考核的读来源统计：两边分工其实相同**：

在参考核的 `DMA::Run9/Run7` 里也加了「DMA 执行中」标记（`g_ref_dma_exec`，
配合 `REF_CART_LOG` 打印），第 1903 帧的读全部是 `dma=1`：

```
cartlog: f=1903 READ ret=50702E5D pos=4  len=512 cnt=1 romcnt=A1C16657 cpsr=0000001F dma=1
cartlog: f=1903 READ ret=694C7461 pos=8  len=512 cnt=1 romcnt=A1C16657 cpsr=0000001F dma=1
…
```

⇒ **参考核的卡带数据也是 DMA 搬的**（CPU 只是待在任务里、模式 System），
与本地统计（127 次读全部 `src=dma`）**分工一致**。差别只剩「传输怎么结束」：
melonDS 的 `DMA::Run9` 每完成一个单元就再 `CheckDMA()`（DRQ 仍置位就继续搬），
最后那个字被搬走时 `ReadROMData()` 看到「取数位置到块尾 + FIFO 已空」→
`ROMEndTransfer()` → bit19。

**本轮已把「搬运后再检查」实现并实测**（含/不含「去掉瞬间推进」两种版本），
单测都能全绿，但游戏都会在第 1898-1903 帧改成卡在 `0x02011C50` 轮询——
不是原来那种「与参考核一致的空闲等待」。⇒ 说明**游戏在这之后还有一处
依赖“传输尚未结束”的时序**（例如下一个命令的排布或任务唤醒顺序），
单靠 DMA 侧补这一步会把死锁搬到别处。因此本步保持「空闲等待」这个
与参考核一致的基线，DMA 侧只保留两个已修好的确定 bug。

**下一步（定向）**：用两侧的 `dma=1` 日志做**逐字对照**（同一笔传输的第 N 个字
在两边分别在哪一帧、由谁搬），找出本地「最后一个字」消失的位置；同时检查
游戏在 `0x02011C20` 那条「写回 ROMCTRL」路径的调用者（IRQ 上下文 vs 任务），
确认参考核里它是否总在传输结束后才被调用。

---

### 21-B9yi（续12，2026-09-12 代码/对照）：卡带 DMA 改成参考核语义 —— 节奏/命令流/中断归属全部对齐，停摆机理定位

**这一轮改了 3 处代码 + 补了 7 个两侧诊断口子**，并用「同一笔传输的逐事件时间线」
把两边**逐条对齐**。结论先说：**卡带节奏、命令流、中断归属三项已与参考核逐条/逐位
一致**（改前本地 4 倍速、ARM7 恒挂 bit19）；**第 1904 帧的停摆仍在**，但机理已精确到
「谁在什么时候停 DMA」——参考核是「整块搬完 → 才停」，本地是「发完命令立刻停 →
该块剩 2 字没人读」。

#### 1. 代码修正（`src/io/dma.c`、`src/io/io.c`）

| # | 旧行为（本地） | 参考核（melonDS） | 修正 |
|---|---|---|---|
| ① | `dma_transfer()` 每次从 `CARD_DATA` 取数前先 `cartbus_advance(cb, 100000)`（21-B9zb 的「瞬间推进卡带时钟」hack） | 卡带 DMA **不**推进卡带时钟；数据由卡带侧按 `xfercycle*(4+gap)` 取进 2 字 FIFO 并 `RaiseDRQ()` | 删掉该 hack（就绪由 `dma_fire_card()` 的 DRQ 门控保证） |
| ② | `dma_fire_card()` 只在「卡带又取到一个字」的边沿触发一次 | `DMA::Run9()` 收尾 `if (StartMode == 0x05) NDSCartSlots[0]->CheckDMA();` ⇒ **电平敏感**：DRQ 仍置位就把同一通道再拉起来搬下一个字 | 改成有界（≤8 轮）的「搬完再检查」循环 |
| ③ | 卡带完成中断**两核都挂** | `ROMEndTransfer()` 里是 `SetIRQ(Num, IRQ_CartXferDone)`，`Num`=卡带槽所属核（NDS 只有槽 0 = ARM9） | 只挂 `irq[0]`（ARM9） |

①+②的净效果：卡带**每个字**由「取数 → DRQ → DMA 搬 1 字」推动，整段 512 字节在
卡带自己的节拍上搬完；最后一个字被读走时才 `ROMEndTransfer`（bit31 落 0 + bit19）。

#### 2. 节奏对照（本步最直观的成果）

```
命令流（两侧第 1886-1903 帧，含 512 字节块读 + B8 芯片 ID）:
    loc 931 条 vs ref 前 931 条 —— **逐条完全相同**（命令字节、PC、顺序）

卡带块速（每帧启动的 512 字节命令数）:
    改前 本地 205/205/206 块/帧（≈4 倍速）   ← 瞬间推进 hack
    改后 本地  52 块/帧                      参考核 51 块/帧

目标地址推进（DMA3 的 DAD，逐帧对照）:
    ref  f=1888 02087440 … f=1903 020E2840
    loc  f=1889 02086440 … f=1903 020E17B0 / 020E2A44（同步推进，差 ~0x1000 内）

中断状态（改前/改后）:
    改前 本地 if7=00080000（bit19 恒挂，参考为 0）→ 改后 if7=00000000、ie7=0104009D
         与参考核**完全一致**；if9/ie9 也逐帧一致（00042009 ↔ 000C2009）
```

#### 3. 游戏行为：卡带阶段对齐了，停摆仍在（对照必须带按键时序）

```
带按键时序（--key-frame 1700 --key-mask 0x3F7 --key-period 120）:
    f=1900-2600 本地 ARM9 恒在 0x02011C50（IRQ 模式 cpsr=A00000D2，lr=0x02011854），
                 卡带 rem=504/pos=8/fifo=2/late=1，IPC fifo=16/0（积压），
                 ARM7 也进入 IRQ 模式；disp 停在 80111418（加载画面）
    改前（21-B9yi 基线）同样在 1904 帧后停摆，但停在**空闲任务** 0200957C
    ⇒ 两轮都停；本轮把「停在哪一段代码」推进到了芯片 ID 读取路径

⚠️ 陷阱（本轮踩过）：不带 --key-frame 的 2600 帧长跑里，游戏停在标题/演示
   （attract）循环，本地会「看起来在推进」（DISPCNT 每 5 帧翻转）——那不是
   START 之后的游戏进度，不能用来判断停摆是否解除。停摆判定一律用带按键时序的运行。
```

| 观测点 | 改前（基线） | 改后 | 参考核 |
|---|---|---|---|
| 卡带块速 | 205 块/帧（≈4 倍速） | **52 块/帧** | 51 块/帧 |
| 前 931 条命令（f=1886-1903） | 一致 | **逐条相同** | — |
| ARM7 `if7` | `00080000`（bit19 恒挂） | `00000000` | `00000000` |
| ARM7 `ie7` | — | `0104009D` | `0104009D` |
| 900 帧里程碑三标记 | 一致 | **一致** | 0xFFFFFFFF / 1 / 1 |
| 帧 100 画面逐像素 | 81.9% | **81.9%** | — |
| 1904 帧之后 | 空闲任务等待（不醒） | 芯片 ID 读取路径等 ROMCTRL bit31（不返回） | 整块搬完 → 芯片 ID 读完 → 继续执行 |

#### 4. 本轮弄清的机制（游戏侧，供后续对照用）

每个 512 字节块的完整循环（两侧寄存器写序列**逐事件相同**）：

```
[任务] ITCM 0x01FF82C0 武装 DMA3：SAD=04100010、DAD=buf、CNT=AF000001
        （AF00 = 使能 + 模式 5（DS 卡带）+ 32 位 + 源固定 + **重复位 bit25**；CNT_L=1）
[IRQ ] 0x02011B14 卡带完成处理器：
        0x02011B24 → 0x020096CC 停 DMA（CNT_H=0x8500 清模式位、再 0x0500 清使能）
        指针 +0x200、剩余 −0x200
        剩余 != 0 → 0x02011BE0 → 0x02011BF4 发下一条 B7 命令（0x02011C4C 等 bit31 落 0 后写命令字节）
        剩余 == 0 → 关 bit19 → 0x02011840 读芯片 ID（B8）
[卡带] 每 20 周期取 1 字进 FIFO → 拉 DRQ → 每字一次 CheckDMA → DMA 搬 1 字
[结束] 最后一个字被读走 → ROMEndTransfer → bit31 落 0 + IF bit19 → 下一轮
```

⇒ **`CNT_L=1` + 重复位是真机用法**：整段数据靠「每字一次 CheckDMA」搬完，
不是「一次搬 `count` 个字」。本地的卡带用例也已改成这个口径（见 `tests/testlog.md`）。

#### 5. 参考核的「停 DMA 时机」实证（本步的关键新证据）

用带时间戳的两侧事件流对齐最后一笔块读 `B70018A6`（都在 f=1903）：

```
参考核（REF_IOLOG + REF_DMALOG2 + cartlog READ 合并按时间排序）：
    t=2132530983  停 DMA（CNT_H=0500）              ← 上一块收尾
    t=2132531269  武装 DMA3：SAD=04100010 DAD=020E2840 CNT=AF000001
    t=2132531477  发命令（ROMCTRL=A1416657）
    t=2132547836  DMA 开始逐字搬（每 DRQ 一次 CheckDMA，dma=1）
    t=2132553118  停 DMA（CNT_H=8500/0500）         ← **整块搬完之后才停**

本地（同一段代码、同样的寄存器值，事件顺序相同但时机不同）：
    武装 → 发命令 → **立刻**停 DMA
    ⇒ 该块的 2 个字留在 FIFO（rem=504/pos=8/fifo=2/late=1），ROMCTRL bit31 恒为 1；
      芯片 ID 读取（0x02011840 → 0x02011C4C 先等 bit31 落 0）永远等不到
      ⇒ ARM9 停在 0x02011C50、IPC FIFO 积压 16 条、ARM7 也被拖进 IRQ 模式
```

⇒ 参考核的块循环是「**武装 → 发命令 → DMA 按 DRQ 把整块搬完 → 停 DMA → 读芯片 ID**」，
本地缺的是「整块搬完」发生在「停 DMA」之前。下一轮要修的是 **ARM9 任务/IRQ 的
插入相位**（谁在什么时候停 DMA、为什么本地会提前一步），DMA 触发模型本身已经与
参考核一致，不需要再动。

#### 6. ARM7 侧嫌疑排除（回应「ARM7 FreeBIOS 低地址 Halt/调度路径大改造」）

三项证据一致指向**不需要**该改造：

1. `if7/ie7` 与参考核**逐位一致**（`00000000` / `0104009D`），ARM7 稳定停在 FreeBIOS
   Halt 的 `0x1158/0x115C`，与参考核同址；
2. 两侧低地址取指直方图此前已 **93/93** 一致（B9ye）；
3. 本轮停摆链完全落在 ARM9 侧（`0x02011C50` 的 ROMCTRL 等待、IPC FIFO 积压），
   与 ARM7 的执行路径无关。

---

### 21-B9yi（续13，2026-09-12 修复）：一次多余的卡带完成中断 → 1904 帧停摆的真正根因

#### 根因（一条日志定位）

把窗口内所有 `END` 事件按形态统计后，发现**只有一条是「没有传输在跑」的**：

```
cartlog2: f=1888 END pos=0 len=0 cnt=0 romctrl=21416657      ← xfer_len==0（没有传输）
其余 794 条 END 都是 pos=512（块读）或 pos=4（B8 芯片 ID）——真正的传输结束
```

游戏的任务计数**每个卡带完成中断减 0x200**（`0x02011B40: subs r1, r1, #0x200`）。
多出来的这一次完成中断让它**提前一块**以为整段搬完 ⇒ 处理器转去读芯片 ID
（`0x02011840` → `0x02011C4C` 先等 ROMCTRL bit31 落 0），而最后一块
（`B70018A6`）刚被发起、数据还没人读 ⇒ bit31 永远为 1 ⇒
ARM9 停在 `0x02011C50`（IRQ 模式）、IPC FIFO 积压 16 条、ARM7 被拖进 IRQ 模式。

**为什么会多读一次 `CARD_DATA`**：游戏每块收尾用「停 DMA 助手」`0x020096CC` 写
`CNT_H=0x8500`（模式 0 = 立即，CNT_L=1）再写 `0x0500`；本地把「立即模式」的搬运
**同步**执行在寄存器写里 ⇒ 这一读可能发生在**命令还没发起**的时刻（`xfer_len==0`），
于是 `cartbus_read32()` 的 `pos>=len`（0>=0）分支误判成「传输结束」。
参考核里 DMA 的搬运是**延迟执行**的（`Start()` 只置 `Running=2`，真正搬在
CPU 步进的 `Run9()` 里），等它读的时候命令已经发起（`xfer_len=512`）⇒ 不会误判。

#### 修正（`src/cart/cartbus.c`）

`cartbus_end()` 增加「真的在传输中」判据：**只有 `xfer_len != 0` 才挂完成中断**
（状态清理照旧）。这样「没有传输时的 `CARD_DATA` 读」不再产生第 19 号中断，
与「完成中断必须对应一笔真实传输结束」的语义一致。

#### 结果（带按键时序 `--key-frame 1700 --key-mask 0x3F7 --key-period 120`）

```
f=1910  本地 ARM9=0200957C（空闲任务，cpsr=0000001F）、ARM7=00001158（Halt）
        —— 改前同帧是 02011C50（IRQ 模式死等 ROMCTRL）

DISPCNT 时间线（本地 vs 参考核，同一序列、本地滞后 ~300 帧）：
    80111418（加载）→ 80111F18 → 00161F10 / 00121F10（标题/过场，5 帧翻转）
    参考核 1930 进 80111F18、2110 进 00161F10；本地 ~2250 / ~2450

GX 侧开始真正出图：f=2400 tri=8906、f=2500 tri=13359（改前一直是 tri=0）
```

| 里程碑 | 结果 |
|---|---|
| 900 帧三标记（0x027FFC30=0xFFFFFFFF、0x03808240=1、0x0380BA7C=1） | 与参考核一致 |
| 帧 100 画面逐像素 | 81.9%（与改前一致，无回归） |
| 单测 | **931 项 0 失败** |

#### 下一步

本地在加载画面阶段（f≈1900-2250）有一段**长时间空闲等待**（ARM9 停在空闲任务、
IPC FIFO 空、无卡带中断），参考核同段只用 ~30 帧就进入下一阶段 ⇒ 下一轮在这段
做逐帧事件对照，找出本地在等哪个事件（ARM7 侧消息 / 卡带中断 / 定时器），
把这段 ~300 帧的滞后收掉。

### 21-B9yi（续14，2026-09-12 分析）：滞后 ~330 帧的机理＝空闲期系统时钟不推进

（承上：停摆修好后游戏继续跑，但比参考核慢约 330 帧。本轮先把机理量出来。）

```
本地 f=2004/2005（ARM9 停在空闲任务 0200957C、ARM7 停在 Halt 0x1158）：
    cart romctrl=A1416657 rem=456 pos=56  →  下一帧 rem=320 pos=192
    ⇒ 卡带每帧只前进 34 个字（≈680 个卡带周期）
    同一时期参考核（同步读卡带阶段）每帧 6400 个字

本地 f=2240-2245：ARM9 终于开始跑同步读卡带循环（02011918/02011C/02011920）
    cart rem=312 pos=200 → rem=512 pos=0（新块）⇒ 与参考核 1905+ 同一段代码
```

⇒ 停滞期的卡带后台取数几乎停摆，原因是本地的**卡带/定时器时钟只在 ARM9 指令步进
时推进**（`io_advance_cart(io, 0, prev_cost)`，`cpu_step` 里调用）；ARM9 在空闲
任务里大量时间处于「等待」状态（WFI / 空转），推进的周期数很少 ⇒ 卡带 FIFO 与
定时器都几乎不动。melonDS 的卡带/定时器挂在**系统时钟**上：ARM9 空转（甚至 WFI）
时系统时钟照样走，`ScheduleEvent` 到点就触发，所以参考核只要 ~30 帧就走完这段。

**下一轮要改的点**：让空闲期的系统时钟继续推进（至少让 `io_advance_cart` /
`io_advance_timers` 按帧预算推进，或把卡带/定时器事件改成独立的系统时钟事件表），
再复测这段 ~330 帧的滞后。

**当前画面状态**（带按键时序，f=2600）：与参考核 f=2600 的顶屏逐像素相同率 0%
（MAD 150），与参考核 f=2300 的 MAD 71 —— 与「滞后约 300 帧」的判断一致。

---

### 21-B9yi（续15，2026-09-12 修复）：卡带时钟不再丢掉剩余周期 —— 加载阶段追平参考核

**根因**：`cartbus_advance()` 每次调用最多取「一个字」（约 20 个卡带周期）就返回，
而调用方 `io_advance_cart()` 每次只调一次 ⇒ **没用完的周期被丢掉**。ARM9 正常执行时
每次只传 1-5 个周期，损失不明显；但空闲期（ARM9 在 WFI、runner 按扫描线事件一次性
传 `delta≈2130` 个周期）就只能取到 ~290 字/帧，只有参考核（6400 字/帧）的 4.5%，
于是「等最后一块搬完」硬生生等了 ~330 帧。

**修正**（`src/io/io.c`）：`io_advance_cart()` 改成
「取字 → 触发该字的 DRQ 卡带 DMA（把 FIFO 读走、腾出位置）→ 用剩余周期取下一个字」
的循环，直到周期用完或没有待取事件（FIFO 满且无人读 / 块已取完）；有界（≤4096 轮）
避免死循环。净行为等价于 melonDS 的「卡带取数挂系统时钟事件 + 每字一次 CheckDMA」。

**效果**（带按键时序，逐帧画面与参考核对照）：

```
卡带取数速率（f=1995-2000 空闲期）：290 字/帧 → 1456~3686 字/帧
加载阶段：f=2000 本地已进入同步读卡带循环（02011938）、DISPCNT=80111F18
          （改前同帧仍在 80111418 等待，2300 帧才进下一阶段）
画面逐帧对照（本地 vs 参考核，顶屏）：
    f=1900 / 2000 / 2100  → 49152/49152 像素相同（100.0%、MAD 0.00）
    f=1900-2110 每 10 帧逐帧比对：**全部 100% 相同**
    f=2120 起分歧（见下）
```

**新的首个分歧点（下一步）**：f=2110 之前逐帧 100% 相同；f=2120 起本地已显示
下一场景（顶屏非黑），而参考核到 f=2140 仍全黑、f=2150 才出现画面
⇒ 本地这段**淡入/过场比参考核早 ~30-40 帧**。两侧 ARM9 都在 ITCM 游戏代码里
（f=2105-2130 逐帧 PC 对照），属于「某个等待（淡入计时/事件）本地提前满足」，
下一步在这 10 帧窗口内做事件级对照（定时器 / VBlank / 卡带完成中断）。

---

### 21-B9yi（续16，2026-09-12 代码/对照）：GXSTAT bit27（3D 忙）建模 + 2120 分歧的排查记录

**动机**：在 f=2110-2120 的反汇编里找到游戏真的在等 3D 引擎：

```
020046B4  ldr  r2, [pc, #0x60]      ; r2 = 0x04000600（GXSTAT）
020046B8  ldr  r0, [r2]             ; 读 GXSTAT
020046BC  ands r0, r0, #0x8000000   ; bit27 = 3D 引擎忙
020046C0  bne  #0x20046b8           ; ← 忙则原地等
```

本地此前**从不置 bit27**（GXSTAT 恒为「FIFO 空 + 不足半满」），这类等待被整段跳过。

**改动**（`src/gx/gx.{c,h}`、`src/io/io.c`）：

- `gx_t` 增加 `busy_cycles`；`gx_exec()` 按 melonDS `GPU3D::AddCycles()` 的口径给每条
  命令记工作周期（矩阵 16-35、顶点 18、交换缓冲 254、起止图元 3、其余 4-5），
  期间置 `GXSTAT bit27`；
- 新增 `gx_advance()`，在 `io_advance_cart()`（ARM9 系统时钟）里按周期消耗，
  归零时清 bit27——对应 melonDS「`Run()` 用 ARM9 时间戳推进 `CycleCount`」；
- `NDS_NOGXBUSY=1` 可关闭本模型用于 A/B 对照。

**实测**：

| 观测 | 结果 |
|---|---|
| 单测 | **931 项 0 失败** |
| 900 帧三标记 | 与参考一致（0xFFFFFFFF / 1 / 1） |
| 帧 100 画面 | 81.9%（不变） |
| f=2090-2110 逐帧 | 仍 **100% 逐像素相同** |
| f=500 画面 A/B（开/关 bit27 模型） | MAD 均 135.27（**无差异**） |
| f=2120 分歧 | **仍然存在**（MAD 589.3，与改动前逐值相同）⇒ 2120 的提前**不是** 3D 忙等待造成的 |

**2120 分歧的新证据与下一步**：

- 参考核 f=2110-2140 顶屏**全黑**（VRAM 里那块还没画/刚被清），本地同段已是完整画面；
- 两侧 DISPCNT 相同（`00161F10`，VRAM 显示 bank 1），说明**不是显示模式差异**，
  而是同一 bank 的**内容**差异 ⇒ 怀疑「清屏/双缓冲交换」语义：melonDS 的 3D
  引擎在 `SwapBuffers` 时清空后缓冲，本地是单缓冲（`gx_t.fb[256*192]`）不交换也不清，
  上一帧内容会留在缓冲里 ⇒ 参考核看到黑（刚清），本地看到旧画面。
- 下一步：把本地 GX 改成**双缓冲 + 交换时清后缓冲**（对齐 melonDS），并用
  f=2110-2150 的逐帧画面验证；必要时同时在参考核侧记录 `SwapBuffers` 时刻做逐事件对照。

**实验记录（已回退）**：把本地 GX 改成「前后缓冲 + `0x50 SWAP_BUFFERS` 时交换并清后缓冲」后：

```
f=2000-2110  仍逐帧 100% 相同（无回归）
f=2120/2140/2150  MAD 589.3 / 587.3 / 552.4 —— **与改动前逐值完全相同**
单测则出现 1 项失败（`gx fifo` 用例直接读 fb[]，交换后画到另一块）
```

⇒ 「交换后清屏」**不是** f=2120 分歧的原因，已回退（`git restore src/gx/gx.c gx.h`），
单测回到 931 项 0 失败。教训：画面差异必须用**内存级**证据定位，不能只凭「参考黑/本地有画面」推测。

**内存级证据（f=2115，首个分歧帧）**：

```
主存 4194304 字节：不同字 274055 / 1048576（26%）
其中「小数值」差异集中在 0x02077xxx 一带（≈457 处，多为 ±1/±3/±7/±30 这类计数）
   例：0x02077004 本地 0x600 vs 参考 0x400；0x0207720C 0x11A vs 0x119；
       0x02077320 0x7FF vs 0x7EA；0x0207738C 0x42 vs 0x20 …
```

0x02077xxx 正是游戏的**任务/调度上下文表**（前几轮的 `TraceSigTbl`/`ctx e4` 诊断也在这一段）。
⇒ 首个分歧是**游戏任务状态**本身（两侧任务处在不同阶段），下一步：

1. 在同一帧把两侧 0x02077xxx 的上下文表**逐字段**打出来（本地 `--dump`、参考核
   `REF_DUMP_RAM`），找出**第一个**不同的字段（不是数值差，而是「哪个任务/哪个状态」）；
2. 沿着该字段的写入方（PC）回查它上一次被写的时机，与参考核逐事件对照
   （两侧都有 `NDS_PCSAMPLE`/`REF_IOTRACE` 可用的时点信息）。

**诊断工具备注**：`--dump <前缀>` 与 `--key-*` 同时使用时，若 `--dump` 写在参数末尾，
Windows 宽字符解析可能把前缀吃掉（文件会落到当前目录且名字变成 `_mainram.bin`）。
把 `--dump` 放在参数**前面**（如 `--dump build\p_ --key-frame … --headless-frames N`）即正常。

---

### 21-B9yi（续18，2026-09-12 代码/对照）：**补上 HBlank 中断（IF bit1）** + 调度节奏线索

**动机（量化证据）**：给两侧都装上 ARM9 IRQ 采样（本地 `NDS_IRQLOG`、参考核 `REF_IRQLOG`，
窗口 f=2090-2110）后对比：

| | 每帧 ARM9 IRQ 数 | IF 位直方图 |
|---|---|---|
| 参考核 | **268-340**（f=2090-2102），2103 后降到 5-6 | bit1(HBlank) **3516**、bit19 3741、bit21 3586、bit11 169 |
| 本地（改前） | 5-6 | bit0/3/4/18/19/21，**没有 bit1** |

⇒ 参考核的 ARM9 在**每条扫描线**都被 HBlank 中断叫一次（268/帧 ≈ 263 行 + 少量其它），
而本地**根本没有 HBlank 中断**（全项目 grep 无 bit1 触发）：FFXII 用它做「边跑边加载」的
每行流式工作，缺了它本地每帧少做一大批工作 ⇒ 加载吞吐明显偏低，后面（f=2120）内容分歧。

**实现**（`src/io/io.c` `io_advance_scanline()`）：每条扫描线在 `DISPSTAT bit4`（HBlank IRQ
使能）置位时，给对应核挂 `IF bit1`（主引擎 `dispstat`→ARM9、ARM7 视角的 `dispstat7`、
副引擎 `dispstat_sub`→ARM9；含 VBlank 期 192-262 行，共 263 次/帧），与 melonDS
「每行 HBlank 起点请求中断」一致。

**实测**：

```
f=2015-2025  本地 IRQ 268/帧、IF bit1 命中 2904/2949   ←→ 参考核 268/帧（完全同型）
f=2000-2110  画面仍逐帧 100% 逐像素相同（HBlank 中断不破坏渲染）
900 帧三标记一致；帧 100 画面 81.9%（不变）；单测 931 项 0 失败
f=2120 的首个画面分歧仍在（MAD 逐值不变）
```

**新线索：游戏自身的调度节拍差 ~20%**。用 `--watch 02077000-02077010`（本地）+
`REF_WATCH=02077000-02077010`（参考核）统计**任务现场保存次数**（0x02007F40 保存例程，
每个任务切换保存一次现场）：

```
f=500  本地 11976  参考 14685   (0.816)
f=1000 本地 14125  参考 17530   (0.806)
f=2000 本地 37636  参考 41645   (0.904)
f=2116 本地 87592  参考 116395  (0.753)
```

⇒ 两侧**从第 1 帧起**调度次数就相差约 20%（本地更少），且画面仍能逐帧一致到 f=2110，
说明这是「自由运行的节拍差」，方向与「本地每帧做的流式/加载工作更少」一致。
下一步：核对**驱动该调度节拍的定时器**（ARM7/ARM9 的 TM1/扫描线节拍，项目此前对过
TM1 频率）以及 HBlank 使能窗口（本地 DISPSTAT 在 f=2014 置 bit4、f=2084 清掉；
参考核在 f=2090-2102 仍在用 HBlank），把节拍差收掉后再看 f=2120 是否自然对齐。
