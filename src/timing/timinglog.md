# timing 模块日志

## 2026-09-06 · 21-B9vw — 最小事件目标调度表

- 按大改造路线第 2 步先落地通用事件表：`timing_t` 维护系统时间戳 `now`
  与最多 8 个事件槽，支持 `timing_arm/disarm/next/advance`。
- 事件在 `advance` 推进到目标时间后触发；回调可重新武装自己。
- 验证：新增 7 项断言（最早截止、到期前不触发、按槽触发、自动解除、
  重新武装），全量 **762 项检查 0 失败**。
- 后续把 VBlank/扫描线/FIFO/卡带就绪接到该表，替换固定步进近似。
